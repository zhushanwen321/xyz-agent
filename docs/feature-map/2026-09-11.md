# xyz-agent 功能开发地图

**日期**: 2026-09-11 | **基准分支**: fix-crash-runtime（基线 af812599a） | **状态**: 长跑稳定性专项两大子项交付——crash-resilience（13 单元）+ idle-pi-reclamation（6 单元），双 Gate 绿 + design-code-sync 收敛

> 本版反映「Taiji 30+ 天长跑稳定性专项」两大交付落地后的状态。上一版 [2026-09-06.md](2026-09-06.md)（后台命令侧边栏视图）保留作当时快照。专项全链：[long-run-stability-architecture.md](../design/long-run-stability-architecture.md)（G1-G4 / E1-E7 三阶段）→ [prevention-deep-dive](../design/long-run-stability-prevention-deep-dive.md)（O1-O4 批次 1-4）→ 本版两大子项。

---

## 一、崩溃韧性中短期防治（crash-resilience D1-D7，13 单元）

### 功能概述

按崩溃面设五道防线，目标「单个组件出错不再杀死 session、崩溃后用户能自动恢复、每次崩溃都有日志可查」：extension 异步回调守卫（消灭 9/3 实锤的最大崩溃源）、renderer 错误边界与崩溃自动恢复（熔断防循环）、出站帧传输预算、历史读取/传输双层内存预算（游标分页）、pi 崩溃自动 respawn（join 语义 + 用户可见）。设计 [docs/design/crash-resilience.md](../design/crash-resilience.md)（v9），实施 [crash-resilience.impl-plan.md](../design/crash-resilience.impl-plan.md)（13 单元 5 Wave + 偏差 D1-D19）。

### 关键落点

| 防线 | 载体 |
|------|------|
| D1 extension 守卫 | `extensions/shared/ext-guards/src/index.ts`（guardStaleCtx：代际检查主判 + stale 文案兜底分诊）；6 包接入（smart-context / plan / scheduler / structured-output / subagent-workflow）；21 包普查判定表 [stale-ctx-audit.md](../../extensions/shared/ext-guards/docs/stale-ctx-audit.md)；PS-30 文案门禁探针（pi-semantics.json + check-pi-semantics.mjs）；约束 C-pi-15 |
| D2 renderer 恢复 | `apps/electron/main/window/recovery-policy.ts`（60s 滑窗 ≤3 次熔断）+ `window-factory.ts`（render-process-gone 自动 reload / 静态错误页）；错误捕获三件套 + renderer-log IPC 落盘（`apps/electron/main/logs/`，限流合并）；恢复提示条 `useCrashRecoveryNotice.ts` + CrashRecoveredBar；约束 C-proc-12 |
| D3 出站帧守卫 | `packages/runtime/src/services/message-bus/outbound-frame-registry.ts`（8 条大字段穷举）+ `message-bus.ts` push 截断（seq 语义内，drop 时 rollbackSeq）+ `transport/message-broker.ts` reply 超限 envelope；8MB 告警 / 32MB 截断双档；约束 C-comm-14 |
| D4/D5 历史预算 | 读侧：`services/session-history.ts` + `services/session/history-reverse-read.ts` + `infra/pi/session-file-streaming.ts`（32MB 预检五档 + 逆序分块尾读）；传输侧：20 turns / 640KB 双预算 + 游标分页（`session.history` RPC cursor 参数，core `useChat.ts` 翻页 + `truncated-window.ts` 窗口态 SSOT）；「加载更早」prepend |
| D5 entryStates 截断 | `packages/core/src/domain/chat/apply-entry-utils.ts`（64KB 条目截断，live/reload 共用同一截断层） |
| D6 取证与水位 | `packages/runtime/src/infra/logger.ts`（主日志 date+size 双策略轮转 + pi-crash 崩溃上下文落盘 + 5min 内存水位行）+ `apps/electron/main/logs/main-logger.ts` / `log-retention.ts`（main 侧 stderr rotation + 每日保留期复扫） |
| D7 pi 自动 respawn | `packages/runtime/src/services/session/pi-respawn.ts`（5s 延迟 + 连续 2 次熔断 + join 语义 + shutdown/删除 cancelAll）；`session.restored/restoreFailed` 入 ring 回放可见 + respawnPending 过渡态 + 对话流恢复提示；裁决修订见 [pi-exit-notification-and-respawn.md](../architecture/pi-exit-notification-and-respawn.md) §6.2（v6：lazy → bounded proactive） |
| 图片落盘 | `packages/core/src/domain/chat/image-cache.ts`（hash→路径记账，单 session 64MB 帽）+ `apps/electron/main/images/`（三通道清理） |

---

## 二、空闲 pi 回收（idle-pi-reclamation，6 单元）

### 功能概述

根治「30 天进程足迹只增不减」（实测 33 进程 3080MB，~10 个空闲 pi 各 80-141MB 常驻）：空闲 2h 且 30min 内未被查看的用户可见 session 由 reaper「最小摘除」（杀 pi 进程、保留 bus 分区订阅 seq 连续 + HistoryRebuildCache），下次使用惰性 restore（增量恢复，Gate B 实测 544ms）。维护通道（promptReload skill 变更风暴）出站+回程双腿豁免不重置空闲时钟；relay 子进程豁免（kill-on-disconnect 自理）；默认生效无需 opt-in（env `XYZ_RUNTIME_PI_RECLAIM_*` 可调）。设计 [docs/design/idle-pi-reclamation.md](../design/idle-pi-reclamation.md)，实施 [idle-pi-reclamation.impl-plan.md](../design/idle-pi-reclamation.impl-plan.md)（u1a/u1b/u2/u3a/u3b/u4 + 偏差 R1-R14）。

### 关键落点

| 层 | 载体 |
|------|------|
| 空闲信号 | `packages/runtime/src/infra/pi/rpc-client.ts`（idle signal 三写点：出站 maintenance 豁免 + 入站 pending maintenance response 排除 + sendPrompt touch） |
| 查看窗口 | `lastViewedAt` per-sid map + `session.switch` 记录 + `RelayRegistry.hasByMainSessionId` 豁免（u1b acc603d4d） |
| reaper | `packages/runtime/src/services/session/idle-pi-reaper.ts`（5min tick，豁免查询走 u3a 只读 accessor） |
| 摘除编排 | `session-lifecycle.ts` `reclaimManagedSession`（七步：占座 reclaimInflight → 代际校验 → 最小摘除，绝不复用 removeSessionEntry） |
| 阈值 SSOT | `packages/shared/src/constants.ts`（tick/idle/viewed，与 reaper fallback 双源一致性有守卫测试） |
| 前提约束 | C-state-12（replicated-state pollTimer 全关 = 回收保留 projection 的前提） |
| 集成测试 | `packages/runtime/test/`（real-pi 端到端回收集成用例，CI 侧 XYZ_SKIP_REAL_PI=1 仅本机真跑） |

---

## 三、边界与登记残留（下一 Phase 启动前须知）

- **V6 七天长跑 soak**（约 2026-09-18 到期）：足迹平台期 / 重启一致性 / PTY 存活 + P4 relay 真机腿 + 阈值校准——登记于 idle impl-plan §6，无自动提醒。
- **未交付面**（专项审计 2026-09-11 裁决 drifted）：架构阶段二 E1 统一崩溃台账 / E7 诊断导出、阶段三 E5 看门狗滚动重启未交付（E5 条件缓建重评估未执行）；批次补课项：pi tee size 轮转（O2-C）、renderer 入站 parse 守卫（O3-D）、截断反转（O3-C）、lint 防回归规则（O1-2 半边）。
- **待用户裁决**：A3 打包版复验（挂 prerelease）；CI real-pi 凭据注入。

## 四、继承条目

- 后台命令侧边栏视图：见 [2026-09-06.md](2026-09-06.md)
- pi 边界可靠性（语义吸收层四支柱）：见 [2026-08-28.md](2026-08-28.md)
- 用量统计页（Settings → 用量）W1-W5：见 [2026-08-25.md](2026-08-25.md)
- v3 视觉重建 / 双 Panel / Overview：见 [2026-06-20.md](2026-06-20.md)

---

## 五、关键文档索引

| 类别 | 文档 |
|------|------|
| 顶层架构 | `docs/design/long-run-stability-architecture.md`（E1-E7 三阶段，阶段二/三部分未交付） |
| 根治调研 | `docs/design/long-run-stability-prevention-deep-dive.md`（O1-O4 批次总表） |
| 设计 | `docs/design/crash-resilience.md`（v9）· `docs/design/idle-pi-reclamation.md` |
| 实施计划 | `crash-resilience.impl-plan.md`（13 单元 + Gate A/B 记录）· `idle-pi-reclamation.impl-plan.md`（6 单元 + Gate A/B 记录） |
| 架构裁决修订 | `docs/architecture/pi-exit-notification-and-respawn.md`（v6：§6.2 lazy → bounded proactive） |
| 约束登记 | C-pi-15（stale 守卫）· C-state-12（pollTimer 全关）· C-comm-14（出站帧守卫）· C-proc-12（renderer 熔断）——[constraints.json](../constraints.json) SSOT |
| 旧版地图 | [2026-09-06.md](2026-09-06.md) · [2026-08-28.md](2026-08-28.md) · [2026-08-25.md](2026-08-25.md) · [2026-06-20.md](2026-06-20.md) |
