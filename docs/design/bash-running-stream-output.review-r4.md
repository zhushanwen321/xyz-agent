# 第 4 轮聚焦复审（终审确认）——bash-running-stream-output.md

> 审查依据：`~/.agents/skills/tech-design/review/rubric-design-doc.md`（跳过 P0-12/19/20，归影响面审）。本轮只聚焦三件事：① r3 三条 suggestion 修复是否成立 ② 新三步管线规格在 §4 图 / §7 U1 / §8 单测 / §9.2 四处是否联动一致 ③ 设计是否就绪。不重查 r1-r3 已确认项。

## Summary

0 must-fix, 1 suggestion（INFO 级）。**设计就绪，可进入实施（DoR 达成）。** 三条 suggestion 修复全部成立，三步管线规格四处联动核对一致，未发现新攻击面。

## Findings

| 优先级 | 位置 | 维度 | 描述 | 修复方向 |
|--------|------|------|------|----------|
| SUGGESTION (INFO) | §4「WS ring 代价」段（行 168） | P1-8 细节 | 该段概括截断规格为「换行锚优先、无换行硬切回退码点边界」，未点名 ANSI 残片推进一步——与 D4 三步管线表述不同步。该段是量级论证的简要引用且显式「见 D4」，不影响决策 | 实施时可顺手补齐为「硬切回退（三步管线，见 D4）」；不阻塞 |

## 聚焦项逐条判定

### ① r3 三条 suggestion 修复是否成立

**S1（ANSI 残片，同源影响面审 MF）——成立。** D4 硬切回退分支升级为三步管线：硬切 → ANSI 残片推进（判据完整：向前最近 `\x1b` 到截断点之间无 CSI 终止字节 `0x40-0x7E` 即视为序列内 → 起点推进到截断点后首个 `\x1b`，残段丢弃）→ 码点边界回退（最后执行，显式声明保证推进后起点同样安全——这一顺序声明封住了「推进后起点劈代理对」的连带攻击）。残余字面渲染（无后续 ESC）按四条件交集登记为已接受代价，四要素齐备。判据本身技术上正确：CSI 终止字节范围 `0x40-0x7E` 与 ANSI 规范一致。U1 要点与单测用例（CSI 中段）同步补齐。

**S2（grapheme cluster 劈开）——成立。** D4 显式判定 ③ 登记接受：视觉级非数据损坏、100ms 后续帧自愈、不上 Intl.Segmenter——量级/恢复/判定三要素在，减法裁决理由充分。

**S3（UX 代价重审触发条件）——成立。** D4 已接受代价 ① 补「重审触发：用户报告 running 期误以为输出丢失」，闭环。

### ② 三步管线四处联动一致性

| 位置 | 表述 | 一致性 |
|---|---|---|
| §6.4 D4 | 权威规格：两分支 + 硬切三步管线 + 不变式「截断后 ≤8KB 无例外」 | 基准 |
| §4 修复后图 | 「原文尾窗截断 ≤8KB」+「帧经 U1 瘦身后有界，见 D4」显式引用 D4 | 一致（§4 ring 段概括句漏 ANSI 一步，见上表 INFO） |
| §7 U1 | `normalizeWithTailCap` 规格逐字对齐 D4 三步；单测点名「无换行硬切（含代理对边界、截断点落 CSI 序列中段两个用例）」 | 一致 |
| §8 单测职责 | 「尾窗截断两分支（换行锚 / 无换行硬切含代理对边界与 CSI 中段残片推进）」 | 一致 |
| §9.2 U1 行 | 「尾窗截断（换行锚 / 硬切回退两分支）……（代理对边界、CSI 中段）」 | 一致 |

交叉核对补充：不变式条件化表述（「当 outputRaw 存在时 output === stripAnsi(outputRaw)」）在 D4 / §7 U1 / §8 单测三处逐字一致。S5 验收命令 ②（`tr '\0' 'a'`）只覆盖硬切 + 码点边界，ANSI 残片推进交由单测覆盖——分层合理（构造「截断点恰好落 CSI 中段且无后续 ESC」的真实 bash 命令不可控，单测是正确层），观察项无需扩。

另核对影响面审 suggestion 回写：per-session ring 聚合（>5 并发 ≈ ≤80MB）已入 D4 量级与重审条件；两层截断语义边界（8KB 尾窗 vs pi 50KB 快照 + fullOutputPath 恢复通道）已登记 D4 显式判定末段；§9.2 U1 测试描述已点名两分支——全部落位。

### ③ 设计就绪结论

**就绪。** 五段骨架完整（P0-1/2/3）、根因三层因果链打到底（P0-4/10）、关键事实全部带源码锚点（P0-11）、验收 S1-S5 真实可执行且含邻居不变量与反向场景（P0-13/14/15/21）、代价四要素齐备（P0-20 域内自查）、无运行时无探针断言（P0-16）。四处联动一致，唯一残留为 INFO 级概括句不同步，不构成 DoR 阻塞。
