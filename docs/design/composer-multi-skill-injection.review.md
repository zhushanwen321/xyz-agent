# 技术设计审查报告：composer-multi-skill-injection.md

> 审查人：tech-design 主审（P0-1~11、P0-13~18、P0-21、全部 P1）。对抗式基调：默认怀疑，逐项找反例，被证据说服才放行。
>
> **R1（初版审查）**：3 must-fix + 5 suggestion + 5 INFO → 已全部处置。
> **R2（第 2 版复审，本段）**：0 must-fix + 4 suggestion。三个 must-fix 修复均经攻击验证成立；修订新增的全部事实声称已 read 源码核实为真。

---

## R2 Summary

0 must-fix, 4 suggestions.

## R2 Findings

| 优先级 | 位置 | 维度(rubric 编号) | 描述 | 修复方向 |
|--------|------|------|------|----------|
| SUGGESTION | §3.3 D1 | P0-10 对抗（残留缺口） | **正则 `[ \t]` 不含全角空格（U+3000）与 NBSP（U+00A0），与「对齐 `#`/`$`/`@` 模式」宣称不一致**。实装四符号用 `\s`（`(?:^|\s)`，`input-dom.ts:237-254`），JS `\s` 含 U+3000（input-dom.ts:238 注释明示「含全角空格」）；新 skill 正则字符类 `[ \t]` 二者皆不含——中文输入法敲全角空格后键 `/` 时 `#` 浮层能触发而 skill 浮层不能，G1 宣称的「对齐 `#`/`$`/`@` 的体验」在中文高频输入形态上有缺口（本产品主用户群中文） | 字符类改 `[^\S\n]`（= `\s` 减换行，语义恰为「任意空白但非换行」，同时保住与命令正则 `^`/`\n` 分支的互斥）或显式 `[ \t\u00a0\u3000]`；D1 正文同步注明该字符类与 `\s` 的关系 |
| SUGGESTION | §3.3 D6 | P0-11/P0-16（推演补强） | **「等效阈值推演」未覆盖代码密集非 CJK 内容的 ÷4 低估边界**。推演只写了「英文内容 ÷4 准确」——对自然语言成立，对代码/base64/长标识符不成立（实际密度约 0.3~0.5 token/char > 0.25，÷4 低估 1~2 倍）。本项目多个 SKILL.md 代码占比高且不含中文（无 CJK ×1.0 富余缓冲），纯英文重代码内容估算可系统性低于真实值。已登记的「漏判-兜底关系」覆盖此场景（overflow 报错链 + 一次性 retry），且死亡尺寸论证对中文成立（×1.0 上界 ≥ 真实密度），故不阻塞——但推演一节应把该边界写明，且检查点 6 的校准对象应显式包含「代码密集型 SKILL.md」样本；另 CJK 字符类定义（全角标点/假名归 CJK 还是 ÷4 侧）实施时需写明（全角标点归 ÷4 低估 4 倍，但占比 <10%，被汉字富余覆盖，可接受需一句说明） | 等效阈值推演补第三行「代码密集非 CJK：÷4 低估，属已登记漏判类，由 overflow 兜底」；检查点 6 样本集加入代码密集型 SKILL.md；实施时写明 CJK 字符类正则 |
| SUGGESTION | §3.4 第 3/4 行 ↔ §4 | P0-13/P1-10（验收缺口） | **两个新错误场景无验收入口**：① fail-safe（contextWindow 获取失败 → 降级标记模式）是 R1-MF1 的核心修复决策，其行为（降级 + 提示 + RPC 恢复后自动回全文注入）无任何验收场景；② hook 改写破坏标记（D8 新增场景）同样无验收。另 fail-safe 触发的降级若复用场景 2③ 的「已按标记模式注入（预算超限）」提示文案会误导用户（真实原因是 RPC 故障而非超预算，用户会去删 skill/换模型而无效） | 补场景 2 变体（轻量）：临时屏蔽 `get_session_stats` RPC（或断开其响应）发送含 skill 消息，断言：降级标记模式生效 + 提示文案区分「获取失败降级」与「预算超限」+ RPC 恢复后下一条自动回全文注入；hook 破坏标记若难以真实构造，在 §3.4 该行注明「实施期验证项 + 不进验收的理由」 |
| SUGGESTION | §3.3 D10 效果栏 | P1-8（交叉引用笔误，R1 漏抓） | **D10 效果栏「消除大文本注入后的坏帧风险（验收场景 8）」错指**——readline 分帧防御的验收是**场景 9**；场景 8 是 pi 格式漂移探针（D5 的验收）。两决策的效果栏验收指向互换会让实施者对错验收口径 | 改为「验收场景 9」 |

## R2 修复验证记录（R1 findings 逐条复审）

### Must-fix 修复验证（全部成立）

**MF1 fail-open 自相矛盾 → 已修复（D6/§3.4）**。改 fail-safe：contextWindow 获取失败 → 降级标记模式；被否栏论证「`get_session_stats` 失败预示 RPC 异常，放行大消息落入 §2.3 失败模式 C 与 G3 直接矛盾」成立。攻击验证：① RPC 持续故障期每条消息降级——降级本就是安全路径（skill 经标记仍可用），无标记的纯文本消息不进预检逻辑（D6「展开前预估」蕴含无标记即 no-op），不受影响；② 恢复指引「RPC 恢复后下一条自动回全文注入」+ §3.4 第 5 行「发送后救援（fork/换模型/新 session）+ 会话内无法自愈」诚实闭合。G3 因果链闭合。残留小点见 SUGGESTION #3（提示文案区分）。

**MF2 D1 正则仲裁 → 基本修复（D1）**。正则改 `/[ \t]\/(\S*)$/`，仲裁规则写入被否 1（行首含换行后新行行首归命令浮层）。攻击验证：① `文字\n/xx`——DOM 换行为 `<br>`/块边界产生新文本节点（beforeCursor = `/xx` 节点开头）命中命令正则 `^` 分支，skill 正则要求 `/` 前有 `[ \t]` 字符故不命中；单节点内含 `\n` 的罕见形态命中命令正则 `\n` 分支，skill 正则同样不命中——两触发域互斥无重叠，验证成立。② 连续空格/tab 后 `/`：`[ \t]` 匹配紧邻字符，行为与 `#`（`\s`）一致。③ 残留缺口：全角空格/NBSP 不在 `[ \t]`，见 SUGGESTION #1。

**MF3 chars/2 虚假保守声称 → 已修复（D6）**。删「双向保守」，改 CJK 感知公式（CJK×1.0 + 非CJK÷4）+ 等效阈值推演（英文 ~80% 准确、中文 48%~80% 提早方向安全）+ 漏判-兜底诚实声明（中英混排密度异常/contextWindow 虚标的漏判由 overflow 报错链兜底）+ 死亡尺寸论证（20000 真实 token 中文内容按 1.0 上界估算 = 20000，不放行）+ 场景 2⑤ 真实 usage 校准 + 检查点 6（偏差 >30% 调系数）。声称与实际能力对齐，校准回路闭环。纯中文方向安全性数学成立（×1.0 ≥ 任意 ≤1 密度）；残留「代码密集非 CJK」边界见 SUGGESTION #2。

### Suggestion 修复验证（S1-S5 全部成立）

- **S1 改动地图 renderer 提示**：✓ 新增 Composer.vue 转发 + renderer 提示呈现条目（含场景 2③/3② 验收依赖 justification）+ P3 拆分补「提示呈现」。
- **S2 场景 9 例外声明**：✓ 例外理由（U+2028 无法依赖真实模型可靠构造 → 协议层构造帧为标准做法）+ 端到端路径由场景 1/4 覆盖 + 对照组设计保留。
- **S3 D5 pi TUI 声称降级**：✓ 改为「整条=单 block 仍可折叠渲染；混排显示 XML 原文登记 §3.5-④」——与 pi `parseSkillBlock`（`agent-session.js:45-53` 整条锚定正则）实装一致。
- **S4 日志路径**：✓ §3.4 改 `<getDataDir>/logs/`（架构约定）。
- **S5 两条 RPC 开销**：✓ 检查点 2 扩为 get_session_stats + get_commands 双 RPC 缓存评估（含失效事件：skillCacheInvalidated / 模型切换）。

### INFO 处置验证（3 条全部落实）

- INFO-3 行号：✓ §2.3 已改 `:308-352`。
- INFO-4 幂等：✓ D9 改结构化保证（dispatcher 单次调用），并明说「不做文本级幂等检测（grep 会被手打字样欺骗）」——比建议更彻底。
- INFO-5 非贪婪截断：✓ D7 已知边界登记 §3.5-③（四要素 + 误还原升级路径的消除方案 + 重审条件）。

### R2 新增事实核实（P0-11，全部属实）

| 文档声称 | 核实结果 |
|---|---|
| `apply-entry-convert.ts:35-47` parseSkillBlock 现状正则**前置正文丢失缺陷**（捕获组从 `<skill` 开始，block 前正文丢弃） | ✓ 属实（:36 正则捕获组 1/2/3 = name/location/后置文本，前置正文不在任何捕获组）——D7 要求升级时修复，正确且必要 |
| 该函数是三链路共用转换 SSOT（live `message_end` / runtime 历史重建 / 文件重放） | ✓ 调用方覆盖 core `effects/registry.ts`（live）+ runtime `session-store.ts`/`entry-tree-builder.ts`（重建）+ apply-entry.ts `replayEntries`（重放） |
| `store.ts:159-175` 第三判据 + `:128-130` 现状失配先例（「skill 展开消息自然失配 → 落回现状」） | ✓ 属实（:163 注释原文在案） |
| §3.5-⑤ 五消费方（UserBubble.vue:101 复制 / :221 draftText 回填、summarize-turn.ts:21、notify-toast.ts:56、useMessageStreamScroll.ts:61） | ✓ 行号与行为全部属实 |
| 检查点 1「已核实」：pi `skills.js:90-110` `createSkillSourceInfo` 含 filePath + baseDir | ✓ 属实（:90-112，sourceInfo 带 baseDir） |
| P2 列出测试文件（segments.test.ts / store.test.ts / turn-skill-badge.test.ts / apply-entry-equivalence.test.ts） | ✓ 全部存在 |

### 交叉引用一致性终检（五处联动）

- **D1 正则**：D1 正文 ↔ P3 拆分 ↔ 改动地图（「空格/tab 后 `/`」）三处一致 ✓
- **fail-safe**：D6 ↔ §3.4 第 4 行 ↔ §3.2 风险栏（漏判兜底）一致 ✓
- **D7 兜底**：D7 ↔ 场景 4③ 正文保留断言 ↔ P2（core SSOT 升级 + 缺陷修复）↔ 改动地图 `apply-entry-convert.ts` 一致 ✓；场景 4⑤ 存量格式回归 ↔ D7「存量兼容」一致 ✓
- **D9 挂载顺序**：D9（hook 后、client.prompt 前，实装顺序 :87→:137 已核实）↔ P1 ↔ 改动地图一致 ✓
- **§3.5 五项登记**：①↔store 判据、②↔JSONL 累积、③↔D7 边界、④↔D5 声称、⑤↔segmentsToText 双用途——各自闭合，四要素（量级/恢复/重审/判定）齐备 ✓
- **§3.1 场景 2 ↔ §4 场景 2**：两个大 skill / 6 万字符 / CJK 为主 / 估算 6 万 token，一致 ✓
- **D10 效果栏 → 场景 8**：✗ 错指（应为场景 9），见 SUGGESTION #4——R1 漏抓，本轮补上
- 变更历史记录第 2 版修订摘要 ✓

### 结论

三个 must-fix 修复经对抗验证成立，方案内在矛盾已消除（fail-safe 闭合 G3 因果链、触发域互斥、估算声称与能力对齐 + 校准回路）；修订版新增的 D7 core SSOT 升级（含前置正文丢失缺陷修复）经核实是真实且必要的补强。剩余 4 条 suggestion 均不阻塞实施——设计达到可实施就绪度（DoR）。
