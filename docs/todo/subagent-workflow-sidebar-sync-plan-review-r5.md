# subagent-workflow-sidebar-sync 第五轮联合审查报告（R5）

> 审查对象：设计文档 `subagent-workflow-sidebar-sync-design.md`（v6）+ 开发计划 `subagent-workflow-sidebar-sync-plan.md`（联合审查）
> 前序报告：R1（`-plan-review.md`，4 MF / 10 SG）、R2（`-plan-review-r2.md`，1 MF / 10 SG / 3 INFO）、R3（`-plan-review-r3.md`，4 MF / 6 SG / 2 INFO）、R4（`-plan-review-r4.md`，2 MF / 6 SG / 4 INFO）、设计 v1 审查（`-design-review.md`）
> 审查依据：`rubric-design-doc.md`（P0/P1 清单）+ 项目 AGENTS.md + 源码交叉核实（声称事实前均已 read：extension `subagent-service.ts` / `subagent-actions.ts` / `session-file-gc.ts` / `alive-store.ts` / `record-store.ts` / `index.ts` / `interface/subagent-tool.ts`，runtime `subagent-extractor.ts` / `subagent-status.ts` / `event-interpreter.ts`，pi-mono `session-manager.ts`，**以及本机真实 session JSONL 的 toolResult details 实测**）
> 审查身份：对抗式第五轮（验证轮）——聚焦 R4 全等级修复质量核对、v6 新增机制对抗（sessionFile 链 / 级 6 时间边界 / normalize 裁决 / 场景 9 可执行性）、全文档收口一致性；已修复问题不重复报，除非修复本身有错。
> 本报告只报告不修改任何文档。

## Summary

2 must-fix, 6 suggestions, 3 info.

总体判断：R4 的 12 条（2 MF / 6 SG / 4 INFO）中 8 条完全修复成立、3 条部分成立（各带一个新缺口）、1 条**修复无效**——本轮用「代码链 + 真实 session JSONL 实测」双重验证发现：**v6 sessionFile 优先级链的第 1 级（R4-MF-2 修复的核心）建立在被证伪的事实上**。`subagent-actions.ts:488` 的 details 副本取 `d.sessionFile`，而 `d.sessionFile = handle.sessionFile = record.sessionFile`（subagent-service.ts:698），`execute()` 在 `kickOffBackground`（fire-and-forget spawn，:697）后立即返回——此刻子进程握手尚未发生、`record.sessionFile` 尚未赋值（赋值点在 session-runner.ts:1072/1099，均为异步握手后），故 **start toolResult 落盘时 `message.details.sessionFile` 恒为 null**。本机真实 JSONL 实测确认（`~/.pi/agent/sessions/.../2026-08-10T05-01-45-*.jsonl` 两条 start toolResult：content 与 details 的 sessionFile 均为 null）。后果：one-shot 最高频路径在 60s 窗口期内四个 sessionFile 来源全部为空（chatMode notify 才透传、list 依赖 LLM 调 list、findSubagentSessionFile 被限 startedAt 存在而 startedAt 来自 notify），级 4（`.alive`+pid）与级 6（子 JSONL 收尾）**结构性不可达**——秒级信号触发的重拉将落 §7.2「sessionFile null 匹配不到 → 保守投影 error」：成功任务误报 error（应为 done）、T+0s 新任务误报 error（应为 streaming），与 §5.1 时间线两处直接矛盾（MF-1）。第二个 must-fix：级 6 的支撑断言「孤儿至少跑过、文件必然存在」被 `session-file-gc.ts` 证伪——扩展在每次 session_start 概率性（5%）清理 mtime > 30 天的子 session JSONL 及其 `.alive`/`.finalized`/`.cancelled` sidecar（index.ts:419 接线，无差扫全目录），级联关闭 / notify 丢失的记录（终态只存在于子 JSONL）在 GC 后落级 6「文件不存在 → streaming」且**无时间上界**——「永久 running」原症状在 30 天时间尺度复发，违背目标 1/3（MF-2）。维度 3 收口扫描：决策编号（1-7 + 6.1/6.2/6.3）、探针编号（A1-A10）、级数（六级）体系全部一致，仅场景计数两处漂移（设计 §8 结论「6 个」/ 计划 §8 标题「8 场景」，实际 9）与轻量事件发起点两处残留（§7.4 / P5 只写「终态转换点」）。场景 9（chatMode 等续聊）可执行性核实通过：`conversation` 参数真实存在（`Type.Optional(Type.Boolean(...))`，tool description 自带 `{"action":"start",...,"conversation":true}` 示例）。

## Findings

| 优先级 | 位置 | 维度 | 标记 | 描述 | 修复方向 |
|--------|------|------|------|------|----------|
| MUST_FIX | 设计 §6.5 sessionFile 优先级链第 1 级 / §5.1 T+0s 与 T+40s / §7.2；计划 P4 extractor 行「新增解析点——数据现成」与单测 fixture「sessionFile 优先级链：toolResult details 有值 > …」 | 维度 1+2 / P0-11 + P0-10 | 新发现（R4-MF-2 修复无效） | **sessionFile 链第 1 级「toolResult entry 的 `message.details.sessionFile`（start 时即有，数据现成）」被证伪**。已 read + 实测双重核实：① 代码链——`execute()` 的 background 返回 `sessionFile: record.sessionFile`（subagent-service.ts:698），而 `createRecordForMode`（:1311-1347）不设 sessionFile，赋值点全在子进程握手之后（session-runner.ts:1072 `record.sessionFile = r.sessionFile` / :1099 header 推导 / :1289），`kickOffBackground` fire-and-forget（:697，注释明言「background 包 detached 立即返回 id」）；② adapter `d.sessionFile ?? null`（subagent-actions.ts:488）；③ **真实 JSONL 实测**：本机 2026-08-10 session 两条 start toolResult 的 `message.details.sessionFile` 均为 `null`（与 LLM content 相同）。R4 报告的「runtime 实时路径 event-interpreter:554 已消费 details」只证明消费代码存在，不证明值非空——该实时路径（:572 `typeof details.sessionFile === 'string' ? … : null`）一直拿到 null（现状盲区，因内存记录的 sessionFile 只影响 drawer 功能而未被察觉）。**后果链**：one-shot 运行中与成功收尾的窗口期内（0 ~ 60s+turn，恰是秒级信号触发重拉的时刻），四个来源全空——级 2/3（sidecar 路径由 sessionFile 推导）不可探测、级 4（`.alive`+pid）不可探测、级 6（读子 JSONL）无路径 → 落 §7.2「匹配不到时保守投影 error」：**成功任务秒级显示 error（窗口 flush 后级 1 修正为 done，产生 error→done 抖动）；T+0s 新任务显示 error（与 §5.1「侧栏出现 A(streaming)」直接矛盾）**。即使把级 6「子 JSONL 不存在 → streaming」宽松解读为覆盖 sessionFile-null（与 §7.2 文本冲突），窗口期内也只能显示 streaming 而非终态 done——目标 1 的「秒级终态收敛」对最高频路径不成立。计划 P4 的链 fixture「toolResult details 有值」会造出生产不存在的数据，单测全绿掩盖恒 null | 任选其一或组合：① 扩展侧让 start 的 toolResult 携带真实 sessionFile（握手后回填不可行——toolResult 已落盘；可改为 spawn 前确定性预推导子 session 文件路径并写入 record/details）；② runtime 侧把链第 4 级 `findSubagentSessionFile` 的时间锚从「notify.startedAt（窗口期缺失）」扩为「主 JSONL entry 自带 timestamp（SessionEntryBase.timestamp，已核存在）」——start toolCall entry 时间 ≈ 子文件名 ISO 时间戳，60s 匹配窗口内可用，使级 4/6 在窗口期内可达；③ A10 候选 a 的轻量事件 payload 携带 sessionFile（作为链的第 2.5 级）。同时：§7.2「匹配不到 → error」限定终态语境（与 SG-5 合并处理）、P4 链 fixture 改为「details 恒 null + entry timestamp 兜底」的真实形态、§5.1 时间线按真实来源改写 |
| MUST_FIX | 设计 §6.5 级 6 / §5.2 kill -9 subagent 行 / 场景 7；计划 P4 级 ⑥ | 维度 2 / P0-11 + P0-16 | 新发现 | **级 6 支撑断言「孤儿至少跑过、文件必然存在」被 `session-file-gc.ts` 证伪，且「文件不存在 → streaming」无时间上界**。已 read 核实：`maybeCleanupExpiredSessionFiles`（session-file-gc.ts:33-45）在**每次 session_start** 调用（index.ts:419）、内部 5% 概率触发，递归扫 `<agentDir>/subagents`（与 runtime `getSubagentSessionDir` 的 `<piAgentDir>/subagents/<enc>/sessions` 同一棵树）unlink mtime > 30 天的 .jsonl 及同名 `.cancelled`/`.finalized`/`.alive`（:82-95，仅活进程豁免）。终态只存在于子 JSONL 的记录（级联关闭——v6 明言落级 6；pi kill-9 后 notify 未 flush 的 one-shot——场景 7 的依赖；idle record GC 归档——subagent-service.ts:454-475 只 archive 不 notify 不写 sidecar）在 30 天 + GC 触发后：主 JSONL 仍在（pi sessions 目录不在扫描范围）而子文件与全部 sidecar 已删 → 级 1 miss（无终态 entry）、级 2/3/4 miss（sidecar/.alive 已删）、级 5 miss → 级 6「子 JSONL 不存在 → streaming」**永久生效**——「永久 running」原症状在 30 天时间尺度复发，违背目标 1（终态收敛）与目标 3（恢复一致），且「必然存在」断言无探针（P0-16）。级 6 的 startedAt 窗口宽限只挂在 A10 冷路径探针的条件分支上，不覆盖本场景 | 级 6「子 JSONL 不存在/不可读」分支加无条件的时间判别（start entry timestamp 距今 > 软超时/窗口期且无任何终态迹象 → 不再保守 streaming，投影 error 或 stopped 并标注「子 session 已清理」）；或明确接受并在 §5.2/级 6 说明「30 天 GC 后历史孤儿记录显示保守态」的残余风险与边界（当前文档声称「必然存在」是不可辩护的硬断言）；顺带在 A10/级 6 探针项补一条 GC 交互验证 |
| SUGGESTION | 设计 §8 章结论（line 369）；计划 §8 标题（line 238） | 维度 3 / P1-8 + P0-13 | 修复引入 | **场景计数漂移两处**：设计 §8 章结论仍是「用 **6** 个真实场景验证」而 §8.2 表已有 9 行（1/2/3/9/4/5/6/7/8，v6 增场景 9 未同步结论句——该数字自 v2 增场景 7/8 起就未更新过）；计划 §8 节标题「跑设计 §8 全部 **8** 场景」而同节 DoD 写「**9/9** 场景 PASS」、§0 总览表写「设计 §8 的 **9** 场景」——同文档三处两种数字 | 设计 §8 结论改「9 个」；计划 §8 标题改「全部 9 场景」 |
| SUGGESTION | 设计 §7.4 第三条 bullet（line 362）；计划 P5 开发内容表第二行（line 213） | 维度 1+3 / P0-12 | 漏修（R3-SG-1 清理不全） | **轻量事件发起点集合残留两处只写「终态转换点」**：设计 §7.4「（可选，A10 候选 a 时）**终态转换点**发轻量状态事件（含 idle 位……）」与计划 P5「轻量状态事件：**终态转换点**发 `{customType:'subagent-state-changed', idle 位}`」——与决策 6.3（§6.6）、§6.5 waiting 数据源段、A10 行、计划 P0-A10 候选 a 补充（四处均已是「终态转换 + 轮次完成 idle 位翻转」）不一致。按 §7.4/P5 字面实施（只挂终态转换点）则 chatMode 等续聊期间无 idle 位 entry、waiting 在 A10a 落地后仍不可达——正是 R3-SG-1 要防的断链 | 两处补齐「发起点 = 终态转换 + 轮次完成（idle 位翻转），见决策 6.3」 |
| SUGGESTION | 设计 §6.5「判定逻辑落点」段（line 295）；计划 P4 subagent-status.ts 行 | 维度 2 / P0-12 | 修复引入（R4-SG-5 裁决的输入域缺口） | **normalize 输入域清单缺 `'running'` 与 falsy 输入的归宿**：计划 P4 归一目标只列 done/completed/success→done、failed/error/crashed→error、cancelled/canceled→cancelled、active/pending→streaming、default→error。但按裁决「组合层先按 `status === 'closed'` 分流，normalize 只处理非 closed 输入」，非 closed 输入域包含 `'running'`（bg-notify 轮次 entry status='running'；listResponse item status='running'——现状 extractor:227-229 的 `normalizeSubagentStatus(notify.status \|\| listItem?.status)` fallback 路径今天就会传 running），两文档均未给 running 映射 → 落 default → **error**（把「在跑」误判「出错」）。同理现状 `if (!status) return 'running'`（subagent-status.ts:17）的 falsy 分支在新枚举下无值可返（running 已退役），两文档未裁决。连带：unknown 新值（扩展未来新增非终态细分）也走 default→error，方向可辩护但应显式声明输入域假设 | 归一目标补 `running → streaming`（或显式声明「running 在级 5 拦截、normalize 永不接收，list item status 路径废弃/保留」的裁决）+ falsy 输入归宿（undefined → streaming 对齐「无状态信息保持初始认知」的既有注释理由）；P4 单测补 running/undefined 输入断言 |
| SUGGESTION | 设计 §6.5 映射表死行注（line 282）；计划 P4 单测矩阵行（line 190） | 维度 1 / P1-5 + P0-11（细节级） | 修复引入 | **「parent-* 仅 normalize 兼容层保留」层级混淆 + 「级联关闭实际落级 6」对 chatMode 记录不成立**：① normalize 的签名是 `(status: string \| undefined) => SubagentStatus`（subagent-status.ts:16），只收 **status** 单值；parent-* 是 **closedReason** 值，永远进不了 normalize——「parent-* 三值仅在 normalize 兼容层保留」「closedReason 的 parent-* 三值只进 normalize 兼容层单测」按字面不可实现（normalize 单测无法接收 closedReason）；正确落点是组合映射表的死行标注（表内已做）或投影函数对 closed+parent-* 的防御分支。② 死行注「级联关闭实际落级 6（无任何磁盘终态信号 → 子 JSONL 末行 → done/error）」：带轮次 entry 的 chatMode 记录（/new、/fork 时已跑过至少一轮）在级联关闭后，最后 bg-notify entry 仍是轮次 running entry → 命中级 5 → **waiting（永久，非 done/error）**—— blanket 声明对该子集失实；P4 fixture「级联关闭（无 entry、无 sidecar、`.alive` 残留、pid 死）→ 级 6」只覆盖无 entry 形态 | 死行注/P4 措辞改为「parent-* 在组合映射表保留死行标注（防御未来通知策略变化）」；级联关闭落点补分类说明（无轮次 entry → 级 6 done/error；有轮次 entry → 级 5 waiting）或接受 waiting 并写明 |
| SUGGESTION | 设计 §7.2 extractor 行（line 349「匹配不到时保守投影 error」） | 维度 1 / P0-12 | 漏修 | **R4-MF-2 ③ 明确要求的「『保守投影 error』限定终态语境」未执行**：§7.2 仍无条件写「sessionFile 为 null 的记录经 findSubagentSessionFile 时间戳匹配兜底，匹配不到时保守投影 error」，与 §6.5 级 6「子 JSONL 不存在 → streaming（start 早期保守）」并存且无统一规则——两个相邻失败模式（解析不出路径 vs 有路径但文件未建）给出相反的保守方向，实现者无所适从；start 早期（MF-1 场景）sessionFile null 会按 §7.2 误报 error。MF-1 的修复会重写这一带，但该残留是 R4 修复方向的显式未完成项，需独立追踪不被 MF-1 修复吞没 | §7.2 改为「匹配不到时：有终态迹象（终态 entry/sidecar 线索）→ 保守 error；无任何终态信号（start 早期）→ streaming」，或统一并入级 6 的判别规则 |
| SUGGESTION | 设计 §6.5 级 5 冷路径注 / §11 A10 行；计划 P0-A10 冷路径探针 / P4 | 维度 2 / P0-13 + P1-6 | 新发现 | **startedAt 窗口宽限规则的时间源未设计**：规则是「start entry 时间距今 < 窗口期 + 1 turn 且无终态 entry → 保守 streaming」，其中「距今」需要当前时间，而 extractor 现签名 `extractSubagentsFromSessionFile(filePath)` 是纯函数（计划 U3/P4 也以「extractor 是纯函数，测试矩阵」为测试策略）——引入 `Date.now()` 会破坏确定性测试（fixture 无法构造「窗口内/外」两种形态而不注入时钟）。「start entry 时间」的锚本身可得（pi `SessionEntryBase.timestamp: string`，session-manager.ts:46-50 已核每个 entry 携带），两文档均未写取哪个 entry、也未写 now 注入 | 宽限规则落地时 extractor 增加可选 `now` 参数（默认 `Date.now()`，测试注入固定值）；设计级 5 注/P4 补一句时间锚 = start toolCall entry 的 timestamp |
| INFO | 设计文档头一句话结论（line 3） | 维度 3 / P1-8 | 新发现 | 一句话结论的六级枚举「（bg-notify 终态 entry / sidecar / `.alive`+pid / 轮次 entry / 子进程 JSONL 收尾 / start 早期保守态）」是按磁盘事实列 6 项，与矩阵级数非一一对应（「sidecar」一项盖级 2+3，「子 JSONL 收尾」与「start 早期保守」同属级 6）——概述性表述，读者对照矩阵可自行对上，不影响决策 | 可不改；若改，标注「按磁盘事实枚举，非级数一一对应」 |
| INFO | 设计 §8 场景 9 / 计划 P6 item 3；`interface/subagent-tool.ts:116-128` | 维度 2 / 已核实为真 | — | **场景 9 的 `conversation:true` 参数核实通过**：tool schema 真实定义 `conversation: Type.Optional(Type.Boolean({...}))`（start action 顶层字段），description 自带示例 `{"action":"start","task":"review iteratively","slug":"review","conversation":true}`；配套 `idleTimeoutMs`（长间隔协作建议调大）与 message/close 动作链完整——场景 9 的操作步骤可按字面执行，无需 fallback 措辞 | 无需动作（记录防后续轮次重复怀疑） |
| INFO | 设计 §6.5 sessionFile 链；`subagent-service.ts:986` / `session-runner.ts:1072/1099/1289` | 维度 2 / 已核实为真 | — | **「start 后子进程换 session / details 与 notify sessionFile 不一致」攻击面不成立**：`record.sessionFile` 全生命周期只有握手赋值（session-runner 异步三点）与跨重启恢复重读同值（subagent-service.ts:986 `record.sessionFile = found.sessionFile`，从磁盘 record 索引读回同一文件）两类写点，无任何换文件/轮转路径——优先级链「details（start 时点）> notify（后来）」的取值稳定性前提成立（MF-1 破坏的是「start 时点有值」，不是「值会漂移」） | 无需动作（记录防后续轮次重复怀疑） |

## R4 修复核对表（维度 1，验收项 1）

| R4 编号 | 内容摘要 | 判定 | 说明 |
|---------|---------|------|------|
| MF-1 | 映射表 parent-* 三行死行（disposeAllRecords 不 notify 且补 notify 落错 session 文件；级联关闭落级 6；stopped 真实数据源 = user-close） | **部分成立** | 死行标注（~~strikethrough~~ + 不可达依据链）、变更历史 v6①、P4 矩阵「parent-* 标注不可达 + 级联关闭 fixture → 级 6」均落地 ✓；但「级联关闭实际落级 6」对带轮次 entry 的 chatMode 记录失实（落级 5 → waiting），且「仅 normalize 兼容层保留」层级混淆（normalize 收不到 closedReason）——见本轮 SG-4 |
| MF-2 | sessionFile 地基（details 第一优先 / BgNotifyRecord 补字段 / findSubagentSessionFile 限 startedAt / start 早期投影矛盾修正） | **修复无效（核心半边）** | ②③④形式上落地（链补入、`message.ts` 确无 sessionFile 字段、级 6 streaming 分支、gating）；但①链第 1 级「start 时即有、数据现成」被代码链 + 真实 JSONL 双重证伪（execute 在握手前返回，details.sessionFile 恒 null）——地基在窗口期内依旧缺失，start 早期误报 error 的矛盾未消除反而被「已修」表象掩盖；§7.2「匹配不到 → error」限定语境亦未执行（SG-5）——见本轮 MF-1 |
| SG-1 | v5 漏同步 7 处（一句话结论/§5.1/§9/§10 级数、running→streaming、sidecar 投影措辞、计划头部版本、P4 标题） | **已修复 ✓（带新尾巴）** | 逐处核实全部改为六级/streaming/v6 ✓；但 v6 增场景 9 自身引入两处新计数漂移（设计 §8 结论「6 个」/计划 §8 标题「8 场景」）——本轮 SG-1 |
| SG-2 | workflow 枚举边界显式化（Out-of-scope 三项 + 目标 4 限定机制层） | **已修复 ✓** | Out-of-scope 补 WorkflowRunStatus 三态不动 / statusDotClass 不迁移 / paused 退役 + 用户终止双色不一致声明；目标 4 加「枚举层统一不在本次范围」 |
| SG-3 | 协议 kind 联合增 `'changed'` + 过滤目标值 | **已修复 ✓** | §7.1 注释含两分支（联合增 changed / 翻译处过滤目标 = notify）；计划 P3 有 `{kind:'changed'}` 行 |
| SG-4 | 级 5 冷路径「done → waiting」抖动（A10 探针实测项 + startedAt 宽限预案） | **已修复 ✓（带尾巴）** | 设计级 5 注 + §11 A10 行 + 计划 P0 冷路径探针 + P4 条件项四处在 ✓；宽限规则的时间源（now 注入 / entry timestamp 锚）未设计——本轮 SG-6 |
| SG-5 | normalizeSubagentStatus 接口裁决（closed 分流 / 单一返回类型 / default→error / active/pending 标注） | **部分成立** | 分流裁决、不 widen、default→error、active/pending 防御值标注均落地 ✓；输入域缺 `'running'` 与 falsy 归宿（default→error 会误判在跑态）——本轮 SG-3 |
| SG-6 | 决策 5 动机的验收回路（F6 + 场景 9 + P6） | **已修复 ✓（带尾巴）** | F6 行（§3.2）+ 场景 9（回溯「目标 1 + F6」）+ P6 item 3 / DoD 9/9 ✓；场景计数两处未同步（并入本轮 SG-1） |
| INFO-1 | user-close 依据列补 closeAfterRound 兑现路径注 | **已修复 ✓** | 映射表 user-close 行含「含 closeAfterRound 兑现路径……结果经 result 字段仍可见」 |
| INFO-2 | 级 3 窗口期「done → stopped」瞬态注 | **已修复 ✓** | 级 3 说明含完整瞬态描述与「≤60s+turn，接受」 |
| INFO-3 | paused 死值 / kill -9 历史 state 非 gap | **已修复 ✓** | paused 退役入 Out-of-scope；「非 gap」结论维持（本轮未重复怀疑） |
| INFO-4 | useBackgroundWork 头注释/docstring 连带更新 | **已修复 ✓** | 计划 P4 验证（renderer）行显式列「连带更新其头注释与 docstring」 |

汇总：**12 条中 8 条完全成立；MF-1 部分成立（SG-4）；SG-5 部分成立（SG-3）；SG-1/SG-6 成立但各带一处新尾巴；MF-2 核心修复无效（本轮 MF-1）**。

## 各维度结论

### 维度 1：R4 全等级修复质量核对

见上方核对表。总评：v6 对 R4 的响应率高（12/12 有对应动作），形式完整度好（映射表死行、Out-of-scope、kind 联合、F6/场景 9、INFO 全吸收）；但**唯一一条「修复无效」恰好落在 R4 两个 must-fix 中更根本的那个（sessionFile 地基）**——修复引入的关键新事实「toolResult details.sessionFile start 时即有」未经源码/实测验证即写入文档（R4 报告自己的核实也有漏洞：把「消费代码存在」当成「值非空」），本轮用真实 JSONL 实测推翻。这正是审查循环中「修复语句与文档其他章节引入新矛盾」的典型样本：链第 1 级为空使得级 4/6 在窗口期不可达，§5.1 T+0s/T+40s 两处时间线与 §7.2 error 兜底形成三边矛盾。

### 维度 2：对 v6 新增机制的对抗

- **sessionFile 优先级链四级**：取值稳定性成立（无换文件路径，INFO-3）；但第 1 级数据源为空（MF-1）——链的形式正确、地基空心。details 与 notify 不一致风险不成立（同源同值）。
- **级 6 与孤儿判定的时间边界**：「文件必然存在」被 30 天 TTL GC 证伪 + streaming 保守无上界（MF-2）；GC 与 findSubagentSessionFile 扫同一棵树（`<piAgentDir>/subagents`，已核 getSubagentSessionDir 与 GC SUBAGENTS_DIR 一致），交互真实存在。
- **级 5 冷路径宽限**：规则条件化挂 A10 探针 ✓，但时间源（now 注入 / entry timestamp 锚）未设计（SG-6）。
- **normalize 接口裁决**：分流点（组合层按 closed 分流）语义清楚，但输入域清单缺 running/falsy（SG-3）；unknown 新值 → default → error 的方向有既有注释理由支撑，可辩护。
- **场景 9 可执行性**：conversation 参数真实存在，操作链完整（INFO-2）。

### 维度 3：全文档最终一致性扫描（收口）

- **编号体系全部一致**：决策 1-7（含 6.1/6.2/6.3）两文档引用无漂移；探针 A1-A10 全部在 §4.1 定义且 §11/A10 行交叉引用闭合；级数统一六级；SubagentStatus 6 态值（streaming/waiting/done/cancelled/stopped/error）在 §6.5/§7.1/§8/计划 P4 间一致；R4-SG-1 的 7 处残留全部清理 ✓。
- **残留漂移仅两类**：场景计数两处（SG-1：设计 §8 结论「6 个」实际 9 行、计划 §8 标题「8 场景」vs DoD「9/9」）；轻量事件发起点两处（SG-2：§7.4 与 P5 仍只写「终态转换点」）。
- 两文档间相同事实表述抽查（六级矩阵条目、sessionFile 链、normalize 裁决、kind 联合、A10 探针步骤、对账点三处、空结果守卫）一致，无新漂移。

### 维度 4：新发现

MF-1（details.sessionFile 恒 null，含真实 JSONL 证据）与 MF-2（GC 证伪「必然存在」）均为前四轮未触达的新攻击面——前四轮的核实方法（read 源码断言）在本轮升级为「read + 运行时产物实测」后才暴露，说明时序类断言（「start 时即有」）只能靠实测消解。另附一个流程观察：R4 报告声称「已 read 核实」的条目中，event-interpreter:554 消费 details 一项的推论不成立——后续轮次对「消费点存在 ≠ 数据有值」保持警惕。

## 已核实为真的关键引用（本轮新增核实）

start toolResult 双轨 sessionFile 均为 null（真实 JSONL `~/.pi/agent/sessions/--Users-zhushanwen-Code-xyz-agent-workspace-feat-optimize-todo-goal--/2026-08-10T05-01-45-911Z_*.jsonl` 两条 entry 实测）；execute 的 background 返回链（subagent-service.ts:694-698 fire-and-forget + `sessionFile: record.sessionFile`）；createRecordForMode 不设 sessionFile（:1311-1347）；sessionFile 赋值点全在握手后（session-runner.ts:1072/1099/1289）；adapter details 副本 `d.sessionFile ?? null`（subagent-actions.ts:483-488）；event-interpreter:572 实时路径消费 details.sessionFile（拿 null）；session-file-gc.ts 全文（30d TTL / 5% 概率 / unlink jsonl + 三类 sidecar / 活进程豁免）；GC 接线 index.ts:419（每次 session_start）+ startGcTimer（subagent-service.ts:454-475，idle record 30d archive 不通知）；getSubagentSessionDir 与 GC 同树（pi-paths.ts:103-104 vs session-file-gc.ts:24/37）；normalizeSubagentStatus 现状映射与 falsy 分支（subagent-status.ts:16-45）；extractor 现状只读 content[0].text（:163-176）、listItem status fallback 调 normalize（:227-229）、findSubagentSessionFile 无 startedAt 返回最近文件（:298-309）；pi SessionEntryBase.timestamp（session-manager.ts:46-50）；conversation 参数定义与示例（interface/subagent-tool.ts:116-128/241）；toNotifyRecord chatMode 条件透传 sessionFile（subagent-service.ts:610）；record.sessionFile 跨重启重读同值（:986）；record-store 重建矩阵分支 1-4（:795-827）；.alive = `${sessionFile}.alive`（alive-store.ts:23-26）。
