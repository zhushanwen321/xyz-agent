{
  "acceptance": [
    {
      "id": "D1",
      "title": "T9 reviewerSchema.required 恒含 reconciliation + 删除两处冗余 required spread",
      "type": "unit",
      "core": false,
      "command": "pnpm install --frozen-lockfile --prefer-offline --ignore-scripts --filter @zhushanwen/pi-subagent-workflow... >/dev/null 2>&1 && cd extensions/subagent-workflow && npx vitest run src/orchestration/__tests__/review-fix-loop-e2e.test.ts",
      "scenario": "e2e mock runner 记录的 review 调用 opts.schema：R1 与 R2+/scoped 的 schema JSON 逐字节相同（required 恒含四字段 report_file/must_fix/suggestion/reconciliation）；脚本内不再存在 `{ ...reviewerSchema, required: [...] }` spread（源码文本断言）；reconciliation description 更新为统一语义（R1 返回空数组说明，无「optional for R1」stale 文案）"
    },
    {
      "id": "D2",
      "title": "T9 三模板共享静态段：动态段起点标记之前逐字节相同（快照守护）",
      "type": "unit",
      "core": false,
      "command": "pnpm install --frozen-lockfile --prefer-offline --ignore-scripts --filter @zhushanwen/pi-subagent-workflow... >/dev/null 2>&1 && cd extensions/subagent-workflow && npx vitest run src/__tests__/review-fix-loop-utils.test.ts",
      "scenario": "buildR1ReviewPrompt（新函数）/buildR2ReviewPrompt/buildScopedRecheckPrompt 三者产物在 `--- ROUND CONTEXT ---` 标记之前逐字节相同（同族参数：同 targetType 静态说明）；静态段含共享审查协议（报告格式 + 修复建议必填列 + 分级说明）；变化内容（header/round/roundDir/报告路径/对账段/known-remaining/dormant/scope 清单/fix 结果）全部在标记之后——断言标记之前不含 roundDir/header 等动态值；快照测试锁定静态段全文（模板回归守护）"
    },
    {
      "id": "D3",
      "title": "T9 R1 空数组说明 + reviewer 报告「修复建议」必填列",
      "type": "unit",
      "core": false,
      "command": "pnpm install --frozen-lockfile --prefer-offline --ignore-scripts --filter @zhushanwen/pi-subagent-workflow... >/dev/null 2>&1 && cd extensions/subagent-workflow && npx vitest run src/__tests__/review-fix-loop-utils.test.ts",
      "scenario": "buildR1ReviewPrompt 的动态段含「R1 无前轮对账，reconciliation 返回空数组 []」说明（required 统一后 R1 必须有输出路径——空数组即合规输出）；三模板的报告指令均含「修复建议/Fix suggestion」必填列说明（guidance 数据链的 reviewer 侧源头，每条 must-fix 附一行修复方向）"
    },
    {
      "id": "D4",
      "title": "T9 e2e 回归：R1 reconciliation 空数组输出下游无异常 + 跨轮 schema 稳定",
      "type": "e2e-mock",
      "core": true,
      "runner": "vitest",
      "command": "pnpm install --frozen-lockfile --prefer-offline --ignore-scripts --filter @zhushanwen/pi-subagent-workflow... >/dev/null 2>&1 && cd extensions/subagent-workflow && npx vitest run src/orchestration/__tests__/review-fix-loop-e2e.test.ts",
      "scenario": "现有 e2e 剧本回归（R1 剧本 reconciliation: [] 或缺省——normalizeReviewResult 缺省 []、reconcile 门控 reconCount===0 行为不变、E2E-1/4/5/6/7 与 B6/B6b/C5 全部通过）；新增断言：同一 run 内 R1 与 R2 review 调用收到的 schema JSON 序列化后逐字节相同（缓存前缀稳定化的 system 段前提）；R1 剧本显式返回 reconciliation: [] 且 run 到达 clean",
      "mockFidelityNote": "唯一 mock 是 deps.runner（AgentRunner = LLM 子进程替身）；引擎与文件系统全真。schema 逐字节对比用真实 workflow 脚本声明的 schema（mock runner 记录的 opts.schema，非测试侧构造）"
    }
  ],
  "contracts": [
    {
      "id": "D-schema",
      "kind": "schema",
      "provider": "rfl-mp",
      "consumer": "rfl-mp",
      "signature": "reviewerSchema = { type: object, properties: {report_file, report_content, must_fix, suggestion, reconciliation}, required: [report_file, must_fix, suggestion, reconciliation] }——R1/R2+/scoped 共用同一对象（无 per-round spread）；schema JSON 逐字嵌入 appendSystemPrompt 后跨轮字节稳定",
      "file": "extensions/subagent-workflow/workflows/review-fix-loop.js",
      "description": "消息级缓存前缀稳定化的 system 段前提；后续任何 schema 变更必须保持跨轮统一（新增可选字段不改 required 是安全方向）"
    }
  ],
  "split": []
}
