source: User-provided 水墨双鱼 reference image (saved at /tmp/ref-fish.png during exploration)
origin: Original hand-painted ink-and-wash dual-fish composition
author: User-supplied reference (署名 "Calli 2018" omitted from logo per user instruction)
generation: Auto-traced via /tmp/dual-fish-trace.py — see generation notes below

generation notes:
  - Python 3 + PIL + scipy only (no skimage/cv2/potrace available)
  - Threshold: 200 (catches both dark ink + lighter ink edges around the white fish)
  - Boundary extraction: morphological gradient (binary AND NOT binary_erode)
  - Connected-component labelling: scipy.ndimage.label
  - Outer contour tracing: Moore-Neighbor boundary tracing (per-region)
  - Simplification: Douglas-Peucker, epsilon=2.0px
  - Kept regions with size > 300 px (final: 9 paths, ~3.8KB)
  - ViewBox: 0 0 658 940 (matches reference image proportions)

design intent:
  - Faithful outline trace, NOT an artistic redraw
  - Inked regions fill with var(--ink) (defaults to #0f1013)
  - White-fish shape emerges as negative space inside surrounding ink
  - Eyes (black fish's dark disc + white fish's small dot) are part of the trace
  - Calligraphy signature from the reference is excluded by the threshold (it's lighter than the ink strokes)
  - Fill uses CSS var for theme switching (light/dark backgrounds)
