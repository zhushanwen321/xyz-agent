# 设计文档对抗式审查报告：04-electron-main.md + 05-extensions.md

> 审查对象：`docs/architecture/refactor-2026-08/04-electron-main.md`（E1-E7，424 行）+ `05-extensions.md`（F1-F7 + 附带项，398 行）
> 审查依据：`rubric-design-doc.md`（P0 致命 / P1 建议）+ 项目 `AGENTS.md`
> 审查方式：对抗式（默认怀疑）+ 源码交叉核实（所有"事实错误"断言均经 `read`/`grep` 验证）
> 结论：**5 must-fix，3 suggestions**。两份文档整体质量高（事实核实通过率 >90%，方案对比与验收投入普遍达标），但 5 处 P0 问题集中在"方案声称解决的目标在边界场景下未真正达成"（E2 时序、F7 收敛边界、F2 接口不完整、F4 验收不可测、F3 决策顺序）。这些不是表述瑕疵，是实施后会复现 bug 或验收放空的设计缺陷。

---

## Summary

5 must-fix, 3 suggestions.

- **04-electron-main.md**：1 must-fix（E2 时序漏洞）+ 2 suggestion（行号偏移、E1 打包表述）
- **05-extensions.md**：4 must-fix（F7 收敛边界、F4 验收不可测、F2 接口不完整、F3 决策顺序）+ 1 suggestion（F1 escapeXml 语义）

事实核实通过项（不列入 findings）：E1 四类逻辑与行数（update-handlers 483 行 / orchestrator 333 行 / 错误翻译 ×3 / testProxyConnection :56-111 / orchestrator 纯逻辑约束）、E3 getAllWindows 五处、E5 WindowState renderer 零消费、E6 KILL_WAIT_MS 双定义 + checkHealthEndpoint 双实现语义分离 + shell-env 孤儿、F1 safeStringify ×3（含 extension-logger 未导出实现）/ escapeXml ×4 / MS_PER_SEC 6 份 + MS_PER_SECOND 3 份 = 9 份、F2 三处 JSONL 解析、F4 structured-output WeakMap+strict:false / args-validator orchestration 路径 + coerceTypes:true、F5 subagent-workflow 真实裸 console=0（13 处全注释/字符串）。文档的"核查修正口径"绝大多数属实。

---

## Findings

| 优先级 | 位置 | 维度 | 描述 | 修复方向 |
|--------|------|------|------|----------|
| MUST_FIX | 04-§3.2 E2 | P0-10 对抗 + P0-16 运行时断言 | **方案 A 删除 before-quit 的 `await flushStderrSink()` 后，自然退出路径（E2 想修的目标场景）反而失去落盘 await 保证。** 核实 `process-control.ts:95` flushStderrSink 幂等机制：首次调用执行 `const sink = stderrSink; stderrSink = null; sink.end(); 等 finish`；**后续调用因 stderrSink===null 直接 `return Promise.resolve()`（不等真正 finish）**。方案 A 在 spawn exit handler（:269 `child.on('exit')`→`onExit?.(code)`）追加 `void flushStderrSink()`（fire-and-forget）并删 main.ts:286 的 `.then(() => flushStderrSink())`。自然退出路径时序：runtime 崩溃 → spawn exit handler fire-and-forget flush 开始 drain 并置 stderrSink=null → 用户关窗 → before-quit 调 `ctx.runtime.stop()`（child.killed=true，stopRuntimeProcess 直接 resolve，**不走 done 不 await flush**）→ `.finally(app.quit)` —— **app.quit 不等 drain 完成，WriteStream 被 OS 强制关闭，丢尾部 stderr**。文档风险段"时序验证点：quit 必须在 finish 之后——方案 A 下 stop() 内部已 await flush（done 路径），语义不变"**只对 stop 路径成立**；E2 的问题描述恰恰是"自然退出路径丢尾部 stderr"，方案 A 在该路径下比现状（main.ts:286 至少有兜底 await）更没保障。验收 1（kill -9 后查尾部完整）在 stderr 量小 / 关窗慢时通过，**测不出竞态**——属运行时断言未探针。 | 二选一：① 保留 before-quit 的 `await flushStderrSink()`（不删），spawn exit handler 追加的 flush 仅作"尽早开始 drain"，before-quit 的 await 兜底等 finish（幂等语义下若已 flush 则立即 resolve，否则真等）；② 让 spawn exit handler 的 flush 经 supervisor 接口暴露一个可被 before-quit await 的 Promise（而非 fire-and-forget）。方案 B（接口补 flushStderr + 组合根保留 await 编排）对自然退出路径反而更安全，不应被简单判为"根因不除"。补一个**能触发竞态的验收**：构造 stderr 量大（如 runtime 持续 warn）+ kill -9 后立即关窗的场景，断言日志尾部不截断。 |
| MUST_FIX | 05-§3.F7 | P0-10 对抗 + P1-6 加机制不减法 | **"消除两套实现"收益被高估：下沉 mapAssistantMessageDelta 只收敛 text/thinking 两分支，event-adapter 的 toolcall/thinking_start/end/error 分支仍需独立 switch。** 核实 `event-adapter.ts:96-134` handleMessageUpdate 是正向 switch，处理 **8+ 种 sub.type**：text_delta→message 帧、thinking_start/thinking_delta/thinking_end→各自独立 message 帧、toolcall_start→tool-call-index 中间事件（附 toolCallId+contentIndex）、toolcall_delta/toolcall_end/text_start/text_end→noop、error→stream_error。而 `session-runner.ts:87` mapAssistantMessageDelta 只处理 **2 种**（text_delta/thinking_delta→AgentEvent）。方案 A 下沉 mapAssistantMessageDelta（输出 `text_delta\|thinking_delta\|noop`），event-adapter 复用它——但 event-adapter 的 toolcall_start（→tool-call-index）、thinking_start/thinking_end（→独立帧）、error（→stream_error）分支**无法被这个三态分类函数覆盖**，仍需 event-adapter 自己 switch。净效果：event-adapter 多了一层 shared import，switch 没减多少（只 text_delta/thinking_delta 两 case 改调下沉函数）；"pi 新增 delta type 单点维护"不成立——新增 toolcall 类仍要改 event-adapter。 | 重新界定下沉范围，二选一：① 下沉一个**完整的 sub.type→事件 kind 分类函数**（覆盖所有已知 sub.type 的分类，产出态类型由调用方包装），event-adapter 的 switch 真正消失，leverage 兑现；② 承认 F7 是"部分收敛"（只统一 text/thinking 分流），下调收益表述（删"消除两套实现""单点维护"），避免实施后验收对不上承诺。当前方案 A 是加间接（import shared）却不减 switch，违反"先尝试砍/简化再谈加机制"。 |
| MUST_FIX | 05-§3.F4 | P0-13/14 验收不可测试 | **验收 1"重放 2026-08-01 事故场景"不可靠触发，"审计确认各自正确"判据未量化。** 验收 1 写"让 LLM 提交一个 schema 重写请求，确认校验用权威 schema 而非 LLM 自报 schema"——但 LLM 是否重写 schema 是**概率行为**，pi CLI 实测（mimo-v2.5-pro）无法可靠触发，同一 prompt 跑 10 次可能 0 次重写。这不 testable（DoR 门槛：有明确通过标准、可重复执行）。同时"审计是硬前置"+"确认两处分叉不是各自正确"无可执行判据——什么算"确认正确"？缺少可证伪标准（如：构造分别只被 strict:false / coerceTypes:true 影响的测试用例，证明移除任一选项会导致对应测试 fail）。F4 涉及 infrastructure builtin（structured-output），2026-08-01 事故的根因是"校验 LLM 自报 schema 而非权威 schema"，收敛若复发改动承载不起。 | ① 验收 1 改为**确定性构造**：直接喂一个"自报 schema ≠ 权威 schema"的 tool call 输入给 executeStructuredOutput（不经 LLM），断言权威 schema 拒绝、4 条 channel 修复不丢失——这是 testable 的；保留 pi CLI 实测只作冒烟（不做断言主载体）。② 审计判据量化：产出"移除 strict:false 的回归测试""移除 coerceTypes:true 的回归测试"两条，各自 fail 即证伪"可移除"，以此作为"确认各自正确"的可执行定义。 |
| MUST_FIX | 05-§3.F2 | P0-12 副作用 + P0-13 验收缺口 | **adapter 接口只给全量读签名，find.ts 流式"命中即停"语义无设计，性能回归无验收。** 核实 `session-reader/src/discovery/find.ts:104` readFirstUserMessageText 用 `createInterface({ input: createReadStream })` **流式逐行读 + 命中即停 + stream.destroy()`**，注释明示"不读全文，命中即停 stream"——这是对大 session 文件（MB 级，pi 长会话常见）的性能优化。方案 A 的 adapter 接口 `readSessionFile(path)` 返回 `{ header, entries, sessionId }` 是**全量读**语义。文档改动点 2 识别了风险（"若 adapter 是全量读，需确认 find.ts 的流式提前停止不是性能关键路径；若 key，adapter 提供 stream 变体"）但**未设计 stream 变体签名**，也未在验收里覆盖大文件性能回归。三处消费方语义不同：find.ts 流式命中即停 / session-reconstructor 全量+坏行防御 / session-pending 全量 includes 扫描——单一全量读 adapter 强行覆盖三种语义会牺牲 find.ts 的性能不变量。 | adapter 设计双签名：① 全量 `readSessionFile(path)→{header,entries,sessionId}`（服务 session-reconstructor / session-pending）；② 流式 `findFirstMatchingLine(path, predicate)→Promise<string\|undefined>`（服务 find.ts，保留命中即停）。验收补一条：用 MB 级 session 文件（如 5MB 长会话）对比改动前后 find.ts 的 readFirstUserMessageText 延迟，断言无数量级回退（流式 vs 全量读在 5MB 文件上差 10-50x）。 |
| MUST_FIX | 05-§3.F3 + §5 | P0-12 决策顺序副作用 | **DP-3 决策（W4）晚于 F1 实施（W3），若 DP-3 裁决"撤销 shared 定位"，F1 刚建的 shared utils 需搬家——决策与实施反序。** §5 实施顺序：F1 在 W3"shared 建通用 utils 模块"（前提：shared 保留）；DP-3 在 W4 才裁决 shared 去留。F3 方案 A 撤销分支自己也说"连带 F1 的 utils 归位决策（若 F1 已建 shared utils，此分支需重新评估其归位）"——识别了矛盾但**没给解法**。这是"先实施假设 shared 保留，再裁决 shared 去留"的逻辑反序：若 DP-3 判撤，F1 的 utils 要从 shared 迁回某顶层包（model-switch 或新建），F1 的依赖声明、消费点 import 全部返工。F5（DP-4）也依赖 DP-3，文档明确"两决策不可反序"——但 F1 与 DP-3 的反序没被同等约束。 | 二选一：① 把 DP-3 前移到 W3 之前（F3 的"删除测试"可独立先行——砍 0 消费者导出不依赖 F1，先把 DP-3 裁决了再决定 F1 的 utils 建在哪）；② F1 明确标注"shared utils 建立以 DP-3=保留分支为前置条件"，若 DP-3 判撤则 F1 的 utils 直接建在 model-switch 包内（不进 shared），把决策分支的连带影响写进 F1 方案而非留到 F3 兜底。 |
| SUGGESTION | 04 全文 | P0-11 事实 | **行号轻微偏移（方向全对，偏移 4-6 行）。** 核实：main.ts 文档说 286 行实际 291 行；E2 flush 调用文档 :281 实际 :286、getAllWindows 文档 :265 实际 :270；E4 ③ 自愈编排水档 :244/:249 实际约 :250/:257（maybeRollback/cleanupCompletedUpdate 在 whenReady 内）。05 侧 F7 event-adapter 文档 :92-130 实际 handleMessageUpdate 从 :96 开始。这些偏移不误导（grep 能定位），但 P0-11 要求行号精确。 | 全文行号二次校准（重新 read 后更新文档引用行号）。非阻塞，但既然文档声明"所有路径/行号按 2026-08-13 审查后代码二次核实"，应兑现该承诺。 |
| SUGGESTION | 04-§4 + §5 收尾 | P1-2 + 一致性 | **E1"打包敏感区"表述前后矛盾：§4 层内验收说"W3 跑 validate-runtime-bundle.sh"，§5 收尾说"E1 不进 runtime bundle"。** AGENTS.md §12 的"逐个 commit 逐个验证"是**针对 runtime bundle 打包链路**（tsup/electron-builder/plugin-host）；E1 改的是 update-handlers/orchestrator（main 进程内，不进 runtime bundle）。文档 §5 收尾自己澄清了"E1 不进 runtime bundle，但属于打包敏感功能域"——但 §4 验收仍写"跑 validate-runtime-bundle.sh"，会把验证导向错误工具（validate-runtime-bundle.sh 验 runtime bundle，查不出 update 域回归）。 | E1 的"逐 commit 验证"明确为：`apps/electron` vitest 全绿（update-handlers-orchestration.test.ts 等）+ 真实升级冒烟（XYZ_DEV_MOCK_UPDATE=1）。把 §4 的"validate-runtime-bundle.sh"从 E1/W3 段挪走或改为"E1 不触发 runtime bundle 验证，功能域验证用 electron 测试 + 冒烟"。update 域的"打包敏感"是功能语义（升级行为正确），不是 AGENTS.md §12 的 bundle 打包语义，避免概念混用。 |
| SUGGESTION | 05-§3.F1 | P0-12 语义边界 | **escapeXml"统一为单一 encode 方向"与 goal 的 escapeXmlText（刻意 3 连子集）有语义张力。** 核实 goal `prompts.ts:22` escapeXmlText 只转义 `& < >`（3 连，XML 文本节点语义，不转义引号），测试断言 `escapeXmlText("a&b<c>")→"a&amp;b&lt;c&gt;"`；subagent-workflow 的 escapeXml 是 5 连（`& < > " '`，XML 属性值语义）。两者用途不同：goal 包裹 `<successCriteria>文本</successCriteria>`（文本节点无需转义引号），subagent-workflow 包裹 `<name>${escapeXml(name)}` 等可能含引号的属性上下文。文档方案 A"统一为单一 encode 方向（escapeXml + decodeXml 成对导出）"若强统一为 5 连，goal 的输出会多转义引号（虽 goal 文本通常无引号，行为差异不明显，但语义被悄悄改变）。 | 下沉时区分两个 API：`escapeXmlText`（3 连，文本节点）+ `escapeXmlAttr`（5 连，属性值），各自语义独立、各自单源；或文档显式说明"goal 的 3 连是上下文决定的刻意子集，统一为 5 连不改变 goal 输出（文本无引号）"并补一条 goal 含引号输入的回归测试证伪。避免"统一"名义下悄悄改变语义。 |

---

## 通过项摘要（不展开）

- **P0-1 五段骨架**：两份文档 §1-§5 齐全（背景目标 / 现状问题 / 解决方案 / 验收 / 下一层拆分）。
- **P0-2 delta 链**：无"参见 vN / Rxx-finding"类 delta 引用；引用审查报告来源（`arch-review-text.md §4/§5`）是事实溯源，非版本链。
- **P0-3 结论先行**：各候选方案对比表后均有"推荐：方案 X + 理由"结论句。
- **P0-7/8/9 方案对比**：每个候选 ≥2 方案 + 长期/短期性质标注 + 明确推荐。
- **P0-13/14/15 验收投入**：除 F4（见上）外，各候选验收普遍含真实场景 + pi CLI 实测步骤（`pi --mode rpc --session-dir ... --model xiaomi-token-plan-cn/mimo-v2.5-pro`），符合 AGENTS.md「pi CLI 实测优先」MANDATORY 规则，未退化为"单测通过"。
- **E1 关键边界**：orchestrator 纯逻辑约束（头注释自述 + grep 核实零 electron import）识别到位，"app.quit/win.webContents.send 必须留 handler"的边界划分正确。
- **E6 核查修正**：checkHealthEndpoint 双实现"有意分离成立"（liveness-probe.ts:19-20 注释论证 boolean vs {ok,ms?} 语义不同）的修正是对的，方案不合并函数只收敛基元的处理诚实。
- **F1/F2/F7 的"无守护→回潮"根因框架**：统一把"声明式约定无可执行机制"识别为根因模式，对抗式视角到位。
