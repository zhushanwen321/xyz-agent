# GUI 数据源登记表（SSOT）

> 父文档：[data-source-governance.md](data-source-governance.md)（§2.2 清单 / §3.3 D1b 空值语义 / D3b 写边界裁定 / D6 队列分工）。计划：[data-source-governance-plan.md](data-source-governance-plan.md)（wave 编号 SSOT）。

## 表头声明

1. **地位**：本表是全部 GUI 数据登记条目的 owner / 权威源 / 唯一写入口 / 字段空值语义 / 已知例外的唯一登记 SSOT（主表编号顺延，不写死数量——W24 基线 #1-#12，#13 trace 台账 2026-08-21 PR #186 登记），是 S1（review-data-governance checklist）、R2（store 写入口许可表）、R3（缓存注解条目校验）的依据，review 时的对照 SSOT（父文档 §3.6 第 4 层）。**§6 起另登记跨进程共享文件的锁协议**（GUI 数据多源之外的「无锁双写」元模式治理，父文档 = integrity-hardening.md），同一查询入口。
2. **演进**：本表 P1 起演进为可执行配置——`ReplicatedState` 配置三元组（快照 RPC / 失效触发源 / 合并策略含字段空值语义）即登记条目，**W6-W8 执行时同步维护本表**（实例落地一条、登记表同步一行），P1 后 markdown 由配置生成或双向校验。
3. **现状基线**：W11 之后——xyz 对 pi JSONL 的直写链路全部消灭（活跃 rename + 兜底直写 W1 删；非活跃 rename W11 切 `withEphemeralPi`；handoff W11 迁 sidecar；patchCwd W11 迁 restore tmp），R1 allowlist 空集、检查无条件化。
4. **行号口径**：行号为 W1 后实测（按符号名定位）；与 plan 基线行号的偏差——非活跃 rename 直写 `:302→:331`（W1 改动使 renameSession 内行号后移），`persistHandedOff` 实现体 `:464→:455`（append `openSync('a')` 在 `:467`），`patchSessionCwd` `:518→:521`（`atomicWrite` 在 `:543`）。
5. **快照 RPC 频率量化（P0.5② 终判，W7/W8 实测）**：标量六实例全量接线后，典型操作序列（3 轮对话 + 1 次切模型）的快照 RPC 总量 5-7 次（W7 标量 5 次：播种 3 + 失效 2；W8 补三实例后合计 7 次：get_session_stats 3 + get_state 2 + get_commands 2），p95 0.6-1.7ms（毫秒级，远低于 UI 可感知阈值）；事件风暴被 300ms 防抖聚合（20 次失效 → 1 次拉取，用例实测），无队列堆积。**结论：已量化，无感知，无需降级**——父文档 P0.5 失败预案（防抖拉长 / 批量快照 / 仅活跃 session 拉取）均不启用。

## 1. GUI 数据登记主表（编号顺延，不写死数量）

owner 列记法：`现状 → 目标（W 编号）`；「无专门 wave」= 父文档 §5 的 19 个实施单元未覆盖该条，仅受 §3.0 架构原则约束（见 §5 维护规约第 4 条）。**读法消歧（W24 审查补）**：「现状」段是各 wave 开工前的基线快照（保留作演进追溯），与当前代码实况可能已不符——查当前写入口以「目标」段与 R2 许可表为准，勿按「现状」段理解现行代码。

| 编号 | GUI 数据 | 权威源 | owner（现状 → 目标（W 编号）） | 唯一写入口（现状 → 目标） | 字段空值语义 | 已知例外 |
|---|---|---|---|---|---|---|
| #1 | session 标签（label） | pi（session_info entry + pi 内存 sessionName） | 多写方：pi（活跃链路，W1 已收口）+ runtime 内存 `session.label` / `sessionMetaCache`（2 生产写点：`index.ts:298` session_info_changed 回写、`session-lifecycle.ts:326` rename 回写）+ renderer store 三路写（updateLabel / setGroups / updateSessionState）+ renderer `applySnapshot`（W13）；`sessionMetaCache` 删除（W9）→ **修订（2026-08-20，PR #185 MF1）**：runtime 侧 owner = 事件驱动 `setLabelCache` 单写方（session_info_changed 事件路径，`renameSession` 活跃分支 RPC 成功后直写为同源补充写点）。原「runtime `ReplicatedState<label>` 实例（W7）」声明**撤销**——实例 `.get()` 生产零消费、每次事件 markDirty 防抖重拉 `get_state` 属无效 RPC；「完成 W7（toSummary/session.renamed 改读实例快照）」属新功能开发，不在 review 修复范畴。实例创建/播种/markDirty 接线/配置条目已全删（`session-service.ts` / `event-interpreter.ts` / `replicated-states.config.ts`） | 活跃 session：pi `set_session_name` RPC（W1 已达成，`rpc-client.ts:519`；非活跃见例外①，W11 切短命 pi）→ **修订（2026-08-20，PR #185 MF1）**：runtime 内存 label 写方全集 = `setLabelCache`（session_info_changed 事件，唯一事件路径写方）+ `renameSession` RPC 成功后直写（两写点同源 pi 权威）；`ReplicatedState` 实例目标撤销，不再挂「W7 收编」待办 | `sessionName` 空 = 合法态（未命名），owner 侧**整字段覆盖含显式空值**；label 是 sessionName 在 xyz 侧同一数据链投影（事件 payload / RPC 后内存直写），**不单独登记**「label 空 = 未设置（可守卫）」语义（空值语义必须唯一，曾双登记致矛盾；原 label 实例的 'explicit-null' 归一条目随实例撤销一并移除） | xyz 指向 pi JSONL 的写点全集 6 处处置见 §3；`sessionMetaCache` = 影子状态（W9 纯删除）。**消费形态备注（2026-08-24，PR #190 S1）**：toast 定位行（`notify-toast.ts`）在 renderer 查询期即时派生 `basename(session.cwd)` + chat store 首条 user prompt 前 15 字，只读不落 store（label 真源仍是 runtime），LRU 驱逐后兜底降级为 sessionId |
| #2 | session 列表（status / modelId / tokenCount） | pi session 文件 + pi agent 态 | runtime 内存 Map + 磁盘扫描合并（SessionScanner），磁盘侧空值为占位符 → 扫描侧空值守卫（W15）+ modelId 并入 #5 实例（W7）+ renderer `applySnapshot`（W13） | 现状：内存 Map（pi 事件写）+ 扫描合并（reload 写）双管线 → 目标：runtime owner 单源，扫描结果仅作冷启动兜底 | `modelId:''` / `tokenCount:0` 为磁盘扫描**占位值**（「无数据」而非权威空值，`session-scanner.ts:81-82` 硬编码），语义 = 占位值**不覆盖已知真值**（W15 守卫落地） | 曾发生 '' 覆盖真值（`packages/core/src/domain/session/store.ts` updateSessionState 注释自述防御：局部更新避免磁盘 session 的 '' modelId 覆盖真值） |
| #3 | 上下文用量（usage / inputTokens） | `get_session_stats` RPC | runtime `session.inputTokens` 缓存 5 写点（turn_end / agent_end / compaction 估算 / switchModel 重算 / restore 拉取）→ `ReplicatedState<usage>`（W8）+ `applyContextUpdate` 收编、switchModel 重算入 owner（W10）；**修订（2026-08-24，context-consistency D1，[设计](../todo/context-consistency-design.md)）**：usage 从 `session.state_changed` 组合投影帧删除（`usagePercent`/`inputTokens`/`contextLimit` 三字段），usage 只经 `context.update` 一条帧贯穿（广播 / stateSnapshot 回放 / `session.getContext` reply 同型）——单帧单数据消除双帧不一致窗口；`buildStateChangedPayload` 的 `?? 0` 基线投影删除，`SessionStateChangedBaseline`/diff 同步缩为 modelId/thinkingLevel 两字段 | 现状：5 写点各写各的缓存，时序靠注释约定 → 目标：owner 内部读自己的缓存，竞态从「注释约定」变「结构不可能」（W10）；**修订（2026-08-24，context-consistency D1）**：`session.getContext` reply 在 `fetchContext()` 返回 null 时仅含 sessionId（0 fallback 删除）；`publishContextFromSnapshot` 仅在 fetch 成功且投影为空快照（pi tokens=null 合法无值）时发布「无值占位帧」（仅含 sessionId——stateSnapshot 显式登记 no-value 态，使切回回放可区分「该 session 无值」与「从未收到帧」；「从未 fetch 成功」的退避重试窗口不发占位帧，值可能马上就来） | **字段缺失 = 无值**（pi tokens=null 合法无值态：新 session 未跑 turn / compact 后无新 turn，UI 显 `—`）；**组合投影/发布层与 fetch/merge 层 obey 相同空值语义，禁止 `?? 0` 把「未知」编码为 0**（此前登记表只覆盖 fetch/merge 层，发布层 `?? 0` 曾把无值降级成 0 基线帧发前端——同一数据两条帧两种空值语义的结构缺口，D1 消灭）；usagePercent 为派生值（contextWindow + inputTokens 计算） | inputTokens 竞态已踩坑（`session-service.ts` switchModel docstring 自述：context.update 先回写、switchModel 后读取，时序由「缓存写入先于读取」保证——注释级约定正是 W10 要消灭的形态）；0 基线帧坑（usage 快照缺失时 state_changed `?? 0` 照发 + 组件对两帧用同一 handler 消费 → stats 被清零显示横线，context-consistency 层 1 根因，D1 消灭）；renderer 分区恢复腿的 in-flight 去重簿记（`useContextUsage` 模块级 Promise Map，非 GUI 用量数据本体——数据本体在 per-session 分区经 useSessionScopedState 持有）taste:allow-no-data-owner W24-EX-C 落定（2026-08-24，context-consistency D2/D3） |
| #4 | thinkingLevel | `get_state` | 事件（thinking_level_changed）+ switchModel 主动查询 + 双层缓存（session 缓存 + sessionMetaCache）+ renderer 字段 → `ReplicatedState<thinkingLevel>`（W7）；`sessionMetaCache.setThinkingLevel` 生产调用 0 处（W9 随 metaCache 删除）；**修订（2026-08-20，PR #185 S2）**：`session.thinkingLevel` 字段缓存与实例快照并存 = **登记的永久双写形态**（原代码注释「W13 收编」系误标——W13 是 renderer store applySnapshot 收敛 wave，从未含 runtime 侧收编步骤，待办撤销）：播种 refetch 三实例异步竞速 + get_state 失败退避（1s/5s/15s）窗口内实例快照未就绪（`.get()` 为 undefined），state_changed 组合投影（`buildStateChangedPayload`）fallback 读该缓存；`toSummary`（session 列表）亦消费该字段。缓存写方 = `setThinkingLevel` RPC 成功后直写 pi get_state 生效值 | 现状：`session-service.ts` setThinkingLevelCache / get_state 查询双路 → 目标：快照拉取 + 事件失效（W7）；**修订（2026-08-28，PR #193 U6/D9）**：失效触发源终态 = `thinking_level_changed` 事件 + `setThinkingLevel` RPC 成功响应（汇聚点 event-interpreter.ts + session-service 的 switchModel markDirty（模型切换联动失效，:628/:635）+ setThinkingLevel 直写 get_state 生效值（RPC 成功后直写、事件收敛，方法体不 markDirty））——30s 周期兜底轮询已删（D9 附录 C.4 定案：自有状态对账类变化 100% 经我方请求/事件路径，pi 侧 effective≠previous 必发事件 [PS-04]，剩余覆盖仅「不受支持的外部双写者」不构成保留理由；配置见 `replicated-states.config.ts` 头注，RPC 成功响应驱动是「事件只做失效」的补充合法形态，D7） | **无空值语义（永不 guard）**：pi `ThinkingLevel` 是具体字符串联合类型、不含 undefined（"off" 或可用档位）；xyz 缓存曾恒 undefined 是影子状态症状，非 pi 权威值域 | pi 同档位切换不 emit 事件（`session-service.ts` switchModel docstring），事件不可作可靠来源，get_state 才是可靠来源 |
| #5 | modelId | pi agent 态 | 缓存 + `session.state_changed` 广播 + 全局默认 + 磁盘空串 → `ReplicatedState<modelId>`（W7）+ 扫描占位守卫（W15）；**修订（2026-08-20，PR #185 S2）**：同 #4——`session.modelId` 缓存为登记的永久双写形态（fallback 读点 = `buildStateChangedPayload`；写方 = `createManagedSession` 初始值 + `switchModel` RPC 成功后直写；`toSummary` 同消费），「W13 收编」待办撤销 | 现状：switchModel RPC 后写缓存并广播 → 目标：失效源 = switchModel RPC 响应后主动拉快照（RPC 响应驱动；pi RPC 层无 model 事件，属「事件只做失效」的补充合法形态，登记进 W7 配置） | 磁盘扫描占位值 `''` 不覆盖已知真值（同 #2，`session-scanner.ts:81`） | 同 #2（'' 覆盖真值历史坑） |
| #6 | 消息队列（steer / followUp） | **按字段分权威（D6，已核实为终态）**：深度 = `queue_update` 帧内 `pendingMessageCount`（= pi 队列深度的**推送投影**，与 `get_state.pendingMessageCount` 同公式同源、数值恒等）；内容 = renderer 提交日志（它提交过所以它有） | 深度：`queue_update` 帧直达 renderer（PR #185 MF2 修订，2026-08-20）；内容：pi `queue_update` 快照 + renderer `pendingBuffer`（`packages/core/src/domain/chat/store.ts:123`，core 包，ADR-0059 薄壳消费）+ pendingBuffer 改计数 FIFO 删文本匹配（W14）→ 原「深度 `ReplicatedState<queue>`（W8）」声明**撤销**——实例 `.get()` 生产零消费、markDirty 防抖重拉 `get_state` 属无效 RPC；实例创建/播种/`session-service` send 汇聚点 markDirty 接线/配置条目已全删 | 队列内容**唯一提交方 = renderer**（WS `message.steer` / `message.follow_up`，入口 `packages/runtime/src/transport/session-message-handler.ts`）；queue_update 帧内深度 = 深度数据载体（renderer 对账 `reconcilePending` 直读帧值，core registry）——原「对账信号非数据载体」口径作废（MF2 定口径：帧 = pi 深度的推送投影） | 无字段空值语义（深度为数值计数；缺快照时以最近一次对账值为准） | 例外的例外见例外④：pi ExtensionAPI `sendUserMessage({deliverAs})` 允许扩展注入队列——xyz 自研扩展禁止使用（S1 拦截）；pi 入队存展开后文本（skill/template 展开），提交原文对不上——文本匹配必死、计数 FIFO 必要性的实证；**修订（2026-08-24，session-delivery PR #189 review）**：例外④口径扩为「renderer 提交日志 + 登记的 runtime 注入通道」——runtime `prompt(streamingBehavior='steer'/'followUp')` RPC（`rpc-client.ts` prompt → pi rpc-mode prompt case 透传，sd-u5/sd-u6 装配：session-manager send 排队投递 + completion-backflow 回流，发起方 = agent-managed send/backflow 而非用户 UI 提交）经同一 pi steer/followUp 队列入队，条目不在 renderer 提交日志——计数 FIFO 对账偏差从「第三方扩展残余风险」扩大为 xyz 自有通道的**登记常态**（深度仍由 pendingMessageCount 结构性对账；agent 注入消息经 pi 自定义消息事件回流 renderer 聊天流，不进 pendingBuffer） |
| #7 | 消息列表 / 内容 | session 文件 entries（append-only 日志） | 事件流累积 + agent_end 权威覆盖 + 文件重放三路 → 单一 `applyEntry(state, entry)` reducer 双路喂入（实时 feed 与文件重放共用一份派生代码，W20 本体 + 文件重放 / W21 实时 feed + 等价性断言）；**fix-chat-flow-order W1-W6 收尾（2026-08-20）**：live 全类型 entry 化（bash W1 / user·custom W2 / compaction W6——直插路径全部消灭，唯一 live-only 例外 = stream_warn） | 现状：live 一套累积、reload 一套重放 → 目标：renderer 消息列表 = entry 日志的纯函数（W20-W21），「live ≡ reload」从构造上成立（W6 起全类型构造性，等价性测试 `apply-entry-equivalence` E1-E4 机器化锁定） | 无字段空值语义（日志形态，append-only） | 消息 entry 无 `entry_appended` 事件（pi 持久化路径不发射，已实测），实时 feed 由 `message_end` 等事件重构 entry 喂同一 reducer（W21）；**turn 归属语义已落地（原「归 fix-chat-flow-order 分支主题」欠账清偿）**：分组 = `message-turns.ts` 边界规则集纯派生（user 锚 / 隐藏完成通知 `COMPLETE_NOTIFY_CUSTOM_TYPES` trigger 边界 / bash·liveOnly inline notice / compaction·branch boundary，无物化 turnId 字段——物化方案被否：第二真相违反 ADR-0062 治理线）；live entry 客户端 id 前缀（`u-`/`bash-`/`cm-`/`cmp-`）vs replay pi uuidv7 = id 空间异源差异类（W21 已裁决，等价性按 id/piEntryId 归一断言）。**例外登记（W6 → closure F1-F3 收敛，2026-08-20，conversation-turn-attribution-closure）**：① bash abort 分歧——**已消灭（closure D1/F1）**：sendBash 解除 token 守卫丢弃（abort 后 pi 正常 resolve 的真实 cancelled 结果照常发布并 entry 化，与 pi `recordBashResult` 落盘同位同值；哨兵帧 `command:''+cancelled:true` 与真实帧职责正交——前者只清 executingBash 后者产 entry，均幂等；sendBash 入口空命令早退守卫使哨兵形态成结构性不变式；等价性测试 E3 改判为两侧 deep-equal）；② executingBash ephemeral 态——权威源 = runtime bash RPC 执行态（bashStart/bashResult 帧本身），唯一写方 = bash-effects 成对置清（bashStart 置 / bashResult 清 / markBashError 兜底清）+ store.disposeSession cleanup 编排同点清理（closure D3：形态保留模块级分区 Map——store 为 factory 模式，渲染层模块级读 `getExecutingBash` 签名不变；架构目标「cleanup 可达」以挂接编排达成；taste:allow-no-data-owner W24-EX-B 已落定非草稿），非缓存不持久化；③ stream_warn liveOnly——xyz runtime 自产健康警告无 pi entry、重开即消失（registry 单点 `liveOnly:true` 标记，分组归 turn 内 notice 不切断）；④ compaction 窄差异——**已消灭（closure D2/F2）**：interpreter 恒发 `message.compactionSummary` 帧（summary 缺省透传，原 `if (r.summary)` 真值门删除），两侧同走 reducer fallback「上下文已压缩」（E4b 锁定）；**⑤ 收窄例外（closure 新增）**：abort 且 sendBash await **抛错**（transport 断/pi 死，无真实数据可发布）时 live 无 cancelled entry、pi 进程若独立存活 `recordBashResult` 落盘有——触发条件较原①从「任何 abort」收窄为「abort 且 transport 抛错」（E3b 显式例外锁定，差异恰为该 entry 且不影响 turn 边界）；**⑤ 切入 reconcile 合并方向（2026-08-22，agent-managed-session 后台 session 形态触发登记）**：切入时 getHistory（pi entries 快照）与 live 分区的合并规则 = **entry 历史为基线、分区尾部 streaming 实体追加其后**（live 真相优先于 entry 快照）——pi entries 不含进行中消息（entry 完成才 append），若直接替换会抹掉 streaming 实体致 text_delta 被 isLastAssistantStreaming 守卫丢弃（流永久停滞）；同时切入对已 hydrate 的 session 也静默重拉（旧一次性守卫废弃——后台 session 的 turn 可在前端不在场时完成，不重拉则最后输出永不出现）。实现 = chat store `reconcileHistory`（core domain/chat/store.ts），selectSession/retryHistory 统一接入；已知残留缺口：session 创建即后台 sendMessage 的最早事件（早于前端 ensureStreamSubscription 注册）依赖 subscribeSession snapshot reconcile 补齐，snapshot 窗口语义未覆盖 create 立即 send 的场景（单列后续 wave 核实 MessageBus snapshot 缓冲边界） |
| #8 | subagent 列表 / 状态 | xyz 扩展 record-store（pi 无 subagent 概念）；终态 = 扩展 `appendEntry` 自描述 entry（pi 文件成持久化权威、写方是 pi） | **W18 已达成**：唯一源 = entry 扫描（`scanSubagentEntries`——事件只失效 + `RecordEntriesCache` 游标增量拉取；extractor legacy 降级为冷启动旧 session 兜底） | **W18 已达成**：唯一源 = entry 扫描，实时与重开同一份代码 | 无字段空值语义（记录形态） | 状态归一出口 6+ 处（normalizeSubagentStatus 历史 bug 出处）；扩展 state 文件 W18 后降级纯性能缓存或退役；W16 探针实测（本地 pi RPC `--mode rpc`，mimo-v2.5-pro，chatMode 1 轮完整生命周期）：`subagent-record` 自描述 entry 单条 569-956 B（register 569B / 轮终 862B / 关闭终态 956B，= 阈值 100KB 的 <1%）、单生命周期 append 3 次（one-shot 生命周期 2 次：register + 轮终 reportRecordTransition；chatMode 每轮 +1）——均低于阈值（>100KB 或 >50 次分流），未触发 trace 增量/状态全量双 customType 预案，维持单 customType 全量快照形态；**W18 已落地**：唯一源 = entry 扫描（`scanSubagentEntries`，实时 get_entries 增量与冷启动磁盘全量同一份派生代码）；runtime owner = session-service `RecordEntriesCache`（cursor + 派生 Map，唯一写方 = `refreshRecordEntries` 扫描结果，发布经 messageBus stateSnapshot）；事件流（tool-call-end / bg-notify / entry_appended）全部只做失效信号，直写已删除；W12-W18 过渡态例外**已撤销**。冷启动旧 session（无自描述 entry）走 extractor legacy 兜底（数据滞后但可用）；**修订（2026-08-26，PR #192）——非 pi 引擎 record 详情读取链三级降级**：`subagent-engine-history.ts` `readEngineSubagentHistory`（设计 D6）①引擎原生共享 reader（zcode sqlite，extension/runtime 双端复用同一份 reader 模块）→ ②宿主 event journal 重放（seq 排序 + turn reducer）→ ③outcome-only（record 字段投影摘要卡），逐级 try、每级失败留 debug/warn 不抛崩溃（详情页永不白屏，设计 A8）；制品落 `<dataDir>/engines/<engineId>/<poolKey>/`，**写方 = extension host 单写**（zcode sqlite + journal 追加写）、**runtime 只读零写**，读取白名单与 extension 写侧经同一份 `paths.ts` 布局 SSOT 推导（dbPath 严格界池目录内 `isStrictlyUnder(poolDir)` / journalPath 严格界 engines root 下——record 来自 JSONL 文本不可信，防 `../` 逃逸）；pi record 不走该链（保持 getSubagentHistory JSONL 直读，A1 守护零变化） |
| #9 | workflow 记录 | xyz 扩展 RunStore；终态 = 自描述完整记录 `workflow-record` entry（W17 已落地：每次成功 flush append 完整快照，pi 文件成持久化权威、写方是 pi；统一 #8/#9 同一形态） | 扩展侧已收敛（W17）：`workflow-record` 自描述 entry 唯一重建源（loadAll 读序 entry > state 文件 > 空；旧 `workflow-state-link` 兼容读、优先级低，存量 run 不丢）+ runtime 消费管线（W18：entry_appended 失效 + get_entries 增量 + extractor 降级兜底） | W18 已达成：runtime 唯一源 = entry 扫描（`scanWorkflowEntries`）；state 文件降级纯性能缓存（写路径保留） | 无字段空值语义（记录形态） | 旧格式 run 静默丢失：snapshotVersion 不匹配 loadAll 跳过、不向后兼容（jsonl-run-store D-5 version guard）；entry 层 v1 guard 独立（v≠1 整条跳过）。W17 探针实测（本地 pi RPC mimo-v2.5-pro，chain workflow 3 agent call 完整生命周期）：`workflow-record` entry 单生命周期 4 条（首写 + 中间态×2 + 终态 done），单条 7.9-52.5KB（快照随 calls/trace 增长，最大 = 阈值 100KB 的 53%）——低于分流阈值（>100KB 或 >50 次），未触发 trace 增量/状态全量双 customType 预案；**W18 已落地**：唯一源 = entry 扫描（`scanWorkflowEntries`，自描述 workflow-record entry 优先、workflow-state-link + state 文件 legacy 兜底）；runtime owner = 同 #8 的 `RecordEntriesCache`；session.workflowUpdate 增量信号由扫描前后状态 diff 派生（runId/status/reason，last-value 语义延续）；W12-W18 过渡态例外**已撤销** |
| #10 | 文件改动（FileChanges） | git 工作区 | 实时 baseline diff（event-adapter + file-change-reconciler，覆盖 write/edit/bash）+ 历史静态解析（toolCall 参数，无法覆盖 bash）双管线，两管线语义不同（`packages/runtime/src/infra/pi/message-converter.ts` 文件头注释自述）→ **无专门 wave**（父文档 §5 未安排实施单元，仅受 §3.0 原则 3「投影一次」约束；live ≡ reload 等价性测试族 W22 覆盖其回归验证面） | 现状：双管线（实时 diff / 历史 toolCall 解析）→ 无收敛计划内 wave；新增改动须遵守：派生归 runtime 一处，禁止 renderer 另起第三管线 | 无字段空值语义（列表形态，空列表 = 无改动） | bash 改动历史路径无法还原（参数静态解析不可行）——已声明并接受的语义差异 |
| #11 | session 活跃态 | `get_state.isStreaming` | renderer 5 状态源派生（chat 消息态 / pendingSend / queue / retry / forceWorking）→ **无专门 wave**（父文档 §5 未安排实施单元）；P1 落地后受 §3.0 原则 3 约束（派生上移 runtime），不新增第 6 状态源 | 现状：renderer 多源派生 → 目标形态 = runtime 投影（无 wave 编号，原则性目标） | isStreaming 为布尔，无中间态语义 | **修订（2026-08-24，PR #189 MF3）**：runtime 侧 `session.isGenerating` / `session.lastActiveAt` 为**登记双写形态**（同 #4/#5「RPC 成功后直写」模式）——写方全集 = message-dispatcher（编排「先置位后 prompt」）+ session-delivery `deliverText`（`prompt(streamingBehavior)` 受理成功后置位，D7 保留副作用，见 session-delivery-registry.ts 注释）；失效源 = message_start 事件流 / agent_settled 多播 |
| #12 | slash 命令列表 | `get_commands` RPC | 广播 + RPC 拉取 + stateSnapshot 三源 → `ReplicatedState<commands>`（W8）+ `session.commands` state 话题数据源切换为 ReplicatedState 发布（W12） | 现状：`session-service.ts:1290` getCommands（RPC 拉取）+ broadcast 推送并行 → 目标：快照拉取 + 事件失效（W8），stateSnapshot last-value 从影子缓存快照变 owner 快照（W12） | 无字段空值语义（列表形态） | broadcast/订阅时序坑已修（AGENTS.md 既有规则：renderer 切换/创建 session 后必须主动拉 `session.getCommands`，不依赖 broadcast）——W8 落地后该人工规则结构性失效 |
| #13 | trace 台账（Trace 视图 per-session entries 视图数据：header / entries / malformed / sessionEnd / leafId / source + 现取 prompt 摘要；新增 WS 推送 `session.traceEntryAppended` + RPC 回包 `session.traceEntries` / `session.currentSystemPrompt`） | pi session 文件 entries（双通路：活跃 session 经 `get_entries` RPC；非活跃 / RPC 降级文件直读） | **登记即终态（现状 = 目标，无 wave，2026-08-21 PR #186 登记）**：runtime session-service（`traceLeafCache` 增量拉取基线 + `traceSyncChains` 增量腿串行链——纯派生缓存，可丢弃后 `getTraceEntries` 重建，删除路径汇聚 `removeSessionEntry` 清除）+ renderer `useSessionTrace`（分区 Map 消费副本，ADR-0049 范式，cleanup 经 `registerSessionCleanup` 统一编排） | renderer 分区 append-only 双路喂入（#7 reducer 同构形态）：`loadTrace` 全量回包替换 + `applyTraceAppend` → `mergeAppendedEntries` 增量 entry.id 去重追加 + `pendingAppends` loading 窗口缓冲（回包 ready 后按序 flush，正向注解 `@data-owner #13`）；runtime 增量腿 = `syncTraceEntries` 经 `get_entries(since)` 从 pi 权威拉取后广播（事件只触发拉取——`withTraceTrigger` 组合器 payload 无数据，广播携带的是拉取产物非事件逆向解析） | `source:'empty'` = session 未落盘合法态（非缺数据；有增量自动回 idle 全量重拉）；entries 列表形态（空列表 = 无 entry）；`filePath` / `leafId` null = 未知或未落盘 | ① renderer 推送直写 store =「推送已是权威拉取产物」登记例外形态第三实例（#7 实时 feed reducer / #6 queue_update 帧直达同构，随本条固化）；② source 'file' 降级路径（非活跃 session 文件直读，可滞后 pi 内存态、无增量腿）；③ runtime `traceLeafCache` / `traceSyncChains` = 派生缓存登记（非第二写方病灶，重建即一致） |
| #14 | session 来源标记（`SessionSummary.spawnSource` / `parentAgentSessionId`——agent-managed session：前端 AI badge + session-manager list 过滤数据） | `.agent.json` sidecar + 运行期 runtime 内存 patch（双形态，同 launchPresetId 模式） | **登记即现状（无 wave，agent-managed-session 分支 2026-08-22 登记）**：创建链 SessionManagerHandler 服务端注入（不信任 extension 参数，防伪造父 id）→ `session-lifecycle` 内存 patch + `persistAgentBinding` 落 sidecar（写前 existsSync 守卫规则 #6；spawnSource 单独成立即持久化，parentAgentSessionId 可选）；冷启动/重扫经 `scanSessionMeta` 第六读恢复（readAgentBinding 守卫收窄枚举，非法值降级） | `persistAgentBinding` / 公共层 `persistBindingSidecar` 单点（session-file-utils，经 sessionStore port）；内存态写方 = lifecycle create 单点 | `spawnSource` 缺省 = `'user'`（合法默认，非占位）；`parentAgentSessionId` 仅 `spawnSource='agent'` 时存在、可 undefined（异常路径防御——#15） | pi 延迟落盘窗口内 sidecar 写不进（existsSync 守卫跳过），内存态兜底（同 #4 launchPresetId 先例）；delete/fork 链清理 `.agent.json`（fork 不继承 agent binding——binding 是创建时语义） |
| #15 | delivery outbox（session 消息投递队列：已向发起方确认 `{queued:true}` 的待投递消息，跨 turn 持有，非瞬时传输态） | **内存 in-flight 态、非持久**（session-delivery 内核 per-handle queue + inflightBatch；runtime `SessionDeliveryRegistry` handles Map 为单例注册表，两处 `@data-owner #15` 注解） | **登记即现状（无 wave，session-delivery PR #189 2026-08-24 登记）**：写方 = 各装配点的 `send/sendChecked`（scheduler dispatch / session-manager send / notifier 合批窗口）；**丢弃语义显式声明**——① runtime 重启即静默丢失（scheduler 有 resume 重放兜底，session-manager sendChecked 已先行 resolve、终态 reject 无回流腿）② 错误重试耗尽 reject 仅在有 onSettled 记账腿的调用方（scheduler）可见；③ `dispose()` 丢弃 queue/inflight 不触发 onSettled（内核 dispose 契约注释，调用方自行补记账） | 内核 handle `send/sendChecked/flush` 唯一入口（同 sessionId 复用同 handle——注册表单例约束）；session 删除 → `dispose` 清空 | 无字段空值语义（队列形态，空队列 = 无待投递） | **known-limitation（一期保守）**：port `hasPendingMessages: () => false` 恒判空（同步签名拿不到 `get_state.pendingMessageCount`）——busy gate 只剩 isIdle 单条件，靠 TOCTOU 宽度 + pi 队列兜底；补齐路径 = pendingMessageCount 异步拉取或 settled 复核（design.md §5 待验证 1）；scheduler 侧另有入队防重标记 + TTL（runtime.ts `queuedInDeliveryAt`）防 park 期重复入队 |
| #16 | 用量统计仪表盘（UsagePage：日/模型/provider/project 切片，`usage.getStats` RPC 数据源） | pi session JSONL（**只读直扫**，`createReadStream` + readline 流式扫描；pi 为唯一写方，xyz 零写——C-data-12 绝对写规则合规） | **登记即现状（无 wave，feat-usage-statistics 2026-08-26 登记）**：runtime `UsageStatsService`（`packages/runtime/src/services/usage/usage-stats-service.ts`）唯一 owner；`shards` 派生缓存（per-file 分片 Map，声明处 `@data-owner #16` 注解）——**(mtimeMs, size) 双键失效**（append-only 场景 mtime 不变但 size 变仍 miss，D9）+ 目录内文件删除时分片同步清理；renderer `aggregate.ts` 聚合为纯函数消费侧派生（S1 登记后续上移项） | 无写入口（只读扫描）；缓存唯一写方 = `getStats` 扫描/复用路径 | 目录不存在或不可读 = 返回空结果（`rows:[]`, `sessionCount:0`——非错误态，UI 显空用量）；`skippedLines` = 行级失败计数（JSON 损坏 / timestamp 非法）；provider/model/project 缺失占位 `'(unknown)'` | 空 README 目录等容错语义 = 合法空态非异常；三分类计入规则（assistant 主桶 / toolResult-with-usage + compaction-with-usage 虚拟桶）对齐 pi `getUsageCostBreakdown`，断言锚点 = @earendil-works/pi-coding-agent@0.84.1 `dist/core/usage-totals.js:22-33`（升级 pi 须重核） |
| #17 | subagent 定向消息气泡（用户 → subagent `@` 定向对话的聊天流可见气泡） | pi 主 session JSONL（`subagent-directive` custom_message entry；**写方 = subagent-workflow extension 经 pi `sendMessage`**（`subagents.ts` emitSubagentDirective，`display:false` 为 pi TUI 语义——TUI 不渲染） | **登记即现状（无 wave，feat-usage-statistics 2026-08-26 登记）**：live ≡ reload 双链路——live：event-adapter `handleMessageEnd` 对 subagent-directive 附加 `subagent.directive` 广播（`event-adapter.ts:753-767`）→ core `useChat` → `chat.appendSubagentDirective` overlay 插入（同 entry 已由 message.customStart → applyEntryFrame 喂过，可见气泡是消费侧覆写产物，与 appendSystemNotice 同款先例）；reload：`session-entry-mapper.ts:79-87` display 覆写为 **true**（xyz 消费语义，与 pi display:false 分开理解）；两链路共用 `shared.parseSubagentDirective` 单点解析 | extension `pi.sendMessage`（customType=`subagent-directive`，details 契约 = `SubagentDirectiveDetails`；不 triggerTurn——「留痕 ≠ 处理」§3.3.8，主 agent 上下文消费但无新 turn）；xyz 侧无第二写方 | details 形状固定（subagentId/slug/direction）；direction 仅 `'user'`（预留双向）；解析失败（parseSubagentDirective 返回 null）= 非法 entry，广播/覆写均不产出气泡 | 一 entry 双消费形态（主 agent 上下文 + renderer 气泡）经设计文档 §3.3.3 论证（custom_message 进 context / appendEntry 不进，锚点实装 0.84.1 核实）；对照 #8/#9 explicit 登记先例 |
| #18 | subagent 引擎配置视图（Settings 引擎选择器数据：engines 动态清单 + defaultEngine 全局默认，`session.getSubagentEngineConfig` / `session.setSubagentDefaultEngine` RPC） | engines 清单 = `<piAgentDir>/subagents/engines.json`（extension 写入的代码注册表镜像）；defaultEngine = `<piAgentDir>/subagents/config.json` 单字段（多写方共享文件，锁协议与字段域见 §6 对应行） | **登记即现状（无 wave，PR #192 2026-08-26 登记）**：engines 清单 owner = 每个 pi 进程一份 subagent-workflow 扩展实例（`syncEnginesFile`，extension 模块加载 `index.ts:186` + 每次 session_start `index.ts:362` 两点写入，内容 = `listEngines()` 代码注册表全量镜像——非用户数据，未来新增引擎零改动出现在 GUI）；defaultEngine 写方全集 4 类（§6 登记行）：runtime RPC RMW（`session-service.ts` setSubagentDefaultEngine）/ agent bash 编辑（`skills/subagent-ext-config/SKILL.md` 指导）/ 用户手编（SKILL.md 字段表公开）/ runtime 启动 ensure 首建（create-only，§4 ⑪）；读方 = runtime `getSubagentEngineConfig`（纯磁盘读，Settings 冷启动无活跃 session 可用）+ extension ModelConfigService（进程启动 + session_start 各读一次） | renderer Settings 选择器 → WS RPC `session.setSubagentDefaultEngine`（engineId 校验：engines.json 清单内才允许，防 GUI 写未知引擎；同值幂等跳过）；engines 清单 renderer 零写（只读消费）；extension 读侧无写口 | engines.json 缺失/损坏/空数组 → 静态声明回退（subagent-workflow package.json `xyz-agent.subagentEngines`，守护测试防与代码注册表漂移）→ 最终兜底 `['pi']`（pi 恒可用）；config.json 缺失/坏 JSON/defaultEngine 坏值（非字符串/空串）→ 缺省 `'pi'`（extension 侧同缺省语义，坏值静默忽略不报错） | 三级回退链（U7b 冷启动）；engines.json 跨进程多写豁免锁论证 + config.json 锁协议/字段域/last-write-wins 残余风险 → §6 两行（C-data-09） |
| #19 | subagent 虚拟分区实时内容（Agents drawer 内 subagent 对话流：WS `session.subagentEntriesAppended` entry 增量帧 + `subagent.stream_delta` 打字机中间态） | relay 子进程 stdout 翻译产物（runtime `RelayTee`：event-adapter 纯翻译 → entry 化提取，独立实例状态）；终态权威 = 引擎侧制品（pi relay 子进程的 session JSONL / 非 pi 引擎 journal+db，reload 腿经 getSubagentHistory / #8 修订的引擎历史链回读） | **登记即现状（无 wave，PR #192 2026-08-26 登记）**：runtime `RelayTee` per-relay-子进程实例（行缓冲 + 帧序状态独立持有，不与主 pi 会话 adapter/interpreter 共享任何状态）；renderer 消费副本 = chat store 虚拟分区（`subagentVirtualId(mainSessionId, recordId)`：reducer state + messages ref，ADR-0049 分区范式） | WS `session.subagentEntriesAppended` 帧 → route-inbound `onSubagentEntries`（全 session 生效——帧可先于 drawer 打开到达也要写分区，不依赖 per-focus 订阅）→ useMessageEffects → `chatStore.applySubagentEntries`（**#7 `applyEntryFrame` reducer 同构**：PiEntry 逐条喂 reducer + 基线投影 + toolCall overlay 后置；与 reload 腿喂同一 reducer——live ≡ reload 构造性成立，等价性测试 `relay-live-reload` 锚定）；`subagent.stream_delta` = transient 打字机（assistant 定稿 entry 取代中间态） | 无 last-value 快照语义——message-bus **state-no-key 混合形态**（`session.subagentEntriesAppended` 在 TOPIC_TABLE 登记 'state' 但刻意不入 STATE_TYPE_KEY_MAP、不入 ring：增量 entry 流非 last-value，快照同 key 覆盖会丢中间 entry、高频帧入 ring 会冲刷主流可回放缓冲；对账不靠快照/ring = reducer 按 entry id 幂等去重 + 重开经 fetchAndInject 全量快照，message-bus.ts 例外登记注释） | ① 推送已是权威翻译产物直写 store = #13 同构形态注记（#7 实时 feed reducer / #6 queue_update 帧直达先例族）；② toolResult entry 超 256KB 只投截断摘要（完整内容留给 reload 腿）；③ 连续 ≥50 次单事件失败放弃 tee 分支（drawer 降级快照 + reload 腿）；④ 基线投影只含本连接存续期间 live entry——丢帧后 fetchAndInject 补齐的头部会被下一次投影替换，重开 drawer 重新快照自愈（store.ts 已知限制注记，W22 对账收敛前的临时态） |
| #20 | 模型能力档位（`ProviderInfo.models[].supportedLevels`——模型 popover 可用思考档，随 `config.providers` 下发） | pi-ai 同源计算（`getSupportedThinkingLevels`，0.84.1 dist/models.js，探针族守卫）+ 在线 `get_available_models` 对账（旁路诊断） | **登记即现状（无 wave，PR #193 U5 2026-08-28 登记）**：runtime `ModelCapabilityRegistry`（`packages/runtime/src/services/model-capability.ts`，ModelService 实例持有的派生缓存 `levelsBySignature`，声明处 `@data-owner #20` 注解）唯一 owner；renderer 零推导（`resolveAvailableLevels` 影子推导已删，消费注册表下发的 `supportedLevels`，core `normalizeSupportedLevels` 仅做缺省归一非模型推断） | 唯一写入口 = `attachSupportedLevels`（config.providers reply 与 broadcast 两路同标，投影一次）；缓存写方 = `levelsFor` 签名键 miss 时 compute（逐模型签名 = reasoning + thinkingLevelMap 排序序列化，值不可能陈旧；批量作废键 = pi 版本 + models.json mtime + builtin-providers.json mtime，任一变化整表作废）；在线对账结果不缓存不落盘（每附着一次现查现比） | 字段缺省 = 下发链路未接通（renderer 归一默认五档，非本地推导）；reasoning 缺失/false → `['off']`（与 pi 两级门控逐字节一致）；缓存为纯派生可丢弃，删除后 attach 路径重建即一致 | 对账 drift 仅记 runtime 日志 + `model:capabilityDrift` 诊断通道（renderer 当前零消费方），不回写缓存不落盘；runtime 首次 import pi-ai 的格局反转与双副本 pin 纪律见 model-capability.ts 头注（D6 版本门禁机器保证） |
| #21 | 模型档位记忆表（composer per-model thinking level 偏好：`"provider/modelId"` → UI 档位，设计 [model-thinking-level-memory](../design/model-thinking-level-memory.md)） | renderer localStorage（KVStorage，key `xyz-agent:model-thinking-memory`；设计 D1 选型：纯 composer UI 偏好不进 runtime 协议） | **登记即现状（无 wave，feat-model-thinking-level-memory 2026-08-31 登记）**：core composer 域 `model-thinking-memory.ts` `record()` 单写点（isThinkingLevel 校验 + 同步内存 + 异步写穿）；内存镜像 = 模块级 reactive Map 单例（taste:allow-no-data-owner W24-EX-B **随本条落定非草稿**）；消费 = sync watch 恢复查询（lookup）+ 记录 watch（设计 D2 双条件门禁） | 登记即现状：`lookup/record/loadOnce/onLoaded` 单模块收口，无第二写方 | `lookup` undefined = 无记忆（回落现有档位对齐规则，非缺数据态）；惰性加载完成前 undefined 同语义（设计 E7①——短暂无记忆窗口） | KV 写失败内存存活不回滚（E2：内存与磁盘短暂分叉，重启自然收敛，console.warn）；加载窗口内 record 挂起、加载完成后补写收敛（防局部快照覆写 KV 整表静默清除未知条目）；记忆档位不可用时不发非法值（恢复侧设计 D5 supportedLevels 校验回落） |
| P1 | plugin sessionData（插件 per-session KV，含 `plugin:statusBarUpdate` 等 WS 推送） | runtime `SessionDataStore`（`packages/runtime/src/services/plugin-service/session-data-store.ts`） | **已 owner 化**（现状 = 目标，无 wave）：WriteBackCache 单写路径 + per-write debounce（500ms）+ 磁盘恢复；**修订（2026-08-28，PR #193 D9）**：定时 flush（5s）已删（commit 175074197，startFlushTimer / stopFlushTimer / FLUSH_INTERVAL_MS 全删，`runtime/src/index.ts` 启停接线同步删除），落盘保障收敛为 per-write debounce 500ms + shutdown flushAll——crash 丢失窗口从 ≤5s 扩大到进程生命周期（插件键值面非会话内容，D9 已接受）。消费者（PluginService / session-data-api / plugin-rpc-setup）经唯一类入口操作，无第二写方、非多源病灶——不进 12 类清单，登记为「已 owner 化声明」条目以维持 G2「任何 GUI 数据可查 owner」的完整性 | `SessionDataStore` 公共方法（唯一类入口） | 无字段空值语义（KV 形态，key 缺失 = 未设置） | 无 |
| #21 | 最近工作区列表（cwd / label / lastUsedAt，≤6 条 LRU；项目选择器数据，WS `workspace.listRecent` / `workspace.record`） | `<configDir>/recent-workspaces.json`（xyz 自有文件，pi 零触碰） | **登记即现状（无 wave，2026-08-29 PR #193 review round 4 补登——存量未登记，D9 触碰其 flush 行为）**：runtime `RecentWorkspacesStore`（`packages/runtime/src/services/workspace/recent-workspaces-store.ts`，WriteBackCache 单写路径）唯一 owner；落盘保障 = per-write debounce 500ms + shutdown flushAll（**修订（2026-08-28，PR #193 D9）**：定时 flush（5s）已删，口径同 P1 行修订注记，crash 丢失窗口语义同） | `RecentWorkspacesStore.record` 唯一写入口（经 `WorkspaceService.record` 收口；调用方 = `workspace.record` WS + `session-lifecycle.ts:411` + `message-dispatcher.ts:125`）；renderer 零写（`workspace.listRecent` → `workspace.recentList` reply 只读消费） | `record` 空 cwd 静默跳过（INV-1 store 层兜底，service 层主守卫）；文件损坏/缺失返空不抛（INV-4，非 ENOENT 记 warn）+ homedir 条目读取期跳过清理（方案 A 自愈） | 无跨进程写方（非 §6 共享面）；LRU 淘汰 lastUsedAt 最小者（INV-2）属数据生命周期语义非例外 |
| P2 | provider 扩展配置（provider 级 quota 套餐绑定 / authMethod 凭证形态标注 / modelStates 模型启停 → ProviderInfo → 设置页） | `<piAgentDir>/config/providers.json`（xyz 自有文件，路径 SSOT = `pi-paths.getProviderExtrasPath`；pi 不扫描 agent/config/ 子目录——断言锚点见该函数注释，机器防线 = `infra/pi/__tests__/pi-paths-config-dir-contract.test.ts`） | **已 owner 化**（现状 = 目标，无 wave；2026-08-22 PR #187 round1 MF1 登记，A1 起出生即单写方）：runtime `XyzProviderStore`（`packages/runtime/src/services/provider-extras-store.ts`，组合根单例注入 ConfigService/QuotaService/A1-2 迁移）单写方，三生产写方全部经 `modify` 锁内 RMW（迁移 `migrateProviderExtras` / `setProvider` authMethod+modelStates / `QuotaService.persistQuotaConfig`），无第二写方、非多源病灶——不进 12 类清单，登记形态同 P1 | `XyzProviderStore.modify/delete`（proper-lockfile 锁内重读最新文件 + atomicWrite；损坏 quarantine `.corrupt-<ts>` 后按空配置继续）；pi 子进程零写（§6 对照行） | KV 形态（providerId 条目缺失 = 未设置）；条目内 authMethod/quota/modelStates 字段独立可选，`modify` 返回值整条替换（undefined 字段 = 显式清除，既有值继承由写方 fn 负责——`provider-extras-store.ts` modify 契约） | ① 双读回退（A1-3）：providers.json 无该 provider 条目时回退读 models.json 旧寄生字段（迁移失败/未迁窗口兜底；迁移成功后 models.json 已剥离、恒命中 providers.json）；② 历史来源：A1-2 迁移自 models.json 寄生字段（quota / authMethod / models[].enabled → modelStates），幂等收敛，models.json 备份 `models.json.bak-migrate-<ts>` |

## 2. 字段空值语义三条区分（D1b 落字，防双登记矛盾）

1. **`sessionName` 空 = 合法态（未命名，必须整字段覆盖）**：pi `get_state.sessionName` 合法值 `string | undefined`，未命名初始态即 undefined——若一刀切「空值不覆盖非空值」，未命名 session 的快照永远保留旧名，影子状态复活。owner 合并规则 = 权威源**整字段覆盖，含显式空值**。wire 层归一细则：JSON 序列化丢 undefined key，快照解析按字段 schema 归一——sessionName key 缺失 = 未命名 = 覆盖，禁止把「key 缺失」当「字段不动」。**label 是 sessionName 在 xyz 侧同一数据链投影（W7 label 实例 fetch 即 `get_state().sessionName`），空值语义唯一化，不单独登记「label 空 = 未设置（可守卫）」**（曾双登记出相反语义，「可守卫」语义仅属磁盘扫描占位值语境，已被第 3 条覆盖）。
2. **`thinkingLevel` 无空值语义（永不 guard）**：pi `ThinkingLevel` 为具体字符串联合类型、不含 undefined；wire 层 key 缺失按协议异常处理，不做空值合并。
3. **`modelId` / `tokenCount` 磁盘扫描占位值守卫**：`scannedToSummary` 硬编码的 `modelId:''` / `tokenCount:0`（`session-scanner.ts:81-82`）是「无数据」占位符而非权威空值，守卫语义 = **占位值不覆盖已知真值**（W15 落地）——仅用于磁盘扫描路径，不与第 1 条的权威空值覆盖规则混用。

## 3. #1 label 条目：xyz 指向 pi JSONL 的写点处置全集（6 处）

xyz runtime 指向 pi session JSONL 的写点全集共 6 处（r3 审查补漏定全集，W2 以全量 grep 自查收口），处置全部有着落：

| 写点 | 位置（W1 后实测） | 处置 |
|---|---|---|
| 1. 活跃 session 手动 rename 直写 | 原 `session-lifecycle.ts` renameSession 活跃分支（`persistSessionName` 直写，plan 基线 `:296`） | **W1 已移除**：切 pi `set_session_name` RPC（`rpc-client.ts:519`，`session-lifecycle.ts:323` 调用；client undefined / RPC 失败一律 throw 走上层 toast，禁静默 no-op） |
| 2. turn_end / agent_end 兜底直写（tryPersistLabel） | 原 `session-service.ts:1282-1286`（经 `persistSessionName` 直写初始 label） | **W1 已移除**：机制全链删除（`tryPersistLabel` / `labelPersisted` 全仓 0 命中）；显式初始 label 改 create/fork 时经 RPC（`session-lifecycle.ts:116` persistExplicitLabel → setSessionName），派生 label（basename(cwd)）退役为显示派生不再持久化 |
| 3. 非活跃 session 手动 rename 直写 | `session-lifecycle.ts:331`（renameSession else 分支，`sessionStore.persistSessionName` → `session-file-utils.ts:417` 实现、`:430` `openSync('a')`） | **已移除（W11）**：切 `process-manager.withEphemeralPi` 短命附着 + `set_session_name` RPC（renameSession else 分支，失败走既有报错保留旧名，见例外①） |
| 4. `persistHandedOff` handoff_marker 直写 | `session-file-utils.ts:455` 实现、`:467` `openSync('a')`（调用链 handoff-service.ts:286 → session-service.ts:1076 → session-store.ts:88） | **已移除（W11）**：迁 sidecar `.handoff.json`（`persistHandoffSidecar`；读侧 `extractHandedOff` sidecar 优先 + 尾读 fallback 存量兼容，见例外②/⑤） |
| 5. `patchSessionCwd` 整文件重写 | `session-file-utils.ts:521` 实现、`:543` `atomicWrite`（唯一生产调用 `session-lifecycle.ts:434` restoreSession） | **已移除（W11）**：迁 restore tmp 读改写管线（`applyHeaderCwdFallback` 对 tmp 首行 header 应用、仅降级时生效，源文件零写，见例外③） |
| 6. `createForkedSessionFile` fork 文件写入 | `session-fork.ts:74` 函数、`:175` `writeFile` 新建文件 | **创建型合法形态，登记保留（零代码改动）**：目标文件写前不存在、无任何进程持有，写后即 spawn pi 移交——不属「写 pi 当前持有的文件」（见合法形态⑥） |

写点 1-5 已全部消灭或迁移（W1×2 + W11×3），6 登记后保留；pi JSONL 本体的 xyz 侧写点已归零、R1 allowlist 已清空为空集（写点集合与源码真实状态一致，防后续 review 误判「另有未登记写方」——r1/r2/r3 连续三轮审查均在此处扫出遗漏）。

## 4. 例外与合法形态登记（十项）

前四项 = 例外（带期限的债务，非制度）；⑤-⑦ / ⑨ / ⑪ = 合法边界形态（规则边界的一部分，非例外）与范围注记；⑧ = 护栏口径登记（R3 规则豁免清单，非数据写方例外）；⑩ = 数据身份治理条目（ADR-0063 I5，受治理数据身份的登记，非写方例外）。

| 项 | 登记内容 | 竞态边界 / 合法性依据 | 移除期限 / 处置 |
|---|---|---|---|
| ① 非活跃 rename 直写 | `persistSessionName` 非活跃分支（`session-lifecycle.ts:331`，目标文件无 pi 进程持有） | 无 pi 进程时无并发写方，时序安全；但属 xyz 直写 pi JSONL，违反绝对写规则 | **已移除（W11）**：切「`process-manager.withEphemeralPi` 短命拉起 pi 附着该文件 → set_session_name RPC → 关闭」（冷启动中位数 ~500ms 已探明，逐次冷起定型；5s 就绪上限，失败保留旧名） |
| ② `persistHandedOff` handoff_marker 直写 | `session-file-utils.ts:455`，`openSync('a')` append `handoff_marker` entry | **竞态边界 = 活跃交接时源 pi 在场**（handoff 编排保证源 session 交接时仍 active，runHandoff 在 agent_end 后、源进程退出前调 markHandedOff）——真实并发窗口，模式 1 双写方实例 | **已移除（W11），迁移形态 = sidecar**：迁 `<sessionFile>.handoff.json`（家族第 4 后缀，`persistHandoffSidecar` 写 + `extractHandedOff` sidecar 优先/尾读 fallback）；裁决不走 appendEntry——runtime 无法直接经 RPC 写 custom entry，且 handedOffTo 是 xyz 自有语义（pi 无 handoff 概念），消费方唯一（scanner 尾读）与 session_end 同构 |
| ③ `patchSessionCwd` 整文件重写 | `session-file-utils.ts:521`，`:543` `atomicWrite` 重写全文件（header.cwd 降级 homedir） | **竞态边界 = 仅 restoreSession 在 pi spawn 前调用**（`session-lifecycle.ts:434`，调用链结构保证 docstring PRECONDITION「必须在 pi session 启动之前」；实现内含 mtime<1s 并发写防御警告）——目标文件无并发写方。「改经 pi」不可行：pi 无修改 header cwd 的 RPC；「先起 pi 再 patch」恰是写写竞态，顺序反转更危险 | **已移除（W11），迁移形态经 W1（restore-fork-attach-fix）再迁移 = F3 原地归一化**：W11 曾迁「tmp 拷贝上应用 fallback」管线；W1 删除 tmp 管线（pi `switch_session` 永久重绑读写目标，附着 tmp + unlink = 数据丢失），cwd fallback 并入 F3 归一化——`applyHeaderCwdFallback` 变换产物经 `normalizeSessionFileInPlace` 原地 rename-over 落回**原文件**（见 ⑨）。W11 声明「源文件 header 永久保持旧 cwd」**已被取代**：归一化后 header cwd 持久化为 homedir，scanner label fallback 与 deleteByCwd 按新 cwd（home）工作 |
| ④ 队列内容唯一提交方 = renderer | queue 内容权威 = renderer 提交日志（D6 按字段分权威；深度权威 = pi `pendingMessageCount`） | pi RPC 命令全集无队列内容快照、ExtensionAPI 无队列内容读口（均源码穷尽核实）——内容权威只能归 renderer 提交日志；pi 入队存展开后文本，提交原文无法反查 | 长期规则（非债务）：xyz 自研扩展**禁止**使用 `sendUserMessage({deliverAs})` 注入队列（注入条目不在 renderer 提交日志，破坏计数差集）——S1 checklist 拦截；第三方扩展注入的残余风险 = 计数 FIFO 有界偏差，深度仍由 pendingMessageCount 结构性对账。**修订（2026-08-24，PR #189）**：新增登记的 runtime 侧注入方——`prompt(streamingBehavior)` RPC（session-manager send 排队 + completion-backflow 回流，经 pi steer/followUp 队列；详见主表 #6 修订与 #15）属 xyz 自有通道的合法注入形态，非本例外的违例；对账处理同「第三方扩展残余风险」款（计数 FIFO 有界偏差 + pendingMessageCount 结构性对账） |
| ⑤ sidecar 家族五后缀合法形态 | `.meta.json`（persistSessionEnd，`session-file-utils.ts:138`）/ `.preset.json`（persistPresetBinding，`:273`）/ `.project.json`（persistProjectBinding，`:198`）/ `.handoff.json`（persistHandoffSidecar，`:434`，W11 迁入启用；行号 W19 实测按入口函数定位）/ `.agent.json`（persistAgentBinding，agent-managed-session 分支新增，2026-08-22 登记） | **合法性依据 = xyz 自有文件**（pi 体系外的 xyz 数据，不是 pi 的文件）——绝对写规则管的是 pi 的 JSONL；读写收口 session-file-utils 单一 util、写前 existsSync 守卫（规则 #6 防 pi `openSync("wx")` 竞态）、写后失效 meta 缓存，五者同构（D3 选项 a 裁决；agent 版另失效 scanDirCache——spawnSource 消费方是列表扫描） | 长期形态（非例外）：R1 对五后缀内置豁免（`check_pi_direct_write.py` `SIDECAR_SUFFIX_RE` + `agentSidecarPath`/公共层 `sidecarPathOf` helper 识别，agent-managed-session 分支同步扩展），豁免清单与本条一一对应（W19 核对同源同集；agent 后缀 2026-08-22 补登）；**W19 收口确认完成**——读写点全量核查全部经 SFU 收口（读方消费 = `scanSessionMeta` 三读合一 + `session-store` port 转发；写方 persist 函数生产调用全经 sessionStore port）；delete 链（session-lifecycle delete 两分支 + fork 防御清理）sidecar 清理扩为五后缀全集（`.agent.json` 由 agent-managed-session 分支补齐）——`.project.json`/`.handoff.json` 此前残留（W11 verifier 观察项，SFU:189「删除 session 归属自动消失」声明语义的实现补齐，W19） |
| ⑥ fork 文件创建型 | `createForkedSessionFile`（`session-fork.ts:74`，写入点 `:175`），唯一创建入口 | **合法性依据 = 目标文件写前不存在**（新 sessionId + 新文件名，无任何进程持有），写入后 spawn pi 附着——不属「写 pi 当前持有的文件」；「pi 侧 fork」被否（pi 原生 fork RPC 语义限制覆盖不了 xyz fork：任意 entryId 截断 + 独立进程 + 源进程不动）；失败分支 unlink 清理的是本流程刚创建、pi 未附着的孤儿文件（创建者清理） | 长期形态（非例外，零代码改动）：边界约束 = 创建型仅限「目标写前不存在的新文件」，**禁止演进为「重写既有 session 文件」**；R1 对此形态静态不可拦（跨文件数据流），守卫 = 本条登记 + S1 语义层 |
| ⑦ 非写点注记（防后续审查误问） | session 删除链（`pm.destroySession` 先行 + `session-store.trash` → system/trash OS 垃圾桶移动 + sidecar unlink（W19 起五后缀全集清理——`.agent.json` 由 agent-managed-session 分支补齐），无并发持有）与 `pi-maintenance.ts`（`packages/runtime/src/infra/pi/`）一次性目录布局迁移 `renameSync` | **合法性依据 = 均属非内容写**：前者是文件系统移动/删除（OS 垃圾桶），后者是一次性迁移的重命名——均不产生对文件内容的并发写，不在「写点」定义内 | 范围注记（非例外、无期限）：明确排除在 R1 检查范围与写点全集计数外 |
| ⑧ R3 注解规则全域豁免清单（W24 落表） | `require-data-owner-annotation` 扫描范围扩全仓 renderer/core 后的 58 处标记（28 生产文件）= **EX-A 14**（ADR-0049 全局 sid 协调器/订阅注册基建：refCount 保护 listener、useSessionScopedState 工厂基础设施等）+ **EX-B 30**（模块级单例 UI 瞬态，12 类未覆盖存量：toast/fork notice/permission 请求/drawer/search modal/ws-client 重连态等）+ **EX-C 10**（非 GUI 数据技术结构：RPC pending promise 表/防抖 timer 句柄/LRU/i18n loadedLocales 等）+ **EX-D 4**（VITE_MOCK 测试基建，全部 `api/mock/index.ts`）；另有 3 处 `@data-owner` 正向注解（useSessionDerivations #11 活跃态派生缓存 + #7 消息 digest 派生缓存、useChat #7 historyTruncatedSessions——renderer 纯派生缓存非第二写方，注解指向主表编号）。**修订（2026-08-21，PR #186 MF1）**：trace 台账登记主表 #13 后，useSessionTrace `pendingAppends`（EX-B 中唯一持有 GUI 数据本体的条目——loading 窗口推送缓冲，flush 后即空）从 EX-B 豁免转为正向注解 `@data-owner #13`（正向注解 3→4 处）；EX-B 其余条目仍为 12 类未覆盖 UI 瞬态存量，口径不变（存量计数随各 PR 演进，以代码 grep 为准） | **合法性依据 = 护栏口径而非写方例外**：R3 管的是「GUI 数据 owner 可查」，四类均非 12 类 GUI 数据（基建自身/瞬态 UI 态/非 GUI 结构/测试桩）。**loadedLocales 特注（verifier W24 minor 3）**：`i18n/index.ts:56` 字面量初始化后仍被运行时 `add('en-US')`（`:73`）变异——「字面量初始化后运行时写」罕见形态，显式例外登记归 S1 语义层兜底，非口径内豁免 | 长期口径（非债务）：EX-B 30 处 12 类未覆盖存量随 UI 重构逐步 owner 化（每归一处删一处标记）；新豁免先本表登记再打标记（规约 3 闭环）；豁免标记格式 = `taste:allow-no-data-owner W24-EX-<类>`，类别语义以本行为准 |
| ⑨ restore-time 归一化 rename-over（`.tmp-migrate-` 临时名） | `normalizeSessionFileInPlace`（`packages/runtime/src/infra/pi/session-file-utils.ts`，restore-fork-attach-fix W1 F3），唯一生产调用点 `session-lifecycle.restoreSession` 的 F3 分支（原文含 session_end 行 **或** header cwd 死路径时触发）：`stripSessionEndEntries` +（仅 cwd 死时）`applyHeaderCwdFallback` 的变换产物写同目录临时名 `<原名>.tmp-migrate-<ts>.jsonl` → `renameSync` 原子覆盖原文件 → pi `switch_session` 附着**原路径** | **合法性依据 = inactive-only + 同目录临时名 + rename 原子替换 + 变换白名单**：目标文件无 pi 进程持有（restoreSession 开头已 detach/destroy 同 id 会话）；同目录 rename 是 POSIX 原子操作，无中间态可见；内容写发生在 rename 之前、目标是写前不存在的临时名（创建型，同 ⑥）；操作本体是文件系统元数据操作（rename），同 ⑦ 先例。变换白名单仅两项且各有 pi 侧依据——strip session_end（防 pi `_buildIndex` 树索引污染：legacy 行无 id 无 parentId，pi-mono session-manager.ts 对所有非 session entry 无差别 `byId.set(entry.id); leafId = entry.id`，使 leafId=undefined → 新 entry parentId 断链 → 历史不进 LLM 上下文）与 header cwd fallback（防 pi `MissingSessionCwdError` 直接 throw，pi-mono session-cwd.ts；RPC `switch_session` 无 cwdOverride）。**为何必须原地而非新文件**：pi `switch_session` 永久重绑读写目标（pi-mono session-manager.ts setSessionFile 把路径存为永久 sessionFile，`_persist` 每轮 appendFileSync 该路径），附着目标必须是原路径；换路径会断 sidecar 五后缀派生 / fork 血缘 parentSession 指针 / scanner 关联 | 长期形态（非例外）：每文件最多一次（归一化产物无 session_end 且 cwd 已修活 → 下次 restore 走 F2 直附着零改写，幂等收敛）；rename 前崩溃残留的 `.tmp-migrate-*.jsonl` 双保险——scanner 按文件名显式排除（`isScannableSessionFile`——scanner 按内容首行 header 识别 session 不按文件名，残留内容是合法 session，不过滤会产生同 sessionId 双条目且残留 mtime 更新排序在前，`findScannedSession` 命中残留 → 附着错位文件；W1 verifier F1 修复，机制保证取代原「scanner 天然忽略」错误声明）+ **残留清理三机制（差距复审 suggestion 6，2026-08-19 实施）**：rename 失败 `normalizeSessionFileInPlace` 回滚删除临时文件（`session-file-utils.ts`）/ 附着前 `cleanupMigrateResidues` 清扫同 basename 旧残留（`normalizeInactiveSessionFileIfNeeded` 顶部，restore F2/F3 与 renameSession 非活跃分支共用入口）/ delete 链与 sidecar 四后缀同点清扫（`session-lifecycle.ts` delete 两分支）；R1 豁免 = `check_pi_direct_write.py` `TMP_MIGRATE_SUFFIX_RE`（语句级 + 写目标单跳赋值链回溯，条件 B③），与本条一一对应；**边界约束：禁止演进为常态改写机制**——变换白名单之外的内容变更不得经此路径（ADR-0062 §2 第三类合法形态已由 W2 正式增补，2026-08-19） |
| ⑩ 会话文件身份（sessionFilePath / attach 状态，ADR-0063 I5） | owner = runtime 记账层：`initializeManagedSession`（`session-service.ts:1362`，create / restore / fork 三入口把 sessionFilePath 登记进 sessions Map）+ attach 断言守卫 `assertPiSessionFile`（`packages/runtime/src/infra/pi/session-attach-assert.ts` 独立模块、`process-manager.ts` re-export；restoreSession / forkSession / withEphemeralPi 三接线——「附着必断言」无例外结构；三个 mock 兼容跳过分支的覆盖边界见 ADR-0063 I1） | **权威源 = sessions 目录扫描 + pi `get_state().sessionFile` 对账**（pi-mono `modes/rpc/rpc-types.ts:101`；pi `switch_session` 是永久重绑读写目标——`core/agent-session-runtime.ts:193-209` 永久采纳 + `core/session-manager.ts:815-816` 永久 sessionFile 字段 + `:934-960` 按路径追加，故「登记路径 ≡ pi 写路径」不变量 ADR-0063 I1 是数据不丢的结构前提）。合法性依据 = 该身份此前无登记（12 类 GUI 数据未覆盖 runtime 记账层），双头分裂（登记 ≠ pi 写目标）曾致 P0 数据丢失静默 40 天；现行护栏三层 = 本条目登记 + 运行时断言（fail loud）+ 生命周期等价测试（`__tests__/equivalence/attach-lifecycle.test.ts`，ADR-0063 I3） | 长期形态（非例外）：新增附着调用点必须接线 `assertPiSessionFile`（照抄三接线先例）；改动会话文件身份链路（登记 / 附着 / 归一化 / 重附着）的 PR 须对照本条目 + ADR-0063，review 拦截点 = review-data-governance checklist 两条 MUST（对话数据禁入 $TMPDIR · pi 行为断言须带 pi-mono 源码锚点） |
| ⑪ extension startupConfig 首建写（create-only 豁免形态，PR #192 登记） | `ensureDeclaredStartupConfigs`（`packages/runtime/src/services/extension-startup-config.ts`）：runtime 启动后台序列统一 ensure 各 extension 在 package.json `xyz-agent.startupConfig` 声明的配置文件（相对 agentDir 路径 + 静态默认内容；例：subagent-workflow 声明 `subagents/config.json` 默认 `{version:1, maxConcurrent:6}`）。解决「装好打开应用、一个 session 都没建时配置文件不存在、不可发现、不可手编」的体验缺口 | **合法性依据 = 创建型 create-only（同 ⑥ fork 先例）+ 结构性「绝不覆盖」**：目标已存在 → 一律跳过（含用户改过/改坏的文件——坏文件由各 extension load 侧 normalize 回落处理，非本机制职责）；缺失首建走 `writeFileSync` flag `'wx'`（O_EXCL）——把「绝不覆盖」从 check-then-act 升级为结构性保证，runtime 与 extension 惰性 ensure 并发首建竞态下 EEXIST = 另一写者已建同内容文件，等价跳过；首建无旧内容可损，直接 writeFileSync 即可（原子写是为防覆盖进行中的旧内容，此处不存在）；守护测试锁两侧默认内容深相等 | 长期形态（非例外，机制本体）：路径校验双保险（声明必须相对路径无 `..` 段 + resolve 后界内复核，防逃逸写 agentDir 外）；**边界约束**：注册物是静态声明而非初始化代码——内容由代码派生的状态（如 engines.json 来自 `listEngines()`）不适用本机制，走双层机制（静态声明兜底 + extension 到场覆写权威版，主表 #18）；声明与各包代码 DEFAULT 常量等值由各包守护测试锁死（防 package.json 声明与代码默认值漂移）；跨包重复 path 声明去重保留首个（warn 点名双方） |

## 5. 维护规约

1. **W6-W8 同步维护（强制）**：`ReplicatedState` 每落地一个实例，主表对应行的「唯一写入口 / 字段空值语义」列即更新为配置引用——本表 P1 起演进为可执行配置，条目失真即护栏失真。
2. **例外是带期限的债务**：①-③ 已移除（W11，§4 前三项已改记），R1 allowlist 已清空（空集 + 检查无条件化）。
3. **误报豁免闭环**：R1/R2/R3 拦到合法写入时，豁免路径 = 先在本表补条目/例外 + 豁免 allowlist 登记，禁止在代码里静默绕过。
4. **无专门 wave 条目（#10 / #11）**：父文档 §5 的 19 个实施单元未覆盖，登记现状 + 原则性目标；改动这两条链路时须遵守 §3.0 原则（绝对写规则 / 投影一次），并先在本表补登记再动手。
5. **新数据先登记**：新增 GUI 数据（新 store 字段 / 新缓存 / 新 WS 推送）必须先在本表建行（编号顺延）或并入既有条目，再写代码——R3 强制注解的条目即指本表编号。

## 6. 跨进程共享文件登记表（integrity-hardening D8a）

> 治理对象：≥2 个进程都会写的磁盘文件（GUI 数据多源之外的元模式 ①——「无锁双写」）。父文档：[integrity-hardening.md](integrity-hardening.md)（§3.1 D1 / §3.5 D5 / §3.8 D8a）。原则：凡登记为「跨进程共享」的文件，全部写入方必须使用**同一把锁**（同一 lockfile 路径），锁内读-改-写且只写调用方声明的字段域；无锁的第二写入者 = 契约违规。**新文件入表是 pr-cr-fix review checklist 项**——新增跨进程共享文件写路径必须先在本表登记锁协议再写代码。

| 文件 | 写入方（进程） | 锁协议 | 字段域归属 | 损坏隔离 | 状态 |
|---|---|---|---|---|---|
| `~/.xyz-agent/pi/agent/settings.json` | pi 子进程（SettingsManager）+ xyz runtime（pi-settings-store） | 同一 lockfile `<settings.json>.lock`。**pi 侧**（不可改，铁律）：`lockSync(realpath:false)` + ELOCKED busy-wait 20ms×10 后抛错放弃、无 stale、仅文件存在才锁；**xyz 侧**（W1a 实施）：`lockSync(realpath:false, stale:30_000)` + 自实现 busy-wait 25ms/预算 ~1s fail-fast（lockSync 不支持 retries——ESYNC）。不对称安全性四点论证见 integrity-hardening.md §3.1 D1a（核心：互斥只依赖同一 lockfile + 双方先取锁再写；xyz stale 夺取让双方自愈） | xyz 侧 `updateSettingsFields(scope)`：`model`（defaultProvider/defaultModel/defaultThinkingLevel/enabledModels）/ `skills` / `extension`（packages）/ `full`（白名单仅 pi-maintenance 启动迁移，review 禁新代码用 full）。pi 侧 persistScopedSettings 同语义 | JsonStore quarantine（`.corrupt-<ts>` + 恢复指引日志，W0 D1c） | **已实施（W1a，探针 3×300 轮零丢失）** |
| `<piAgentDir>/config/rename-session-ext-config.json` | xyz runtime（worktree-config-helper.setRenameModel 写 `model` 字段）+ pi 子进程内 rename-session 扩展（`extensions/rename-session/src/pure.ts:257` 写 enabled/maxTitleLength/thinkingLevel 等） | 同一 lockfile（ext-config 文件自身 + `.lock`）。**已实施双端闭环（W4）**：runtime 侧 withFileLockSync（W1b）+ 扩展侧 llm-shared `saveConfig` 持 `@zhushanwen/pi-file-lock` withFileLockSync（`config.ts:26,207`），协议逐字对齐（realpath:false + stale 30s + busy-wait 25ms/预算 1s fail-fast）。**锁失败语义（与 worktrees 行不同，双端登记）**：extension 侧锁失败返回 `success:false` **不降级**——对端 runtime 可能正持锁写，无锁写会交错丢字段；worktrees 锁失败可降级无锁 RMW 的差异理由见下行。tmp 双侧唯一化（pid+随机段，`config.ts:130-132`；runtime 侧同步对齐），`<path>.tmp` 同名碰撞面消除 | runtime = `model`；扩展 = 其余字段 | 待补（D1c quarantine 模式，登记 debt；当前坏 JSON 回退 defaults，两侧读路径行为一致） | **已实施（W4 双端闭环）** |
| `<piAgentDir>/config/<其他包>-ext-config.json`（permission / model-switch 等） | 仅该包扩展自身（pi 子进程内），runtime 无同文件写方 | 单写方无跨进程双写面，无需锁（登记备查） | 整文件归该扩展 | — | 健康（登记对照） |
| `<piAgentDir>/subagents/worktrees.json` | 每个活跃 pi 进程一份 subagent-workflow 扩展实例（多写方） | **已实施（W4）**：`@zhushanwen/pi-file-lock` async 版 `withFileLock`（`worktree-registry.ts:162`）——retries 10 次指数退避 100ms~10s randomize + stale 30s + onCompromised（锁被 stale 夺取时 fn 前抛错）；写方全在 xyz 生态（D5a）。**锁失败降级无锁 RMW + warn**（`worktree-registry.ts:166-178`）——与 ext-config 行的 fail-fast 语义不同，理由：worktree 条目丢失有对账兜底可收敛（见下格），ext-config 字段丢失无兜底 | 整文件（条目级 append/remove/updatePid） | —（对账兜底**已实现**：`worktree-manager.ts:371` reconcileWithPhysical 双向 diff 自愈，D5b——物理面 `git branch --list 'pi-sub-*'` + tmpdir 扫描对注册表，死清活补、歧义保守跳过） | **已实施（W4；bundle 探针 proper-lockfile 内联 15-18 处 + pi CLI 冒烟 exit 0）** |
| `~/.xyz-agent/pi/agent/auth.json` | xyz runtime + pi 子进程 | proper-lockfile async `lock()` + retries 指数退避 + stale 30s + onCompromised 守卫（`auth-storage.ts:42-74`） | 整文件（双方全量读写对称） | ensureFileExists + 0600 原子写 | **已达成（范本条目）** |
| auto-rename 标志文件（`<piAgentDir>/auto-rename-enabled`，`worktree-config-helper.ts:52`） | xyz runtime（setAutoRenameEnabled）+ rename-session 扩展（setAutoRenameSwitch） | **豁免锁**：语义 = 「文件存在=开，不存在=关」，内容恒空从不读取，create/delete 幂等（W1b 核实：worktree-config-helper.ts:144-160 与 pure.ts:160-170 同构）。最坏交错 = 布尔开关 last-write-wins，属开关语义本身，非数据丢失 | — | — | **豁免登记（W1b 核实）** |
| quota-cache.json（quota-providers 配额缓存） | 多个 pi 进程内的 quota-providers 扩展实例（多 session 并发时） | **豁免锁（易失缓存论证，R2 审查登记）**：数据为 TTL 2 分钟纯缓存，并发写交错最坏损失 = 单次缓存写入（TTL 过期重建自愈），无持久数据丢失面；tmp 唯一名（pid+随机段，`cache.ts` uniqueTmpPath）+ rename 原子写（atomicWriteJson，写/rename 抛错清理残留 tmp 后重抛，D1e 对齐上行 ext-config 同款），固定 `.tmp` 同名互踩面消除。权衡：唯一名不自覆盖，硬崩溃残留的 `.tmp_*` 文件不自行消失（固定名时代的自覆盖清理换成显式错误路径清理），属易失缓存旁侧垃圾、不影响读取。损坏面已治理（parse 失败 quarantine，`.corrupt-<ts>` + 恢复指引日志，R1 修复轮）。若未来升级为持久数据须回本表升格为锁条目 | 整文件（缓存 KV） | 已实施（quarantine） | **豁免登记**（非原子写族其余项见 integrity-hardening 附录 A backlog） |
| `<piAgentDir>/system-prompt-trace-baseline.json`（system-prompt-trace 自持久化基线，`extensions/system-prompt-trace/src/baseline.ts` writePersistedBaseline） | 每个 pi 子进程一份 system-prompt-trace 扩展实例（session pool 多 session 并发时多进程共享同一 agentDir，RMW 同一文件） | **豁免锁（有界丢失论证，PR #186 MF2 登记）**：RMW 竞态按 last-writer-wins 容忍——最坏交错丢某 session 的基线条目，后果有界可自愈（下次 resume 多写一条留痕 entry，设计 D2 已接受，非 auto-rename 式「零丢失」豁免，属 quota-cache 同款有界自愈类）。读方永不见半截 JSON：唯一 tmp（`.tmp_<pid>_<随机段>`，ext-config W4 / quota-cache D1e 同款 uniqueTmpPath）+ rename 原子替换；写/rename 抛错清理残留 tmp 后重抛（唯一名不自覆盖，不清理会随崩溃累积残留）。长期升格路径：若未来基线丢失代价不可接受，升格 `@zhushanwen/pi-file-lock` withFileLockSync 正规锁条目（worktree-registry.ts:162 / rename-session config.ts 双端先例） | 整文件（sessions map，per-session 基线条目） | 读方逐 entry 校验（loadBaselineFileForWrite 丢弃损坏/形状不符 entry），JSON 损坏 → 空文件重开 | **豁免登记（PR #186 MF2）** |
| `<dataDir>/update/updater.pid`（升级脚本互斥信号文件，`constants.ts` UPDATER_PID_FILE，批次 5 §3.7.1） | ① mac/linux detached updater 脚本（`updater-script.ts` 模板 `echo $$ > "$PID_FILE"` 启动即写，`trap 'rm -f "$PID_FILE"' EXIT` 覆盖全部退出路径清理，kill -9 例外）② win 场景 xyz main 进程（`platform-updater.ts` writeUpdaterPid 写 child.pid——cmd wrapper 无内建变量可廉价自取 PID；无退出清理，模板不在写方领地）③ xyz runtime 读侧自愈（`update-self-healer.ts` isUpdaterInFlight：PID 已死 kill(pid,0) 失败 → unlink 残留；mac/linux 叠加 `ps -o command=` argv 校验防 PID 复用误判） | **豁免锁（互斥信号本体论证，PR #193 登记）**：文件内容 = pid 字符串单字段，语义 = 「存在且进程存活 = updater 在飞」，写方为互斥信号的生产者而非共享数据的并发编辑者——无 RMW 字段丢失面（各写方整文件覆盖写自己的 pid，读方只做存在性 + 存活性判定）；单活跃 updater 由互斥语义本身保证（在飞时新升级入口 fail-fast），最坏交错（写失败/kill -9 残留）由读侧死 PID 自愈 unlink 收敛，platform-updater 写失败显式降级为「无互斥探测」仅告警不阻断（互斥辅助信号定位，宁降级不阻塞升级主链路）；对齐 auto-rename 标志文件豁免先例 | pid 十进制字符串（唯一字段） | 解析失败（非整数/≤0）按「无在飞 updater」处理 + 读侧自愈清理 | **豁免登记（PR #193 review round 1，C-data-09）** |
| rfl run 遥测（`~/.review-fix-loop/<repo-slug>/<runId>/state.json`，tier-1 §7.5） | 仅 review-fix-loop workflow 单写（`review-fix-loop.js` `saveState` tmp+rename 原子写）；`scripts/rfl.mjs` CLI 读取为主，另含 `clean --older-than <N>d` 生命周期删除（干跑默认，`--yes` 才执行 `rmSync` 整 run 目录——限 run 级 state/telemetry 文件，不触 session 数据；§4 ⑦ 同类生命周期操作，非内容写） | 单写方单读方，非跨进程共享，无锁需求（登记对照）。**$TMPDIR 降级理由**：home 不可写时才降级（`resolveRunRoot` WARN），降级后属易失 run 遥测（calls/issues/scores/dormant/fixResults）——非 GUI 数据（12 类外）、非会话内容（I2 定义外），系统清理丢失可接受 | 整文件（run 级快照） | 原子写 + per-run 目录隔离（坏 run 不波及邻 run） | 健康（登记对照，PR #185 review S3 留痕；设计锚点 [tier-1-cheap-wins.md](../todo/review-fix-loop-efficiency/tier-1-cheap-wins.md) §7.5/§11） |
| plugin sessionData（`<getDataDir>/plugins/...`） | 仅 runtime PluginService（Worker/fork 子进程单写，flush 回主进程） | 单进程单写方，非跨进程共享（对照组——证明登记边界） | 整文件（KV） | WriteBackCache + flushAll（shutdown） | 健康（登记对照） |
| `<piAgentDir>/config/providers.json`（xyz 扩展域，主表 P2） | 仅 xyz runtime（`XyzProviderStore`）；pi 子进程零写——pi 不扫描 agent/config/ 子目录（断言锚点见 `pi-paths.ts` getProviderExtrasPath 注释，机器防线 = `infra/pi/__tests__/pi-paths-config-dir-contract.test.ts`） | 单写方 + 多 xyz 实例并发面：proper-lockfile async `lock()` + retries 指数退避 + stale 30s + onCompromised throw（`provider-extras-store.ts` withFileLock，参数对齐上行 auth.json 的 AuthStorage——锁内重读 + atomicWrite，多实例 RMW 互斥；pi 无写方，锁不构成跨 pi 协议） | 整文件（条目级 `modify`/`delete` RMW；version 恒 1） | quarantine `.corrupt-<ts>` + 按空配置继续（`provider-extras-store.ts` readInternal，对齐 JsonStore 隔离行为）；迁移备份在 models.json 侧（`models.json.bak-migrate-<ts>`，A1-2） | 健康（登记对照，PR #187 round1 MF1） |
| `<piAgentDir>/subagents/config.json`（subagent-workflow 全局配置：version / defaultEngine / engineRouting.strict / maxConcurrent，主表 #18） | ① xyz runtime（`session-service.ts` setSubagentDefaultEngine，RMW 写 defaultEngine 单字段）② agent bash 编辑（`extensions/universal/subagent-workflow/skills/subagent-ext-config/SKILL.md` 指导 agent 改写，pi 进程侧发起）③ 用户手编（SKILL.md 字段表公开路径与字段形态）④ runtime 启动 ensure 首建（extension-startup-config，`'wx'` 仅缺失时 create-only，见 §4 ⑪——写前不存在，无覆盖面，良性） | **已实施（PR #192 review round 1 登记；C-data-09 目标态，锁化代码本 PR 落地）**：runtime 写方接入 `withFileLockSync`（runtime `utils/file-lock.ts`，lockfile = `config.json.lock`；协议对齐上行 rename-session ext-config W4 先例：realpath:false + stale 30s + busy-wait 25ms/预算 1s fail-fast，锁失败抛错经 RPC 错误通路返回 GUI）锁内 RMW（锁内重读最新文件 → 改 defaultEngine 单字段 → tmp(pid+ts)+rename 原子写，rename 失败清理 tmp）。写方 ②③ 无锁（bash/手编整文件写，无法收编锁协议）——**last-write-wins 残余风险显式登记**：最坏交错 = 无锁写方在 runtime 锁内重读之后落笔，整文件内容覆盖 runtime 刚写的 defaultEngine（或反向覆盖 bash 刚写的 engineRouting/maxConcurrent），后果有界 = 该次字段写回滚，extension 读侧（进程启动 + session_start 各读一次、session 内不重读）下次读取自愈，无静默累积丢失 | runtime = `defaultEngine` 单字段（engines.json 清单内校验 + 同值幂等跳过）；agent bash / 用户 = 全字段（version / defaultEngine / engineRouting.strict / maxConcurrent） | 读侧防御（extension `execution/config.ts` + runtime `getSubagentEngineConfig` 双侧一致）：坏 JSON / 坏值静默回默认配置不报错；旧版 categories/fallback/yoloByDefault 字段读取时忽略 | **已实施（PR #192，随本 PR 锁化落地）** |
| `<piAgentDir>/subagents/engines.json`（引擎清单镜像，主表 #18） | 每个活跃 pi 进程一份 subagent-workflow 扩展实例（extension 模块加载 `index.ts:186` + 每次 session_start `index.ts:362` 两点 `syncEnginesFile`；桌面并行 session 常态 ≥2 进程并发）；读方 = xyz runtime `getSubagentEngineConfig` + GUI Settings 引擎选择器 | **豁免锁（有界自愈论证，参照上行 `<piAgentDir>/system-prompt-trace-baseline.json` PR #186 MF2 先例）**：①内容为代码注册表镜像（`listEngines()` 全量投影，非用户数据、无 RMW 累积语义——写方各自全量重写，无「读-改-写放大」丢失面）②读-比较-写幂等（引擎清单未变零写入，mtime 不动——读侧无谓失效）③版本偏斜 last-writer-wins 后果有界：并发窗口内 GUI 可能短暂读到旧清单，下次任一进程 session_start 重同步自愈；tmp 唯一名（pid+ts）+ rename 原子替换（失败路径清理残留 tmp），读方永不见半截 JSON；IO 异常吞掉（fail-safe——可发现性降级不阻塞 session 启动，GUI 走静态声明回退链兜底 `['pi']`） | 整文件（engines 清单 + v + updatedAt 诊断字段） | 读方形状校验（runtime：非字符串数组/空数组 → 静态声明回退；extension 写侧读-比较-写自身防御） | **豁免登记（PR #192 review round 1）** |
| `<dataDir>/base-tool-enhance/<sessionId>/registry.json`（后台 bash 任务登记表，per-session 目录，base-tool-enhance 扩展） | 全部 pi 进程侧、五写方收口单点 `writeRegistryEntry`（`background/registry.ts`）：① spawn 登记 + killing 标记（spawn-background.ts）② poller 终态写入（poller.ts）③ process-exit-guard 终态写入（process-exit-guard.ts）④ bash_kill 工具 killing 标记落盘（bash-kill-tool.ts，intent 落盘在 kill 信号之前——查询面无倒挂窗口）⑤ reaper 孤儿条目终态回写（reaper.ts）；同 sessionId 目录可被发起 pi 进程与桌面 ephemeral 附着进程并发写 | `withFileLockSync(registryPath)` 锁内 RMW：读全量 → 条目级合并（同 taskId 覆盖）→ 终态条目 LRU 上限 50（与单例表 MAX_TERMINAL_TASKS 对称）→ tmp(pid+随机段)+rename 原子写（失败清理 tmp）；**锁获取失败不降级**——返回 `{success:false}`，条目停留 running 由 M5 reaper 兜底对账；reaper 扫描/补杀/写全程再套跨进程 `reaper.lock`（固定名，proper-lockfile mkdir 目录形态，扫描按名排除）——**锁序恒为 reaper.lock → registry 锁**（spawn 侧只取 registry 锁，无环无死锁） | 条目级 RMW（taskId 条目 set/终态 LRU 淘汰 delete；version 恒 1） | 解析失败/形状非法（version 不符/核心标识字段缺失）→ 重命名 `.corrupt` 保留现场（固定名优先，已存在带时间戳不覆盖前一份）+ 按空表重建 + warn 日志 | **已实施（PR #192，随包首发）** |

**维护规约（本节）**：新增跨进程共享文件写路径，先在本表登记锁协议 + 字段域再写代码；已有条目新增第二写入方时，锁协议必须与登记的对齐（同一 lockfile），否则为契约违规；豁免锁条目必须像 auto-rename 一样给出「无 RMW 数据丢失面」的核实依据。
