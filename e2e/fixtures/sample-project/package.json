{
  "name": "sample-project",
  "version": "1.0.0",
  "description": "E2E fixture project — simulates a real project structure for file-tree rendering tests.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.8",
    "vitest": "^4"
  }
}
