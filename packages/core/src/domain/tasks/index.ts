/**
 * domain/tasks —— tasks store 域（P3 绞杀过渡存根，TODO @P4-§6.7-delete）。
 *
 * 按 D9 原样迁自 renderer stores/tasks.ts + tasks-readers.ts（内容零改动，仅归位）。
 * P4 §6.7 builtin tasks plugin 化时整体删除。详见 tasks.ts 文件头标记。
 */
export * from './tasks'
export * from './tasks-readers'
