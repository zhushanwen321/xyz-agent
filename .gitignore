!.env.example
.DS_Store
.env
.env.*
# .superpowers/ skill 临时产物（plan/brainstorm 等），全目录忽略。
# 旧规则只匹配 2026-04-* 和 brainstorm/，漏 2026-06+（事故：2026-06-22-sidebar-polish/plan.md 漏网被 commit）。
.superpowers/
.xyz-harness/gate/
*.local
*.log
dist/
node_modules/
sidecar/dist/
sidecar/node_modules/
src-tauri/binaries/
src-tauri/gen/
src-tauri/target/
src/dist/
src/node_modules/
superpowers/20*/
target/
.xyz-harness/workflow-state.json
.claude/worktrees/

# pi resources — pi binary downloaded by prepare-pi-resources.sh from badlogic/pi-mono
# gh releases. Nothing under here is tracked. Builtin extensions are npm deps (root
# package.json), skills are user/project-level — neither is bundled here.
apps/electron/resources/
.pi/
.pi/infinite-context/

# Electron binary cache (immutable, shared across worktrees)
.electron-dist/
**/__pycache__/
*.pyc

# Playwright E2E artifacts (test results / reports / cache)
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/
/playwright/.auth/

# ZCode plan mode artifacts (session-local planning docs)
.zcode/plans/

# Legacy build dir from pre-monorepo layout (commit 0f6eed87 migrated src-electron/
# to packages/* + apps/electron). Leftover build artifacts + node_modules linger in
# some worktrees; ignore to prevent accidental staging.
src-electron/

# Ad-hoc screenshots (debugging artifacts, not source)
screenshot.png

# CW v1 subagent dispatch payloads (临时入参文件，派发完即废弃)
.cw-rsu-*.json

# impeccable skill 产生的 UI critique 报告（临时审查产物，非源码）。
# AGENTS.md 规则 13 禁止 impeccable/ 目录进版本管理；历史遗留的 4 个已 git rm --cached 清理。
# 子目录也要匹配（事故：packages/renderer/src/.impeccable/ 漏网被 commit），用 /**/.impeccable/ 兜底。
.impeccable/
/**/.impeccable/

# === cw v1 工作流临时产物（根目录散落，应放 .xyz-harness/ 归档）===
# .cw-*.json/.md 是 cw 跑完手动归档的中间产物，不是源码；.cw/（带斜杠）是 tracked 测试脚本，不忽略。
/.cw-*.json
/.cw-*.md
# wave-* / clarify / plan / design-review 等是 cw 当前 wave 的运行时产物。
/wave-*.json
/wave-*.md
/clarify.json
/clarify-*.json
/design-review.json
/plan.json
/plan.md
/exec-review.json
/retrospect.json
/closeout.json
/test.json

# === 备份/临时后缀兜底 ===
*.bak
*.tmp
*.swp
*.orig
*~
