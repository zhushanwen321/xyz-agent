{
  "project": "xyz-agent frontend v3 rebuild",
  "spec": ".xyz-harness/2026-06-20-frontend-rebuild/spec.md",
  "test_plan": ".xyz-harness/2026-06-20-frontend-rebuild/e2e-test-plan.md",
  "framework": "vitest",
  "mock_mode": "VITE_MOCK=true",
  "v1_scope_note": "只测 spec §8.5 v1 in-scope；§9 DEFERRED 项不测",
  "test_cases": [
    {
      "id": "TC-01-1",
      "type": "manual",
      "title": "P0 design-tokens 落 style.css + tailwind.config，冷蓝暗色画布",
      "description": "覆盖 §6 整体验收点「冷蓝暗色画布」+ ADR-0018。验证 --bg/#4f8ef7/#0d0d0f 等 token 命名与色值归一（SSOT=design-tokens.md），radius sm:3px/DEFAULT:8px/lg:12px（D5）",
      "steps": [
        "启动 npm run dev",
        "DevTools 检查 :root CSS 变量与 design-tokens.md 一致",
        "肉眼对照 shell/draft-overlay-states.html 冷蓝暗色"
      ]
    },
    {
      "id": "TC-01-2",
      "type": "manual",
      "title": "P0 shadcn-vue 装机 + Inter 字体 + 删除断链 symlink",
      "description": "覆盖 §4 P0 验收「token 正确」。验证 components.json 存在、@/components/ui/ 基础结构、Inter 字体加载、composables/__tests__ 断链 symlink 已清理",
      "steps": [
        "确认 components.json + src/components/ui/ 存在",
        "确认 Inter woff2 加载（Network 面板）",
        "ls composables/ 无红色断链"
      ]
    },
    {
      "id": "TC-02-1",
      "type": "ui",
      "title": "P1 App.vue shell 渲染 aside-region 透明 + main-panel 浮起",
      "description": "覆盖 UC-1 + TS-2。mount(App) 后断言 aside 透明背景、main-panel float 阴影层叠",
      "steps": [
        "mount(App) with pinia+i18n",
        "断言 aside region 背景透明（rgba 透明通道）",
        "断言 main-panel 有 shadow + rounded-lg"
      ]
    },
    {
      "id": "TC-02-2",
      "type": "ui",
      "title": "P1 traffic light 安全区 padding 非全屏态",
      "description": "覆盖 ADR-0016 + §6。断言非全屏时最左侧 panel header padding-left≈78px，全屏时无额外 padding",
      "steps": [
        "渲染 PanelBar 非全屏，断言 padding-left 命中 safe zone",
        "切换 fullscreen state，断言 padding 归零"
      ]
    },
    {
      "id": "TC-03-1",
      "type": "ui",
      "title": "P2 Sidebar 容器四态渲染（默认/激活/hover/折叠）",
      "description": "覆盖 UC-3 + §8.5 P2 v1。props 驱动四态视觉断言，折叠态 DEFERRED 的导航迁移不测",
      "steps": [
        "mount(SidebarItem, props: state='default'|'active'|'hover'|'collapsed')",
        "断言每态 class/style 符合 draft-five-states.html"
      ]
    },
    {
      "id": "TC-03-2",
      "type": "integration",
      "title": "P2 segmented tab 会话列表 ↔ 文件视图骨架切换",
      "description": "覆盖 UC-3 + §8.5 P2。File View 内容渲染 DEFERRED（G2-003），只验 tab 切换骨架。断言 store.activeTab 变化驱动视图",
      "steps": [
        "mount(Sidebar) + 激活 sessionStore",
        "点击 files tab，断言 tab 视图切换为空骨架",
        "切回 sessions tab，断言会话列表渲染"
      ]
    },
    {
      "id": "TC-03-3",
      "type": "integration",
      "title": "P2 Overview 入口按钮 + ⌘N 新建 session 快捷键",
      "description": "覆盖 UC-3 + §8.5 P2 + Round 3 v1 快捷键范围。断言 Overview 按钮触发 navigation.push('overview')，⌘N 触发 sessionStore.create()",
      "steps": [
        "mount(Sidebar)，点 Overview 按钮，断言 navigation.currentView==='overview'",
        "trigger keydown ⌘N，断言新建 session 调用"
      ]
    },
    {
      "id": "TC-04-1",
      "type": "integration",
      "title": "P3 Workspace 双 Panel 主从状态机渲染",
      "description": "覆盖 §8.5 P3 骨架。断言 master/active panel 与 slave/secondary panel 状态切换，split 单 session 场景 DEFERRED（G-023）不测",
      "steps": [
        "mount(Workspace) 双 panel 状态",
        "断言两 panel 均渲染且 active 标识正确",
        "切换 active，断言主从互换"
      ]
    },
    {
      "id": "TC-04-2",
      "type": "integration",
      "title": "P3 Panel 5 zone 空壳 throw 占位",
      "description": "覆盖 §8.5 P3。断言 5 zone（如 header/stream/companion/detail/footer）均有占位渲染，不报错",
      "steps": [
        "mount(Panel) with minimal mock",
        "断言 5 zone DOM 节点存在"
      ]
    },
    {
      "id": "TC-05-1",
      "type": "integration",
      "title": "P4 message-stream 7 块渲染（user/assistant text/tool_call/summary/error 等）",
      "description": "覆盖 UC-2 + §8.5 P4 + G2-006 fixture 丰富度。tool_call 仅简化展示（无审批按钮，G-018 DEFERRED）。断言每块类型正确渲染",
      "steps": [
        "灌 mock fixture（含主要块类型）到 chatStore",
        "mount(MessageStream)",
        "断言每块类型节点存在 + 流式光标"
      ]
    },
    {
      "id": "TC-05-2",
      "type": "ui",
      "title": "P4 回合折叠 pill 渲染与展开",
      "description": "覆盖 UC-2。断言回合折叠 pill 可点击展开/收起内部块",
      "steps": [
        "mount(TurnPill, props: collapsed=true)",
        "点击展开，断言 emitted('toggle') + 内部块可见"
      ]
    },
    {
      "id": "TC-05-3",
      "type": "ui",
      "title": "P4 composer S1 空 → S2 聚焦 → S5 发送中 → S6 停止 → 回 S1",
      "description": "覆盖 UC-2 主聊天路径 + §8.5 P4 v1。S3/S4（G2-002）/S7-S9（G-019）DEFERRED 不测。断言 4 态切换与按钮文案/图标",
      "steps": [
        "mount(Composer)，断言 S1 占位文案",
        "focus input → 断言 S2",
        "submit → 断言 S5 发送中（禁用+停止按钮）",
        "点停止 → 断言 S6 → 回 S1"
      ]
    },
    {
      "id": "TC-05-4",
      "type": "integration",
      "title": "P4 auto-scroll 基础版：新消息滚到底",
      "description": "覆盖 §8.5 P4 + Round 2 auto-scroll 基础版。用户上滚暂停/跳底提示（G2-007）DEFERRED 不测",
      "steps": [
        "mount(MessageStream) + 追加新消息",
        "nextTick 后断言 scrollTop === scrollHeight"
      ]
    },
    {
      "id": "TC-06-1",
      "type": "api",
      "title": "P5 api/transport.ts mock 注入点契约",
      "description": "覆盖 D2 mock 优先 + §6「ws-client 仅 useConnection+api/transport 引用」。断言 api/transport 是 mock/real 切换点，ws-client 不被业务直接 import",
      "steps": [
        "import api/transport，注入 mock impl",
        "断言 send 路由到 mockSend",
        "rg 'from.*ws-client' 仅 useConnection + api/transport"
      ]
    },
    {
      "id": "TC-06-2",
      "type": "api",
      "title": "P5 mock 返回镜像 shared 类型全字段（D7 契约）",
      "description": "覆盖 D7 mock 返回类型严格镜像 shared（非 Partial）。断言 mock fixture 字段与 shared/session.ts、shared/message.ts 一致",
      "steps": [
        "调 mock session.create/list，断言返回满足 Session 类型全字段",
        "调 mock chat.send 流式块，断言满足 MessageBlock 类型"
      ]
    },
    {
      "id": "TC-06-3",
      "type": "api",
      "title": "P5 session domains create/send/stream/switch 主流程契约",
      "description": "覆盖 UC-2/UC-3 + §6 VITE_MOCK 主流程。断言 domains/session 4 操作均 resolve 且触发对应事件",
      "steps": [
        "调 domains.session.create → 断言新 sessionId",
        "send(msg) → 断言 stream 事件序列（assistant text 块）",
        "switch(id) → 断言 active 切换"
      ]
    },
    {
      "id": "TC-07-1",
      "type": "integration",
      "title": "整体验收：VITE_MOCK=true 主流程完整对话（UC-2）",
      "description": "覆盖 §6 整体验收「VITE_MOCK 主流程跑通」+ UC-2 全链路。create→send→stream→switch→fork，全程不触达真 runtime",
      "steps": [
        "VITE_MOCK=true 启动，新建 session",
        "发送消息，观察流式 + 回合折叠",
        "切换会话，fork session，断言分支正确"
      ]
    },
    {
      "id": "TC-07-2",
      "type": "integration",
      "title": "整体验收：架构铁律 + lint + typecheck",
      "description": "覆盖 §6 整体验收剩余 4 项：ws-client 引用收敛、stores 互不 import、npm run lint、vue-tsc --noEmit",
      "steps": [
        "rg 'from.*ws-client' renderer/src/ 仅 useConnection + api/transport.ts",
        "rg 'from.*stores/' renderer/src/stores/ 为空",
        "npm run lint exit 0",
        "npm run typecheck exit 0"
      ]
    }
  ]
}
