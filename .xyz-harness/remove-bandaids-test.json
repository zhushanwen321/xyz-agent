{
  "testJudgment": {
    "necessityMet": "是——删兜底的前提（subscribe stateSnapshot 能力）已补齐并测试覆盖：T1 message-bus TC11 验证 subscribe 返回 4 个 state topic last-value；T2 protocol-seq TC3 验证 ReplyPayloadMap['session.subscribe'] 含 stateSnapshot 字段；T3 runtime-wiring TC3/TC4 验证 RPC handler 透传 stateSnapshot（fromSeq 不过滤）；T4 useMessageBusSubscription TC1 验证 applySnapshot dispatch stateSnapshot。删兜底（T5-T7）+ 保留验证（T8）均通过：30 个修改/新增的 renderer 测试 pass，runtime 2021 pass，renderer 全套 2435 pass，shared protocol-seq 8 pass。",
    "sufficiencyMet": {
      "gapsConfirmed": [],
      "gapsNewlyFound": [],
      "overlapsConfirmed": [],
      "note": "覆盖完整无 gap：补能力 4 层（bus/protocol/runtime/renderer）各有专测；删兜底 3 处（selectSession/submitFirstMessage）测试断言反转（RPC not called）验证删除生效；保留清单（store 方法/sync composables/useConnection.workflowUpdate/runtime RPC handler）相关测试无回归（全套 2435+2021 pass）。workflows 链路虽不进 stateSnapshot（runtime 只发 workflowUpdate 增量信号），但经 streamRing snapshot 内 workflowUpdate → triggerWorkflowReload → loadWorkflows RPC 闭环，store 自身方法保留，与 useWorkflowListSync focusedSessionId watch 互补——非 gap。"
    },
    "alternativesReconsidered": "无需重新考虑替代方案。design-review 阶段已评估 (A) selectSession 内 fire-and-forget subscribe（否决：违反 SSOT）/ (B) 只删 useSidebar（否决：留不一致半成品）/ (C) runtime publish 全量 session.workflows（否决：超范围），实际执行按计划走，无偏差。",
    "tradeoffCostRealized": [
      {
        "tradeoffRef": "TO1",
        "costRealized": true,
        "note": "workflows 切 session 时经两跳（streamRing workflowUpdate → triggerWorkflowReload → loadWorkflows RPC）。实测 useWorkflowListSync focusedSessionId watch 首拉兜底仍在，store 方法保留，无功能缺失。"
      },
      {
        "tradeoffRef": "TO2",
        "costRealized": true,
        "note": "commands/context 极端时序场景暂空降级与原 bandaid 一致（原 bandaid 失败也是 console.warn + 暂空，无 retry）。删除后由 subscribe stateSnapshot 接管，覆盖面更广（subscribe 在 ensureStreamSubscription 统一触发）。"
      }
    ],
    "riskOutcome": [
      {
        "riskRef": "R4",
        "outcome": "not-materialized",
        "note": "grep 确认保留清单完整：useSubagentListSync/useWorkflowListSync/useSidebarSubagentActions/Sidebar.vue/useConnection.workflowUpdate/api 层/runtime RPC handler 全部保留，T8 用例 + 全套测试无回归。"
      },
      {
        "riskRef": "R5",
        "outcome": "not-materialized",
        "note": "先补能力（T1-T4）再删兜底（T5），中间状态可独立验证。applySnapshot dispatch stateSnapshot 后 routeInbound 兜底分支正常更新 stores（commands/context/subagents 经 stateSnapshot，workflows 经 streamRing 增量信号）。"
      },
      {
        "riskRef": "R6",
        "outcome": "not-materialized",
        "note": "message-bus TC11 用例按 type 检索 stateSnapshot（find by msg.type）而非硬断言数组顺序，测试不脆弱。"
      }
    ]
  }
}
