{
  "format": "lite",
  "objective": "修复 session_end 断树问题（sidecar 替代 JSONL 追加）+ session.list 重命名为 config.sessions（消除 warn 噪音）",
  "waves": [
    {
      "id": "W1",
      "changes": [
        { "file": "packages/runtime/src/infra/pi/session-file-utils.ts", "description": "FR-1: persistSessionEnd 改写 <filePath>.meta.json sidecar 而非追加 JSONL entry。FR-2: extractSessionOutcome 优先读 sidecar (.meta.json)，miss fallback 现有 JSONL 尾读逻辑" },
        { "file": "packages/runtime/src/services/session/session-lifecycle.ts", "description": "FR-3: restoreSession/forkSession 在 client.switchSession(filePath) 前 strip JSONL 尾部的 session_end 及其后孤立 metadata entries（拷贝清理后临时文件或就地清理）。FR-4: deleteSession 两条路径（active/offline）都同步 trash 对应 .meta.json sidecar" },
        { "file": "packages/runtime/src/services/session/session-scanner.ts", "description": "FR-2: scannedToSummary 中 outcome 取值路径不变（经 scanSessionMeta 三读合一），确认 sidecar fallback 链路正确" },
        { "file": "packages/runtime/src/services/session/session-service.ts", "description": "FR-1: 确认 persistSessionOutcome 调用链（4 个终态点）经 ISessionStore 接口透传，调用方无感知" }
      ],
      "dependsOn": [],
      "priority": "P0"
    },
    {
      "id": "W2",
      "changes": [
        { "file": "packages/shared/src/protocol.ts", "description": "FR-5: ClientMessageType L19, ClientMessageMap L90, ServerMessageType L212, ServerMessageMap L295, message-routes L570 中 session.list → config.sessions" },
        { "file": "packages/runtime/src/transport/session-message-handler.ts", "description": "FR-5: handles 数组 L24 + case L75-76 中 session.list → config.sessions" },
        { "file": "packages/runtime/src/transport/message-broker.ts", "description": "FR-5: buildSessionListMsg L124 type + L215 label 中 session.list → config.sessions" },
        { "file": "packages/runtime/src/services/session/session-service.ts", "description": "FR-5: broadcastSessionList L133 type 中 session.list → config.sessions" },
        { "file": "packages/runtime/src/index.ts", "description": "FR-5: broadcastFn L131 cast 类型 session.list → config.sessions" },
        { "file": "packages/renderer/src/composables/features/useSidebar.ts", "description": "FR-5: onGlobalType L53 session.list → config.sessions" },
        { "file": "packages/renderer/src/api/domains/session.ts", "description": "FR-5: command L18 session.list → config.sessions" },
        { "file": "packages/renderer/src/api/mock/index.ts", "description": "FR-5: mock dispatch L77 type session.list → config.sessions" }
      ],
      "dependsOn": [],
      "priority": "P0"
    }
  ]
}
