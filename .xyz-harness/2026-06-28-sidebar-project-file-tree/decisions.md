---
topic: 2026-06-28-sidebar-project-file-tree
created_at: 2026-06-28
complexity_tier: L1
---

## 决策账本（append-only，一行一条决策）

> 表头与字段顺序固定（check 脚本/下游引用依赖）。`superseded_by` 空列留空；有值时原行 `status` 必须同步改 `revisited`。

| id | decision | rationale | classification | confirmed_by | stage | source | status | superseded_by |
|----|----------|-----------|----------------|--------------|-------|--------|--------|---------------|
| D-001 | 推翻 sidebar/spec.md §视图切换「File View 计数=当前 active session 改动文件数」，重新定义为「全项目文件树 + git 状态标注」。File View = session cwd 下完整目录树 | 现有 spec 与 handoff/用户预期矛盾；用户已裁决要全项目树。被否方案：维持改动清单语义 | `D-不可逆` | `ask_user` | `clarity` | `[from: 2026-06-28-sidebar-project-file-tree §1]` | `confirmed` | |
| D-002 | 文件树根目录 = 当前 active session 的 cwd（工作目录）。无 active session 时显示空态引导选 session | session.cwd 后端已就绪；git-service/tree 数据天然按 cwd。被否：项目根固定路径（多 worktree 不适用） | `D-可逆` | `agent-opinionated` | `clarity` | `[from: 2026-06-28-sidebar-project-file-tree §2]` | `confirmed` | |
| D-003 | git 状态标注数据源 = runtime `git.status`（请求-响应，全仓库 XY 双列码快照），而非 `message.file_changes`（单回合增量）。File View 切到该 session 时主动拉一次 + 后台轮询/事件刷新 | git.status 是真实工作区权威源，覆盖未改动/暂存/untracked 全态；message.file_changes 只覆盖 agent 本次改的文件，撑不起全树标注 | `D-可逆` | `agent-opinionated` | `clarity` | `[from: 2026-06-28-sidebar-project-file-tree §3]` | `confirmed` | |
| D-004 | 文件树浏览范围遵循 `.gitignore`（默认隐藏 node_modules/dist/.git 等），并提供「显示忽略项」开关（灰斜体区分）。不读取项目源码做语言级解析 | 隐藏巨量生成目录是文件树可用性前提；.gitignore 是项目自带的标准 ignore 源 | `D-可逆` | `agent-opinionated` | `clarity` | `[from: 2026-06-28-sidebar-project-file-tree §3,§5]` | `confirmed` | |
| D-005 | 点文件 → Panel 右侧 SideDrawer detail-pane 预览（文件内容/改动 diff），结构对齐 `draft-detail-pane.html`（dd-head + dd-tabs[文件名] + view-toggle[Diff/预览] + cs-diff + cs-foot） | 用户已裁决选方案 A。被否：B(仅定位 message-stream，对未改动文件无效)/C(暂不落地，体验割裂)。成本：SideDrawer 需改造(现锁死 per-Panel + tab 硬编码 terminal/browser/git)，detail-pane 组件待建，runtime 需放开 file.read 工作区权限 + 新增 git.diff | `D-不可逆` | `ask_user` | `clarity` | `[from: 2026-06-28-sidebar-project-file-tree §5]` | `confirmed` | |
| D-006 | 文件操作（新建/重命名/删除）本轮纳入 demo 设计，但标注为「后续实现」。demo 画右键菜单 + 工具栏交互，但明确其依赖 runtime file-service（当前后端 G4 缺失） | 用户指令「先做这个（全项目树）」，文件操作是 handoff §3 要求项但非当前最高优先 | `D-可逆` | `agent-opinionated` | `clarity` | `[from: 2026-06-28-sidebar-project-file-tree §8]` | `confirmed` | |
| D-007 | 文件/目录名默认亮色(fg)，选中=蓝粗体(accent)；.gitignore 忽略项=灰斜体(muted)。弃用旧「未改动文件 dim 弱化」 | 用户裁决：文件目录统一亮色，仅角标区分改动；忽略项灰斜体。design-tokens 无字号 scale，文件树取 12px 对齐 detail-pane .frow 惯例 | `D-可逆` | `ask_user` | `clarity` | `[from: 2026-06-28-sidebar-project-file-tree §4]` | `confirmed` | |
| D-008 | FileService 三层沿用 + port：services 层 FileService（编排，定义 IFileExecutor port）+ infra 层用 node:fs/promises 实现 port。不内联 fs、不塞进 GitService。（注：原含 IIgnoreReader port，已由 D-013 supersede） | 用户裁决。核心计算是技术编排但 fs 访问要可测试/可替换（mock），且与 GitService 的 IGitExecutor 范式对称。被否：内联 fs（不可测）；合入 GitService（职责膨胀，违反单一变化轴） | `D-不可逆` | `ask_user` | `architecture` | `[from: 2026-06-28-sidebar-project-file-tree §6]` | `revisited` | D-013 |
| D-009 | 懒加载：runtime file.tree 首次返回顶层+一级子目录；展开目录时 file.tree.expand 按需拉子节点。前端每节点独立加载态。适合大项目（node_modules 同级项多） | 用户裁决。全量加载在大项目（几千文件）首加载慢/内存大；虚拟滚动引入前端复杂度。被否：全量（性能差）；全量+虚拟滚动（过度工程） | `D-不可逆` | `ask_user` | `architecture` | `[from: 2026-06-28-sidebar-project-file-tree §5,§7]` | `confirmed` | |
| D-010 | 搭便车全纳入：①修 G1 event-adapter cwd 丢失（同区域真 bug）②修 G5 convertPiHistory 不还原 fileChanges ③整合 FileView/FileTreeRow/ChangeSetCard 重复 TreeNode 类型 ④放开 file.read 工作区权限（本主题前置）| 用户裁决全纳入。①②是同区域/相邻代码，③④是本主题必要前置（非可选）。意向列入 §1 搭便车表，真实工作量待⑤骨架验证 | `D-可逆` | `ask_user` | `architecture` | `[from: 2026-06-28-sidebar-project-file-tree §1]` | `confirmed` | |
| D-011 | FileNode 加载态状态机宽松（只守 loaded/error 终态 + loaded后折叠不丢数据 + error可重试），不设显式转换表 | 单用户交互、前端单写者、无并发竞争。严格转换表是过度设计。被否：显式转换表（过度收紧） | `D-可逆` | `agent-opinionated` | `architecture` | `[from: 2026-06-28-sidebar-project-file-tree §5]` | `confirmed` | |
| D-012 | 文件树与 git 标注数据彻底分离：FileNode 不含 gitStatus（纯结构 DTO）；前端 store 内 GitStatusOverlay 独立 ref（Map<path,GitFileStatus>）按 path join；**runtime file.tree 响应不合并 status**（纯两步：file.tree 拿结构 + git.status 拿标注） | 树结构稳定 vs status 频繁变，分离避免 status 变化触发整树重建；不合并避免「status 变了要重拉整棵树」的耦合。被否：树节点内嵌 gitStatus（耦合生命周期）；runtime 合并返回（重拉耦合） | `D-可逆` | `agent-opinionated` | `architecture` | `[from: 2026-06-28-sidebar-project-file-tree §10]` | `confirmed` | |
| D-013 | [REVISIT of D-008] IIgnoreReader port 取消，改为纯函数范式（shared/ignore-parser.ts 导出 compileIgnoreRules/matchPath 纯函数，FileService 经 IFileExecutor.readFile 读内容 + 调纯函数匹配）。放 shared 而非 infra（纯函数无 IO 属 shared）。与 git-status-parser 范式真对称 | ②追踪发现 git-service.ts:26 实证 GitService 直接 import infra/git-status-parser 纯函数豁免（不包 port），D-008 声称「范式对称」却把 ignore 包成 port，实际不对称。纯函数范式更干净，port 多余。放 shared 使 service 引纯函数不违反三层 AC-1 | `D-可逆` | `agent-opinionated` | `architecture` | `[REVISIT of D-008] from: 2026-06-28-sidebar-project-file-tree §6,§10]` | `confirmed` | |
| D-014 | P0/P1 划线：P0（阻塞项）= runtime 后端（#1 shared 基础+协议 / #2 FileService+fs-executor+handler）；前端全链路（store/composable/view/git.diff）+ 搭便车 4 项全 P1 | runtime 文件树协议是唯一「不做则前端无数据可渲染」的阻塞项，前端 store/view 需要协议先就位。被否：把前端 store 提 P0（store 依赖协议，本质 P1） | `D-可逆` | `ask_user` | `issues` | `[from: 2026-06-28-sidebar-project-file-tree issues §1]` | `confirmed` | |
| D-015 | 搭便车 4 项（修 G1 cwd / 修 G5 history / TreeNode→FileNode 迁移 / file.read 权限放开）全 P1，统一本轮纳入 | D-010 已意向全纳入。本阶段需先定 P 级，⑤code-arch 只回填打回不提级。④file.read 是 UC-6 前置、③迁移是 FileView 重写必要前置、①②是同区域/相邻真 bug | `D-可逆` | `ask_user` | `issues` | `[from: 2026-06-28-sidebar-project-file-tree issues §1]` | `confirmed` | |
| D-016 | 文件操作（新建/重命名/删除，UC-5）实现延后 P3，本轮仅保留 demo 设计稿。**协议骨架（类型+handler/service）不延后**（见 D-018）| D-006 已裁决本轮仅 demo。依赖的 runtime file 写协议（G4）当前缺失，实现纳入会显著增加工作量且超出「先做全项目树」指令。协议骨架单独见 D-018 | `D-可逆` | `ask_user` | `issues` | `[from: 2026-06-28-sidebar-project-file-tree issues §1]` | `confirmed` | |
| D-017 | [REVISIT of ②§5/§10] 文件树缓存失效机制：架构 §5 状态机补 `loaded→invalidated→loading` 转移；失效触发 = agent_end / file_changes ready 帧（agent 新建/删除文件后增量补/删节点）。避免「agent 新建文件进 GitOverlay 但无树节点 → G2 静默失效」 | ③追踪发现架构 §5 状态机只有 loaded/error 终态无 invalidated，未建模 agent 运行中文件变化场景。git.status（D-003 实时层）捕获新文件进 overlay，但 file.tree 快照（session 初加载）无对应节点 → 角标孤立失效。须反哺架构 §5 补 invalidated 转移 + §10 补失效决策 | `D-可逆` | `ask_user` | `issues` | `[REVISIT of ②§5/§10] from: tracing-round-1 角色 B]` | `confirmed` | |
| D-018 | UC-5 文件操作协议骨架不延后：新增独立 P1 issue 交付 file 写协议（create/rename/delete）的类型定义（protocol.ts）+ handler/service 骨架（file.write handler + FileService 写方法签名），**实现延后**（对齐 ②§1「协议骨架就绪，实现延后」） | ③追踪 M1 发现 D-016 与 ②§1 冲突。②§1 架构定稿承诺本轮交付协议骨架；孤立骨架无后端实现确无运行价值，但类型契约就位让下游 G4 实现有依归，且与 file.tree 协议（#1）同期定义保持 file 域协议一致性。用户裁决补骨架 issue | `D-可逆` | `ask_user` | `issues` | `[from: tracing-round-1 M1]` | `confirmed` | |
| D-019 | [REVISIT/BACKFED from ③] 展开态持久化：②§4「整体 reset」澄清为「切走按 sessionId 缓存展开态，切回 rehydrate 恢复」（非销毁）。调和 ①UC-3 AC-3.2「切回恢复」+ ③AC-3.5。store 内按 sessionId 保留展开路径集，切回 rehydrate | ③跨阶段审计发现 ①UC-3 AC-3.2 + ②§4 整体 reset（销毁）+ ③AC-3.5 模糊措辞三角矛盾，②无 rehydrate 机制。用户裁决按 session 缓存+rehydrate。反哺②§4 | `D-可逆` | `ask_user` | `issues` | `[REVISIT of ②§4] from: cross-stage-audit#2]` | `confirmed` | |
| D-020 | 显示忽略项开关 + 灰斜体本轮纳入：新增 P1 issue（FileNode 加 `ignored` 标志 + ignore-parser 双模式 matchPath/returnIgnored + FileService 编排隐藏/显示 + 协议参数 showIgnored + 前端开关）。D-004/D-007②灰斜体本轮落地（D-007①③ 默认亮色/弃dim 已在 #4 AC-4.14） | ③跨阶段审计发现 D-004（agent-opinionated）+ D-007（ask_user）confirmed，原裁决延后 P3 经用户复审改为本轮纳入。需 FileNode 模型扩展 + 双模式，反哺②§4 FileNode 模型 + §6/§7 ignore 双模式 | `D-可逆` | `ask_user` | `issues` | `[from: cross-stage-audit#1/3, deep-review]` | `confirmed` | |
| D-021 | [REVISIT/BACKFED from ②③⑤一致性审查] 前端 store 节点状态模型重构，三处子裁决合并：(a) **加载态对象化**——`nodeStatus: Map<path,LoadStatus>`（光杆 string）改为 `nodeStates: Map<sid,Map<path,NodeState>>`，NodeState=`{status,reason?}` 复合对象为单一权威源；`tree.children` 不再用三态编码加载态，仅承载结构数据；新增 `setNodeState(sid,path,state)` 单一更新入口，展开成功时原子「set loaded + merge children」消除双源不一致态。(b) **reason 入 store**——NodeState.reason 承载 error 态原因（来自 runtime FileError.code 经 WS error envelope `{code,message}` 透传），AC-4.7「错误文案区分 reason」数据源落地（非 transient 直传）。(c) **4 facet 全 per-session**——gitOverlay + nodeStates 都按 sessionId 分桶（Map<sid,Map<path,...>>），与 tree/expandedPaths 同生命周期，切回 rehydrate 覆盖全部 facet，兑现②§4「4 facet 生命周期一致」+ D-019「按 sessionId 缓存 rehydrate」 | ②③⑤一致性审查（建模/结构/演进三帧 subagent + 主 agent 交叉核实）发现三处设计-骨架偏差：骨架 `nodeStatus`（光杆 string）与 `tree.children` 三态双编码加载态、更新入口分离 → `nodeStatus=loaded 但 children=undefined` 不一致态（#1）；②§5 断言「error 态附带 reason 与 Status 正交」但 store 无 reason 落点、AC-4.7 链路断（#2）；tree/expandedPaths per-session 而 gitOverlay/nodeStatus per-path 不一致，与②§4/D-019 矛盾（#3）。根因统一：骨架用光杆原始值存复合状态。用户裁决：a+b 合并对象化解决（NodeState 单一权威源 + reason 同对象）、c 全 per-session。**非 D-不可逆**（store 内部状态模型重构，不改外部协议/类型契约），ask_user 裁决。**反哺②§3/§4/§5 + ⑤§3/§4/§9**；骨架代码（Step 7 生成于 D-021 前）仍为旧结构，列为 ⑥Wave W3 store 实现强制对齐项 | `D-可逆` | `ask_user` | `code-arch`（一致性审查） | `[REVISIT of ②§4/§5 + ⑤§3 store state] from: design-architecture/issues 一致性审查]` | `confirmed` | |
| D-022 | [BACKFED post-closeout] 文件树行视觉对齐 SSOT，三项合一：(a) **chevron 固定槽**——每行左侧 14px 固定宽度 `.chevron-slot`（目录放 ChevronRight、文件空占位），目录 folder icon 与文件 file icon 因此垂直对齐；(b) **单一 padding 公式**——目录行/文件行/子层提示行共用 `paddingLeft = depth*14+8`（提示行用 `(depth+1)*14+8`），废弃旧文件行 `+10px` 数值补偿；(c) **字号统一落地 D-007**——目录行从 `text-[11px]`（违反 D-007）改为 `text-[12px] font-mono`，与文件行一致。同时修复 FileTreeRow v-if/v-else 链断裂（独立 `<template v-if="dir&&isExpanded">` 插在目录 v-if 与文件 v-else 之间致 v-else 错绑 → 折叠目录被同时渲染为目录行+同名文件行） | 用户验收反馈三项视觉问题：(1) 折叠目录出现同名「文件」打不开——查为 v-if/v-else 链断裂的渲染 bug（非数据层，fs-executor isDirectory() 分类正确，`ls .agents` 仅一个真目录）；(2) 目录 11px vs 文件 12px 字号不一致，违反 D-007 已确认的「12px」；(3) 目录带 chevron 整体比文件缩进多，icon 不对齐——旧实现用 `depth*10+8`(目录) / `depth*10+18`(文件) padding 手算补偿 chevron 占位，目录 folder icon 起点≈26px vs 文件 file icon 起点≈18px 差 ~8px。spec/D-007 只规定字号未规定 icon 对齐，本裁决补缺口。用户裁决选「固定宽度 chevron 槽 + 单一 padding 公式 + 统一 12px font-mono」。同步补 FileTreeRow 组件渲染测试（v-if/v-else 回归防护），堵「三视角」事故——原仅 store/composable 白盒测试零组件渲染断言，故 bug 潜伏至用户验收 | `D-可逆` | `ask_user` | `closeout-followup` | `[from: 用户验收反馈 2026-06-30，BACKFED D-007 字号落地 + 补 icon 对齐 spec 缺口]` | `confirmed` | |
