{
  "testCases": [
    {
      "id": "TC1",
      "status": "active",
      "name": "rebuildGlobal 重挂 watcher 含新路径",
      "scenario": "SkillRegistry.rebuildGlobal() 应 close 旧 globalWatcher，用最新 configStore（getSkillPaths 返回新路径）resolveGlobalSkillDirs 重扫 globalCache + 重挂 watcher。",
      "input": "mock configStore.getSkillPaths 初始返回 []，initGlobal 后改为 ['/new/skill/dir']（existsSync mock 返回 true），调 rebuildGlobal()。",
      "expected": "chokidar watch 被以新 dirs 数组重新调用；globalCache 重扫（scanFn 被调）；旧 watcher.close 被调一次；notifyGlobalChange 触发 onChange。",
      "type": "unit",
      "verification": "unit"
    },
    {
      "id": "TC2",
      "status": "active",
      "name": "rebuildGlobal close 旧 watcher（await）",
      "scenario": "rebuildGlobal 调用旧 globalWatcher.close()，必须 await 完成后再 setupGlobalWatcher，避免新旧 watcher 并发。",
      "input": "已有 globalWatcher，调 rebuildGlobal()。",
      "expected": "旧 watcher.close 被调用；rebuildGlobal 是 async，await close 完成后才 setupGlobalWatcher（可通过 spy 顺序断言）。",
      "type": "unit",
      "verification": "unit"
    },
    {
      "id": "TC3",
      "status": "active",
      "name": "invalidateAllProjects 清空 projectCache + projectWatchers",
      "scenario": "SkillRegistry.invalidateAllProjects() 应 close 所有 projectWatchers + 清空 projectCache Map。",
      "input": "先 getProjectSkills('/cwdA') + getProjectSkills('/cwdB') 填充 projectCache + projectWatchers（mock watcher），调 invalidateAllProjects()。",
      "expected": "projectCache.size === 0；projectWatchers.size === 0；每个 projectWatcher.close 被调。",
      "type": "unit",
      "verification": "unit"
    },
    {
      "id": "TC4",
      "status": "active",
      "name": "onChange 收到 SkillChangeEvent 对象（签名 breaking）",
      "scenario": "onChange 回调签名从 (affectedSessionIds: string[]) 改为 (event: SkillChangeEvent)。notifyGlobalChange 构造 {scope:'global', affectedSessionIds}；notifyProjectChange(cwd) 构造 {scope:'project', cwd, affectedSessionIds}。",
      "input": "注册 onChange handler，触发 notifyGlobalChange 和 notifyProjectChange('/x')。",
      "expected": "global handler 收到 {scope:'global', affectedSessionIds:[...]}（无 cwd）；project handler 收到 {scope:'project', cwd:'/x', affectedSessionIds:[...]}。",
      "type": "unit",
      "verification": "unit"
    },
    {
      "id": "TC5",
      "status": "active",
      "name": "setSkillDirs handler 触发 rebuildGlobal + invalidateAllProjects + 广播",
      "scenario": "settings-message-handler config.setSkillDirs handler 改造后：写 discovery.json + 调 skillRegistry.rebuildGlobal().then(() => { invalidateAllProjects(); broadcastSkillCacheInvalidated('project') }) + 保留 broadcastSkillDirs + broadcastSkillList。",
      "input": "mock ctx.skillRegistry / ctx.broadcastSkillCacheInvalidated / ctx.broadcastSkillDirs / ctx.broadcastSkillList，发 config.setSkillDirs 消息。",
      "expected": "skillRegistry.rebuildGlobal 被调一次；rebuildGlobal resolve 后 invalidateAllProjects 被调；broadcastSkillCacheInvalidated('project') 被调；broadcastSkillDirs + broadcastSkillList 仍被调（服务 settings 弹窗）。",
      "type": "integration",
      "verification": "unit"
    },
    {
      "id": "TC6",
      "status": "active",
      "name": "index.ts onChange 回调广播失效信号",
      "scenario": "index.ts 的 skillRegistry.onChange 回调适配新签名：event => { reloadOrchestrator.onSkillChange(event.affectedSessionIds); broadcastSkillCacheInvalidated(event.scope, event.cwd) }。",
      "input": "review index.ts line 326 区域改动 + 类型检查通过。",
      "expected": "onChange 回调用 event.affectedSessionIds（不再用裸数组）；broadcastSkillCacheInvalidated 以 event.scope + event.cwd 调用；runtime tsc EXIT 0（除预存 codex-parser 错误）。",
      "type": "integration",
      "verification": "unit"
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "status": "active",
      "type": "refactor",
      "files": ["packages/runtime/src/services/skill-registry.ts"],
      "steps": [
        "定义 export interface SkillChangeEvent { scope: 'global'|'project'; cwd?: string; affectedSessionIds: string[] }",
        "private changeHandlers 签名改 Set<(event: SkillChangeEvent) => void>",
        "onChange(handler: (event: SkillChangeEvent) => void) 同步改签名",
        "notifyGlobalChange 构造 {scope:'global', affectedSessionIds: ids}",
        "notifyProjectChange(cwd) 构造 {scope:'project', cwd, affectedSessionIds: affected}",
        "抽出 private setupGlobalWatcher()（从 initGlobal 提取挂 watcher 逻辑：resolveGlobalSkillDirs + filter existsSync + watch + setupWatcher）",
        "initGlobal 改为：globalCache = await scanFn('') + setupGlobalWatcher()",
        "新增 async rebuildGlobal(): close 旧 watcher (await) + globalCache = await scanFn('') + setupGlobalWatcher() + await notifyGlobalChange()",
        "新增 invalidateAllProjects(): 遍历 projectWatchers close + clear projectWatchers + clear projectCache"
      ],
      "dependsOn": []
    },
    {
      "id": "T2",
      "status": "active",
      "type": "impl",
      "files": ["packages/runtime/src/transport/message-broker.ts"],
      "steps": [
        "新增 broadcastSkillCacheInvalidated(scope: 'global'|'project', cwd?: string): void",
        "构造 { type: 'config.skillCacheInvalidated', id: nextPushId(), payload: { scope, cwd } } 并 this.broadcast(msg)",
        "加 JSDoc 注释说明与 broadcastSkillList 的区分"
      ],
      "dependsOn": ["T1"]
    },
    {
      "id": "T3",
      "status": "active",
      "type": "impl",
      "files": ["packages/runtime/src/transport/server.ts"],
      "steps": [
        "在 line 145 附近的 broker 方法转发列表（broadcastSkillList / broadcastSkillDirs 等位置）新增 broadcastSkillCacheInvalidated: (scope, cwd?) => this.broker.broadcastSkillCacheInvalidated(scope, cwd)"
      ],
      "dependsOn": ["T2"]
    },
    {
      "id": "T4",
      "status": "active",
      "type": "impl",
      "files": ["packages/runtime/src/transport/settings-message-handler.ts"],
      "steps": [
        "SettingsHandlerContext 接口（line 13-28）新增 broadcastSkillCacheInvalidated(scope: 'global'|'project', cwd?: string): void",
        "config.setSkillDirs handler（line 102-110）改造：保留 configService.setSkillDirs + reply + broadcastSkillDirs + broadcastSkillList；新增 void this.ctx.skillRegistry.rebuildGlobal().then(() => { this.ctx.skillRegistry.invalidateAllProjects(); this.ctx.broadcastSkillCacheInvalidated('project') })",
        "加注释说明：rebuildGlobal 内部 notifyGlobalChange 触发 onChange → 广播 config.skillCacheInvalidated('global')；invalidateAllProjects 后显式广播 ('project')"
      ],
      "dependsOn": ["T3"]
    },
    {
      "id": "T5",
      "status": "active",
      "type": "refactor",
      "files": ["packages/runtime/src/index.ts"],
      "steps": [
        "line 326 skillRegistry.onChange 回调签名从 (affectedSessionIds) 改为 (event)",
        "回调体：void reloadOrchestrator.onSkillChange(event.affectedSessionIds)；新增 broker.broadcastSkillCacheInvalidated(event.scope, event.cwd)",
        "确认 broker 引用可达（line 253 broker: server，server 已转发 broadcastSkillCacheInvalidated）"
      ],
      "dependsOn": ["T1"]
    }
  ],
  "files": [
    { "id": "F1", "status": "active", "path": "packages/runtime/src/services/skill-registry.ts", "action": "modify", "description": "onChange 签名 breaking（SkillChangeEvent）+ 抽出 setupGlobalWatcher + 新增 rebuildGlobal/invalidateAllProjects" },
    { "id": "F2", "status": "active", "path": "packages/runtime/src/transport/message-broker.ts", "action": "modify", "description": "+broadcastSkillCacheInvalidated 方法" },
    { "id": "F3", "status": "active", "path": "packages/runtime/src/transport/server.ts", "action": "modify", "description": "broker 方法转发列表 +broadcastSkillCacheInvalidated" },
    { "id": "F4", "status": "active", "path": "packages/runtime/src/transport/settings-message-handler.ts", "action": "modify", "description": "SettingsHandlerContext 接口 + 方法签名；setSkillDirs handler 调 rebuildGlobal/invalidateAllProjects/广播" },
    { "id": "F5", "status": "active", "path": "packages/runtime/src/index.ts", "action": "modify", "description": "onChange 回调适配 SkillChangeEvent 签名 + 加广播" }
  ],
  "contracts": [
    { "id": "C1", "status": "active", "name": "SkillChangeEvent 接口", "contract": "interface SkillChangeEvent { scope: 'global'|'project'; cwd?: string; affectedSessionIds: string[] }。scope='global' 时 cwd 缺省（全局变动）；scope='project' 时 cwd 必填（项目变动所在 cwd）", "consumers": ["skillRegistry.onChange handler 签名", "index.ts onChange 编排", "reloadOrchestrator.onSkillChange 消费 affectedSessionIds"] },
    { "id": "C2", "status": "active", "name": "rebuildGlobal 副作用契约", "contract": "async rebuildGlobal(): close 旧 globalWatcher (await) → 重扫 globalCache → setupGlobalWatcher (新 dirs) → notifyGlobalChange。幂等（多次调用安全，每次 close+重建）。低频操作不加 in-flight 守卫", "consumers": ["settings-message-handler setSkillDirs handler"] },
    { "id": "C3", "status": "active", "name": "invalidateAllProjects 副作用契约", "contract": "invalidateAllProjects(): close 所有 projectWatchers + clear projectCache。下次 getProjectSkills 重扫重建。不发广播（由调用方显式 broadcastSkillCacheInvalidated）", "consumers": ["settings-message-handler setSkillDirs handler"] }
  ]
}
