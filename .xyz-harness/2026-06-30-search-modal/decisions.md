---
topic: search-modal
created_at: 2026-06-30
---

# 决策账本 · search-modal

append-only，一行一条决策。字段定义见 loop-skeleton.md Step 1.2 schema。

| id | decision | rationale | classification | confirmed_by | stage | source | status | superseded_by |
|----|----------|-----------|----------------|--------------|-------|--------|--------|---------------|
| D-001 | 四类分组（命令/文件/符号/会话）UI 全保留；命令/文件/会话三类接真实数据，符号类数据降级占位（显示「需要语言服务，暂不可用」），单开 topic 设计 | 符号搜索 zero base（无 LSP/tree-sitter），成本远超其他三类总和，与 agent 工作台定位偏离。三类真实数据已覆盖最高频场景 | `D-不可逆` | `ask_user` | `clarity` | `[from: search-modal §8]` | `confirmed` | |
| D-002 | 符号类占位策略：UI 保留分组位但结果区显示降级提示（非隐藏整个分组） | spec 定义四类结构，保留 UI 一致性；后续补符号数据时无需改 UI 结构 | `D-可逆` | `agent-opinionated` | `clarity` | `[from: search-modal §2 UC-5]` | `confirmed` | |
| D-003 | 文件搜索只搜文件路径（文件名/相对路径子串匹配），不搜文件内容；不引入 ripgrep；复用 runtime 现有 searchFiles 全递归 | 路径搜索覆盖「找文件」主场景；内容全文搜需引 rg 二进制（打包分发成本高），收益与 agent 工作台定位不匹配。searchFiles（file-service.ts:161，深度 8/上限 500）已就绪 | `D-不可逆` | `ask_user` | `clarity` | `[from: search-modal §7]` | `confirmed` | |
| D-004 | 命令搜索建统一注册表：应用内置命令（⌘N/⌘B/⌘, 等）+ pi slash 命令（/commit /fix 等）合并 | spec 要求「命令注册表（与 Sidebar nav 共享 ⌘N/⌘K 注册）」。新建注册表抽象，Sidebar keydown 复用同一注册表，消除硬编码 if/else。pi 命令复用现有 session.getCommands handler | `D-不可逆` | `ask_user` | `clarity` | `[from: search-modal §2 UC-2]` | `confirmed` | |
| D-005 | 会话搜索范围：全局跨项目（搜全部项目的所有会话，按 cwd 分组） | spec 定义搜索浮层为「全局·跨项目」性质；session.list 天然返回全量分组。符合「找上次那个 auth 项目的会话」主用例 | `D-不可逆` | `ask_user` | `clarity` | `[from: search-modal §2 UC-4]` | `confirmed` | |
| D-006 | 三类命中项全接入真实跳转：命令→执行 / 文件→DetailPane 打开预览 / 会话→切换 active session；跳转后关闭浮层 | spec 完整闭环。文件跳转复用现有 file.read + useDetailPane（不引入 Workspace 编辑器）；会话跳转复用 session.switch。危险命令确认见 D-008（本期不做） | `D-不可逆` | `ask_user` | `clarity` | `[from: search-modal §2 UC-2/3/4]` | `confirmed` | |
| D-007 | recents（最近项）localStorage 持久化，每类上限 5 项共 20 项 | 符合「最近找过」直觉，跨会话保留。spec 遗留④建议值 | `D-可逆` | `ask_user` | `clarity` | `[from: search-modal §2 UC-1]` | `confirmed` | |
| D-008 | 本期不做危险命令分级与二次确认，所有命令选中即执行 | xyz-agent 当前应用命令都是安全操作，pi slash 命令也是常规操作，无真正「危险」命令。后续有终止任务/删除会话等命令时再加 danger 标记 | `D-可逆` | `ask_user` | `clarity` | `[from: search-modal §8]` | `confirmed` | |
| D-009 | 命令合并去重键：按 name 唯一标识，不做复杂去重 | pi 命令带 `/` 前缀（/commit），应用命令无前缀（新建任务），天然不撞名。简单可逆 | `D-可逆` | `agent-opinionated` | `clarity` | `[from: search-modal §3]` | `confirmed` | |
| D-010 | 会话跳转只做切换 active session，不做进概览视图（spec 的 ?focus=overview） | 切换 session 是主路径，overview 跳转是次要场景，不阻塞 MVP。后续可增强 | `D-可逆` | `ask_user` | `clarity` | `[from: search-modal §8]` | `confirmed` | |
| D-011 | 三层架构（Interface / Service / Infrastructure），不建 DDD 四层 | 核心计算是技术流程编排（多数据源聚合 + 匹配 + 跳转路由），无复杂业务规则/策略引擎。Domain 层会是空壳，违反复杂度归位。与项目现有 domains/* + transport + runtime 三层一致 | `D-不可逆` | `ask_user` | `architecture` | `[from: search-modal §2]` | `confirmed` | |
| D-012 | 不做 port，保持扁平 | 4 个数据源是 4 个不同 api 调用（非同一接口多实现），删/翻/挪证伪为伪 port。匹配策略（子串）稳定无替换需求。与项目 domains/* 直接 transport.send 模式一致 | `D-不可逆` | `ask_user` | `architecture` | `[from: search-modal §6]` | `confirmed` | |
| D-013 | 纯 DTO/值对象，无 aggregate/实体 | SearchItem/Command/SessionSummary/FileNode 是已有 DTO，recents 是值对象。核心是编排非领域规则，建 aggregate 是空壳模型（装着 UI 逻辑的反模式） | `D-不可逆` | `ask_user` | `architecture` | `[from: search-modal §4]` | `confirmed` | |
| D-014 | 浮层状态松散，只守态不建显式转换表 | UI 状态（closed/open/recents/query/empty/loading/error）是 Vue computed 派生态（query/结果驱动），非有不变式守卫的领域状态机。显式表与 computed 冲突且无非法转换可防 | `D-可逆` | `ask_user` | `architecture` | `[from: search-modal §5]` | `confirmed` | |
| D-015 | 搭便车 3 项纳入（候选，待⑤骨架验证确认）：①Sidebar keydown 接入命令注册表 ②scrollIntoView→scrollIntoViewIfNeeded ③查询 debounce(120ms) | 三项都是 D-004 命令注册表/search 改造的顺势重构，消除硬编码/spec 违规/无 debounce。工作量到⑤验证 | `D-可逆` | `ask_user` | `architecture` | `[from: search-modal §1]` | `confirmed` | |
| D-016 | 命令注册表扩展 command store（加全局应用命令区），两区物理隔离（独立 ref：appCommands 静态 + slashCommands per-session Map） | 复用现有 store 基建；两区失效语义不同（应用命令静态无失效 vs slash per-session 动态），独立 ref 避免应用命令被 session 切换误触发响应式 | `D-不可逆` | `agent-opinionated` | `architecture` | `[from: search-modal §10]` | `confirmed` | |
| D-017 | P0/P1 划线=基础设施先行：命令注册表/匹配引擎/recents 三独立无依赖基础设施列 P0；search real domain/跳转编排/api 接线/SearchModal 改造列 P1 | P0 三项无前置依赖且被多个 P1 依赖（命令源/过滤/持久化），先行解锁后续；分层依赖清晰，Wave1 解锁 Wave2。符合长期架构优先——基础设施不稳则上层返工 | `D-可逆` | `ask_user` | `issues` | `[from: search-modal issues.md §P0/§P1]` | `confirmed` | |
| D-018 | 状态完整性优先级：loading 态(>200ms)+error 态(toast+空态)列 P1，Tab 切类列 P2 | loading/error 是核心闭环质量契约（AC-3.4 + no-silent-catch lint 强制 + 跳转失败必须反馈，lint 不过无法提交）；Tab 切类 spec 状态机有但无独立用例，列 P2 增强 | `D-可逆` | `ask_user` | `issues` | `[from: search-modal issues.md #8/#9]` | `confirmed` | |
| D-019 | 搭便车 3 项纳入 P2，标注「待⑤骨架验证确认」：Sidebar keydown 接入注册表/scrollIntoView→scrollIntoViewIfNeeded/debounce 120ms | 列 P2 让⑤阶段有具体验证对象；若⑤发现工作量超预期可降级。消除硬编码 if/else(D-004)/spec 违规/无 debounce | `D-可逆` | `ask_user` | `issues` | `[from: search-modal issues.md #10]` | `revisited` | D-020 |
| D-020 | debounce(120ms) 从搭便车 #10(P2) 提前到 #7(P1 SearchModal 改造)：异常猎手 AH-C2 发现 #4(P1) search domain 用 allSettled 每次按键发 3 源全量拉取，debounce 在 P2 造成 P1 阶段性能炸点。D-019 修订——debounce 升 P1（与 watch query 同 PR），scrollIntoViewIfNeeded/Sidebar keydown 留 P2 | 顺序倒挂修复：debounce 是 #4/#7 落地即需要的性能护栏，不能等到 P2。原 D-019「搭便车待⑤」对 debounce 过于保守（已验证无 debounce 的性能问题真实存在）。Sidebar keydown/scrollIntoView 仍留 P2 搭便车（无性能阻断） | `D-可逆` | `ask_user` | `issues` | `[REVISIT of D-019, from: tracing-round-1 AH-C2, issues.md #7 AC-7.15]` | `confirmed` | |
| D-021 | AC-4.7 反哺修订：MAX_SEARCH_RESULTS 引用值从 500 校正为 5000（file-service.ts:59 真实值，2026-06 已从 500 调整，注释说明旧值 500 会丢失根目录 AGENTS.md 等靠后文件） | 事实性错误（③引用旧值/废弃值），非设计变更。反哺纪律第1条：事实性矛盾必修订。不修订会让⑤写测试用错阈值、⑥验证用错数字 | `D-可逆` | `ask_user` | `nfr` | `[from: nfr Step1 事实核查, issues.md #4 AC-4.7 ← file-service.ts:59]` | `confirmed` | |
| D-022 | loadSeq 守卫迁移（前端组件→domain 内部）仅标正确性不变式，不单独生成 NFR-AC：BC-9/AC-4.4 已是正确性不变式（乱序响应不覆盖新结果），NFR 侧标「骨架约束」——loadSeq 字段存在于 search domain 即被⑤骨架 tsc 兜住 | 避免与 AC-4.4 重复登记（反膨胀）。loadSeq 是字段存在性约束（骨架 tsc 验证），非代码行为断言，不进⑤test-matrix 重复测试 | `D-可逆` | `ask_user` | `nfr` | `[from: nfr Step1, issues.md #4 AC-4.4 + §12 BC-9]` | `confirmed` | |
| D-023 | F-1 漏洞修复：新建 issue #17 [P1]（WS 源超时 race + onclose reject pending），blocked_by #4。追踪发现 ws-client.ts onclose 不 reject in-flight pending → allSettled 永不 settle → 浮层永久 loading 挂死。domain query() 对 file/session WS 源加超时 race（10s 量级，对齐 runtime），超时→reject→allSettled settle→分组空态+toast | 浮层挂死是 UX 阻断级 bug（用户看到永久 loading 无反馈），与 no-silent-catch 精神一致。**长期方案**——根因修复（WS 断连无超时是 transport 层遗漏）。并入 #4 会超载单 issue 并发面职责（#4 已有 loadSeq/缓存/冒泡链多个并发关注点），新建独立 issue 更清晰 | `D-不可逆` | `ask_user` | `nfr` | `[from: nfr Step2 tracing-round-1-issue4 F-1, ws-client.ts pending.ts 源码核查]` | `confirmed` | |
| D-024 | GAP-2 修复（file 跳转吞错层）：#6 补 AC 约束「file 跳转须直调 fileApi.read 校验不经 useDetailPane.openPreview 吞错层」，与 #4 AH-E1/E2（useFileSearch.load 吞错层）同构对称。useDetailPane.openPreview 现状 try/catch 吞错（设 status='error' 不抛），致 useSearchJump catch 永不触发，AC-6.5「file.read 失败→toast」假性 PASS | 与 #4 AC-4.5 error 冒泡链是同构问题（吞错层阻断失败冒泡），#6 侧须对称约束。不新建 issue，补入 #6 AC-6.x（与 AH-E1/E2 同模式标记） | `D-可逆` | `agent-opinionated` | `nfr` | `[from: nfr Step2 tracing-round-1-issue6 GAP-2, useDetailPane.openPreview 吞错层]` | `confirmed` | |
| D-025 | GAP-BL-1 修复：NFR #5 兼容性 ✅ 论据修订——mock SearchItem 与 real DTO 异构（FileNode/SessionSummary/SessionCommand 非 SearchItem 形态），domain 须做 DTO 映射（参考 lib/file-candidates.ts）。映射正确性由 #4 AC-4.1/4.2/4.3 + ⑤test-matrix 兜底 | 事实性错误（追踪 subagent 源码核查 real 源返回异构 DTO）。NFR 原论据「mock 与 real 结构一致」不成立。修订为正确论据，不推翻 D-005/D-011 决策 | `D-可逆` | `agent-opinionated` | `nfr` | `[from: nfr Step2 tracing-round-1-batch-low GAP-BL-1, requirements.md:137/192]` | `confirmed` | |
| D-026 | [REVISIT of issues #4 方案A「domain 编排」] search 编排归 composable（新建 `composables/features/useSearch.ts`），**不新建 api/domains/search.ts**。domain 严格只调 transport+pending 的现有铁律不变。`useSearch.query()` 在 composable 层聚合 4 源：① fileSearchStore 缓存命中→直返；② 缓存未命中→调 composer.getFileCandidates/session.list domain（WS）+ WS 源超时 race（#17）；③ commandStore（应用+slash）；④ useRecents（空查询）。DTO 映射（FileNode/SessionSummary/SessionCommand→SearchItem）+ matchFilter 过滤 + 分组在 composable 内。recents 读写 + 命令聚合 + DTO 映射提取为 lib 纯函数供 composable 调用。loadSeq 守卫、close 孤儿查询守卫在 composable 内 | ⑤grilling 发现现有 10 个 domain（composer/file/session/...）严格只调 transport+pending（不 import store/composable），search domain 若编排 3 源必跨 commandStore + fileSearchStore + composer/session domain，破坏「domain 纯净」铁律。归 composable 与 useSidebar/useFileSearch（同层跨 store+domain 编排）模式 100% 一致。**长期方案**——分层正确归位（编排逻辑在 composable 层，domain 层保持纯净），不引入「编排型 domain 特例」破坏一致性。代价：api/index.ts 的 search 门面 real 侧无 domain 可指，SearchModal 改调 useSearch.query 替代 api.search.query（这是真实改动，非门面破坏——search 本就不该有 domain） | `D-不可逆` | `ask_user` | `code-arch` | `[REVISIT of issues #4 方案A, from: code-arch Step1 grilling domain 边界歧义]` | `confirmed` | |
| D-027 | ⑥执行计划 Wave 细化：⑤§8 的 3-Wave 粗粒度 DAG（Wave1 P0 / Wave2 P1 全部 / Wave3 P2）细化为 5-Wave（Wave1 P0 基础设施 3 并行 / Wave2 P1 编排层 #4‖#6 2 并行 / Wave3 P1 UI 集成 #7+#5+#8 单切片 / Wave4 P2 增强 #9+#10 串行 / Wave5 验收 Gate）。细化理由：① Wave2 拆为编排层+UI 集成两层——#4/#6 是 composable（叶子消费方独立可验），#7 是 SearchModal 集成点（依赖 #4/#6 全就绪），分层后 Wave2 可先并行交付编排 composable 再集成 UI；② #17 物理合并入 #4（withWsTimeout 同文件）、#5 并入 #7（消费链原子性）、#8 并入 #7（loading/error 同文件）——合并消除文件冲突中间态；③ 末尾追加 Wave5 验收 Gate（闭环闸门，wave-template 强制）。5-Wave 非推翻⑤§8（⑤§8 是粗粒度 P 级分层，⑥按文件冲突+消费依赖细化是 execution 阶段职责） | Wave 细化由真实文件冲突（#7 独占 SearchModal.vue）+ 消费方依赖（#7 blocked_by #4/#6 跨 Wave 直达）+ wave-template 末尾验收 Wave 强制驱动。回退 3-Wave 要么丢 Wave2 内 #4‖#6 并行（串行 wall-clock 翻倍）要么隐式串行（#7 与 #4/#6 同 Wave 但 #7 依赖它们完成才能集成测试，实际串行却标并行）。5-Wave 是 execution 阶段对⑤粗粒度的合理工程细化 | `D-可逆` | `agent-opinionated` | `execution` | `[from: execution Step1 Wave 编排, ⑤§8 Wave DAG 粗粒度细化]` | `confirmed` | |
| D-028 | [实现期架构修复] SearchType/SearchItem 定义从 `api/mock/search-data.ts` 迁至 `lib/search-types.ts`（领域类型 SSOT 归位）。mock 反向 import 自 `@/lib/search-types`，`api/index.ts` 门面 re-export 改指 lib。7 个消费方（4 生产 + 3 测试）统一从 `@/lib/search-types` 导入 | 历史遗留：v1 搜索全在 mock 目录，SearchItem 定义埋在 mock 内部，生产代码（useSearch/useSearchJump）反向依赖 mock 目录获取领域类型——架构倒置（mock 是可选测试/开发依赖，不该持有生产领域类型）。提交前 grep 审查发现：useSearch.ts/useSearchJump.ts/lib/search-types.ts 从 `@/api/mock/search-data` 直穿 mock 内部，而 SearchModal.vue 却走 `@/api` 门面 re-export，两路矛盾。**长期方案**——类型 SSOT 归领域层（lib/search-types 与 Section/SearchCtx/AppCommand 等搜索领域类型同处），mock 反向依赖领域类型（方向正确），删除 mock 不再破坏生产构建。短期方案（统一走 api 门面）不改根因，mock 仍持有类型 | `D-不可逆` | `agent-opinionated` | `code` | `[实现期发现, from: commit 前 grep 审查 mock/search-data 反向依赖]` | `confirmed` | |
