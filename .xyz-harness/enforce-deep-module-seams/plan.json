{
  "format": "lite",
  "objective": "删除绕过既有 deep module 的旁路，将 R1(pi 事件订阅收口到 EventAdapter)、R2(tool-result 归一统一)、R5(subagent 流式收进 chat store)、R7(PluginInstaller 走 IInstaller port) 四处 seam 泄漏修复，恢复四个唯一 seam",
  "waves": [
    {
      "id": "W1",
      "changes": [
        "新建 packages/runtime/src/infra/pi/normalize-tool-result.ts：导出 NormalizedToolResult 接口(output/outputRaw/details/images)、ANSI_REGEX 常量、stripAnsi 函数、normalizePiToolResult(raw: unknown) 纯函数，吸收 pi tool result 三态判定(string/content-array/object) + text 提取(filter type==='text' + map .text + join) + images 提取(filter type==='image') + raw 内 details 提取 + stripAnsi/outputRaw 对称逻辑",
        "修改 packages/runtime/src/infra/pi/event-adapter.ts:38-42 删除本地 ANSI_REGEX/stripAnsi 定义，改为从 normalize-tool-result.ts import(stripAnsi 还服务 setStatus:267 和 setWidget:334，import 后调用点不变)；重写 handleToolExecutionEnd:134-195，删除三态判定 + text/images 提取逻辑(141-168)，改为调 normalizePiToolResult(raw=event.result ?? event.output)，保留 toolCallId/toolName/isError 读取(170-172) + writeContent 逻辑(174-182，依赖 extractPath 保留) + tool-call-end 中间事件组装(184-194)",
        "修改 packages/runtime/src/infra/pi/message-converter.ts:8-12 删除本地 ANSI_REGEX/stripAnsi 定义，改为从 normalize-tool-result.ts import；重写 toolResult 分支核心(87-96)，删除 rawText 提取 + stripAnsi + outputRaw 对称，改为调 normalizePiToolResult(toolResult) 取 output/outputRaw，赋值 tc.output/tc.outputRaw；保留顶层 toolResult.details 透传(100-102，来源是顶层非 raw 内) + isError→status(97) + extractHistoryFileChanges + WRITE/EDIT_TOOL_NAMES(46-64 不动)"
      ],
      "dependsOn": []
    },
    {
      "id": "W2",
      "changes": [
        "新建 packages/runtime/src/services/ports/plugin-installer.ts：定义 IPluginInstaller interface(install(packageSpecifier): Promise<InstallResult>; uninstall(pluginId, pluginPath): Promise<void>) + 从 plugin-installer.ts:10-15 搬迁 InstallResult 类型到此 port 文件",
        "修改 packages/runtime/src/infra/installers/npm-installer.ts：新增导出 downloadPackageTarball(spec: string, targetDir: string, opts?: {timeout?: number}): Promise<{name: string; version: string}> 组合函数，内部复用私有 parseSpec(202-211)→fetchMetadata(213-217)→resolveVersion(219-239)→downloadAndExtract(308-372)，不递归装依赖(区别于 installPackage)；不改动现有 installPackage/uninstallPackage/installDependencies/fetchLatestVersion 签名",
        "新建 packages/runtime/src/infra/installers/plugin-installer-adapter.ts：NpmPluginInstaller implements IPluginInstaller，constructor 接 pluginsDir；install(spec) 调 downloadPackageTarball(spec, targetDir=join(pluginsDir,name)) 下载解压(strip=1 落包内容到 targetDir 根) → 读 join(targetDir,'package.json') 校验 pkg.xyzAgent?.manifestVersion===1(失败则 rm(targetDir) 清理并返回 {success:false}) → 返回 {success:true, pluginId:pkg.name, path:targetDir}；uninstall(_id,path) 调 rm(path,{recursive,force})",
        "修改 packages/runtime/src/services/plugin-service/plugin-service.ts:16(import 改 IPluginInstaller/InstallResult 自 ports) + :63(字段类型 IPluginInstaller) + :91(改为从 this.deps.pluginInstaller 注入)",
        "修改 packages/runtime/src/services/plugin-service/plugin-types.ts:362-369 IPluginServiceDeps 加 pluginInstaller?: IPluginInstaller 字段",
        "修改 packages/runtime/src/index.ts:116-129 之间(在 configDir 算出后、PluginService 构造前)新增 const pluginInstaller = new NpmPluginInstaller(join(configDir,'plugins')) + 注入 PluginService deps.pluginInstaller",
        "修改 packages/runtime/src/interfaces.ts:284 InstallResult import 路径从 services/plugin-service/plugin-installer 改指 services/ports/plugin-installer",
        "删除 packages/runtime/src/services/plugin-service/plugin-installer.ts(86 行，职责已由 IPluginInstaller port + NpmPluginInstaller adapter 取代，含 child_process spawn)"
      ],
      "dependsOn": []
    },
    {
      "id": "W3",
      "changes": [
        "修改 packages/runtime/src/services/session/event-interpreter.ts:40-59 EventInterpreterOptions 新增两个回调：onTurnUsage?: (sessionId: string) => void(对应 pi turn_end，触发 tryPersistLabel 主路径) + onTurnFinalize?: (sessionId: string) => void(对应 pi agent_end，复位 isGenerating=false + tryPersistLabel 兜底)；turn-usage handler(116-120) 在 onContextUpdate 之后调 onTurnUsage；turn-end handler handleTurnEnd(228-246) 在转发 message.complete(230) 之后调 onTurnFinalize",
        "修改 packages/runtime/src/services/session/session-service.ts：applyContextUpdate(361-374) 扩展签名接收 totalTokens 参数并写入 session.tokenCount；新增 handleTurnUsageSideEffects(sessionId) 封装 tryPersistLabel + 新增 handleTurnEndSideEffects(sessionId) 封装 isGenerating=false + tryPersistLabel 兜底，暴露为 internal 方法(tryPersistLabel 当前 private:592-596 改为经这两个方法间接暴露，不直接 public)",
        "修改 packages/runtime/src/services/session/session-internal.ts:41 + packages/runtime/src/interfaces.ts:126 同步 applyContextUpdate 签名(加 totalTokens) 及新增的 side-effect 方法签名",
        "修改 packages/runtime/src/index.ts:140-178 createAdapter closure 内 new EventInterpreter(sessionId,{...}) 注入 onTurnUsage/onTurnFinalize 回调，分别调 sessionService.handleTurnUsageSideEffects(sid)/handleTurnEndSideEffects(sid)；onContextUpdate 回调(158-163) 扩展传 totalTokens 给 applyContextUpdate",
        "修改 packages/runtime/src/infra/pi/event-adapter.ts:246 handleTurnEndPi 核对 message 字段读取，若原 attachUsageListener(session-service.ts:558) 的 e.message ?? e.payload 双读在 event-adapter 只读 event.message，则补 ?? event.payload fallback 对齐(implementer 核对 pi 实际发 message 还是 payload)",
        "删除 packages/runtime/src/services/session/session-service.ts:462(attachUsageListener 注册)、469(ManagedSession.unsubUsageListener 字段赋值)、104+386+439(三处 unsubscribe 调用)、42(ManagedSession interface 的 unsubUsageListener 字段)、548-580(attachUsageListener 方法本体)——必须确认 W3 上述迁移完成后才删"
      ],
      "dependsOn": []
    },
    {
      "id": "W4",
      "changes": [
        "修改 packages/renderer/src/stores/chat.ts:165-168 之后新增 applySubagentStreamDelta(virtualId: string, lines: string[]): void action——迁移 subagent.ts:153-188 的 applyStreamDelta 逻辑：取 messages.value.get(virtualId)，逆序找最后一条 role==='assistant' && status==='streaming'，找到则 spread 后 content=lines.join('\\n') + 幂等补 {type:'text',refId:'text'} contentBlock(已有则不动)，找不到则 push 新 assistant Message(id:'sa-'+uuid, role:'assistant', status:'streaming', contentBlocks:[{type:'text',refId:'text'}])；不可变 set 回 messages.value；加 sealed guard(参考 chat-message-effects.ts:136-146 isLastAssistantStreaming)",
        "修改 packages/renderer/src/stores/chat.ts 新增 finalizeSubagentStream(virtualId: string): void action——取该分区 messages，把最后一条 status==='streaming' 的 assistant 翻成 status:'complete'(补 sealed 收口，对齐主路径 finalizeSession:303-341 不变量)；return 块(479-517) 导出这两个新 action",
        "修改 packages/renderer/src/stores/subagent.ts:153-188 删除 applyStreamDelta 函数(逻辑已迁 chat store)；subscribeStream(214-237) 的 :234 改调注入的 chatApplyDelta 回调(指向 chat.applySubagentStreamDelta)；终态分支(228-233)保留 stopStream/stopPolling，调 chat.finalizeSubagentStream(virtualId) 收口，fetchAndInject(134-147) 留 subagent store(含 IO 调 sessionApi.getSubagentHistory，经 chat.setMessages 公开入口注入，chat store 保持纯状态机零 api import)",
        "修改 packages/renderer/src/components/sidebar/Sidebar.vue:235-247 onSelectSubagent 的注入回调(:244 getMessages/:245 setMessages)改为注入 chat.applySubagentStreamDelta + chat.finalizeSubagentStream(Sidebar.vue 是 features 层可同时 import 两 store，:239 已 useChatStore())"
      ],
      "dependsOn": []
    }
  ],
  "testCases": [
    {
      "id": "U1",
      "layer": "mock",
      "scenario": "normalizePiToolResult 处理 pi tool result 三种 raw 形态",
      "steps": "分别传入 string(含 ANSI)、{content:[{type:'text',text:'hi'}]}、{foo:1}，断言 output/outputRaw",
      "expected": { "text": "string 'a\\x1b[31m'→output='a' outputRaw='a\\x1b[31m'；content-array→output='hi' outputRaw=undefined；object {foo:1}→output='{\"foo\":1}' outputRaw=undefined" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U2",
      "layer": "mock",
      "scenario": "normalizePiToolResult ANSI 对称回归——event-adapter 和 message-converter 两路重构后输出与重构前一致",
      "steps": "用同一组 pi tool result fixture，分别调重构后的 event-adapter.handleToolExecutionEnd 和 message-converter.convertPiHistory toolResult 分支，断言产出的 output/outputRaw/details 字段与重构前快照一致",
      "expected": { "text": "两路产出的 output/outputRaw/details 字段值与重构前 baseline 快照逐字段相等" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U3",
      "layer": "mock",
      "scenario": "stripAnsi 只从 normalize-tool-result.ts 导出，两文件无本地副本",
      "steps": "grep ANSI_REGEX 定义在 packages/runtime/src/infra/pi 下只出现在 normalize-tool-result.ts 一处；event-adapter.ts 和 message-converter.ts 均 import 自 normalize-tool-result",
      "expected": { "text": "ANSI_REGEX 定义仅 1 处(normalize-tool-result.ts)；两文件 stripAnsi 为 import 非本地定义" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U4",
      "layer": "mock",
      "scenario": "NpmPluginInstaller.install 成功路径——manifest 校验通过",
      "steps": "mock downloadPackageTarball 返回成功并在 targetDir 写入含 xyzAgent:{manifestVersion:1} 的 package.json，调 NpmPluginInstaller.install(spec)，断言返回值",
      "expected": { "text": "返回 {success:true, pluginId 非空, path===join(pluginsDir,pluginId)}" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U5",
      "layer": "mock",
      "scenario": "NpmPluginInstaller.install manifest 校验失败——清理 targetDir",
      "steps": "mock downloadPackageTarball 成功但 package.json 含 xyzAgent:{manifestVersion:2}，调 install，断言返回值且 targetDir 已删",
      "expected": { "text": "返回 {success:false} 且 targetDir 目录不存在" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "E1",
      "layer": "real",
      "scenario": "PluginService 经 port 真实安装 npm plugin 包到临时 pluginsDir",
      "steps": "用临时 pluginsDir，调 pluginService.installPlugin(一个已知测试包名)，验证文件落地 + registry.reload 扫到；测试后 rm -rf 临时目录清理",
      "expected": { "text": "pluginsDir/<name>/package.json 存在 且 registry.getDescriptor(name) 返回非空 descriptor" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U6",
      "layer": "mock",
      "scenario": "EventInterpreter turn-end/turn-usage handler 调用新回调",
      "steps": "构造 EventInterpreter 注入 spy 回调，分别 dispatch turn-usage 和 turn-end 中间事件，断言 onTurnUsage/onTurnFinalize 各被调用一次且参数含 sessionId",
      "expected": { "text": "turn-usage→onTurnUsage 调用 1 次参数含 sessionId；turn-end→onTurnFinalize 调用 1 次参数含 sessionId" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U7",
      "layer": "mock",
      "scenario": "删除 attachUsageListener 后副作用经中间事件链路保留——isGenerating 复位 + tryPersistLabel + tokenCount",
      "steps": "mock SessionService，经 onTurnFinalize 回调触发 handleTurnEndSideEffects，断言 isGenerating=false + tryPersistLabel 被调；经 onContextUpdate(含 totalTokens) 触发 applyContextUpdate，断言 tokenCount 被写入",
      "expected": { "text": "onTurnFinalize 后 session.isGenerating===false 且 tryPersistLabel 被调用；onContextUpdate(含 totalTokens=500) 后 session.tokenCount===500" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "E2",
      "layer": "real",
      "scenario": "完整 pi 事件流集成——删除 attachUsageListener 后正常生成完成路径不卡死",
      "steps": "构造 RpcClient mock 依次发送 message_start→text_delta→tool_execution_start→tool_execution_end→turn_end(含 usage)→agent_end(含 usage) 事件流，经 EventAdapter→Interpreter→SessionService 全链路，断言终态 session 状态；非破坏性(内存态)",
      "expected": { "text": "事件流结束后 session.isGenerating===false 且 tokenCount>0 且 inputTokens>0" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U8",
      "layer": "mock",
      "scenario": "chat.applySubagentStreamDelta 有 streaming assistant 时全量替换 + 幂等补 contentBlock",
      "steps": "预设 chat store messages 分区含一条 status:streaming 的 assistant(content='old', contentBlocks:[])，调 applySubagentStreamDelta(virtualId,['l1','l2'])，断言 content 替换且 contentBlocks 补一个 text 块；再次调用断言 contentBlocks 不重复 push",
      "expected": { "text": "首次调用后 content='l1\\nl2' contentBlocks 含 1 个 text 块；再次调用后 contentBlocks 仍 1 个 text 块(幂等)" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "U9",
      "layer": "mock",
      "scenario": "chat.finalizeSubagentStream 收口 streaming→complete",
      "steps": "预设分区含 status:streaming 的 assistant，调 finalizeSubagentStream(virtualId)，断言最后一条 assistant status 翻转",
      "expected": { "text": "最后一条 assistant message status==='complete'" },
      "executor": "agent",
      "requiresScreenshot": false
    },
    {
      "id": "E3",
      "layer": "real",
      "scenario": "mount Panel 组件树，subagent.stream_delta WS 事件经 store 收口后正确渲染",
      "steps": "mount Panel，mock WS 派发 subagent.stream_delta(含 lines)，断言 DOM 渲染 streaming assistant 消息且 content 正确；再派发 lines=undefined 终态，断言消息收口为 complete",
      "expected": { "text": "stream_delta 后 DOM 含 assistant 消息节点且文本===lines.join；终态后该消息 status 收口为 complete" },
      "executor": "agent",
      "requiresScreenshot": false
    }
  ]
}
