{
  "format": "lite",
  "objective": "前端思考档位判定复刻 pi-ai 0.84.1 getSupportedThinkingLevels 语义：map 缺失时默认 off/minimal/low/medium/high 可用（xhigh/max 需显式定义）；显式 map 按「叠加禁用」而非白名单判定；non-reasoning 模型只 off；THINKING_LEVELS 枚举补 minimal。修复 builtin 快照 thinkingLevelMap=null（832 个模型）被前端当作全 6 档开放的漂移。",
  "waves": [
    {
      "id": "W1",
      "changes": [
        "packages/core/src/domain/composer/thinking-levels.ts: THINKING_LEVELS 补 minimal + LEVEL_STRENGTH",
        "packages/core/src/domain/composer/thinking-levels.ts: resolveAvailableLevels(map?, reasoning?) 复刻 pi 规则（non-reasoning 只 off；map 缺失默认五档；null 禁用；xhigh/max 需显式；空结果 fallback off）",
        "packages/core/src/domain/composer/thinking-levels.ts: resolveThinkingKey fallback 改 highestAvailableLevel(map)",
        "core 单测 U1-U8 落盘 __tests__/thinking-levels.test.ts"
      ],
      "dependsOn": []
    },
    {
      "id": "W2",
      "changes": [
        "model-thinking.ts + thinking-level-sync.ts: deps 加 getModelReasoning 注入，返回 currentModelReasoning",
        "composer-shell.ts: getThinkingReasoning 回调注入并导出 currentModelReasoning",
        "Composer.vue: popover :reasoning 透传",
        "ThinkingLevelPopover.vue: reasoning prop + availableOptions 用新签名（U9）",
        "zh-CN/en-US i18n 补 composable.thinkingLevel.minimal（U10）",
        "model-thinking.test.ts U11 及受影响断言修正"
      ],
      "dependsOn": ["W1"]
    }
  ],
  "testCases": [
    {
      "id": "E1",
      "layer": "mock",
      "scenario": "core composer 域测试全绿（含新 U1-U8 与既有回归）",
      "steps": "cd packages/core && npx vitest run src/domain/composer --coverage --coverage.include='src/domain/composer/**' --coverage.thresholds.lines=60；期望 U1-U8 全部通过、既有用例无回退、coverage lines>=60%",
      "expected": { "text": "packages/core vitest 全绿且覆盖率 lines≥60%" },
      "file": "packages/core/src/domain/composer/thinking-levels.ts",
      "executor": "vitest",
      "requiresScreenshot": false
    },
    {
      "id": "E2",
      "layer": "mock",
      "scenario": "renderer 接入验证：popover 渲染 + i18n + core model-thinking 回归",
      "steps": "cd packages/renderer && npx vitest run src/__tests__/panel/thinking-levels.test.ts src/__tests__/i18n/thinking-levels-i18n.test.ts；cd packages/core && npx vitest run src/domain/composer/model-thinking.test.ts；期望 U9/U10 通过、mimo 场景可用档含 minimal 无 xhigh/max、切换重置无回归",
      "expected": { "text": "renderer 相关 vitest 全绿" },
      "file": "packages/renderer/src/components/panel/ThinkingLevelPopover.vue",
      "executor": "vitest",
      "requiresScreenshot": false
    }
  ]
}
