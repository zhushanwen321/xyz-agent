{
  "testCases": [
    {
      "id": "TC1",
      "name": "image segment 序列化为占位文本",
      "given": "segments = [{type:'image', path:'/tmp/x.png', name:'screenshot.png'}]",
      "when": "segmentsToText(segments)",
      "expected": "包含 '[图片: screenshot.png]' 占位（不包含 base64、不包含 path 绝对路径）"
    },
    {
      "id": "TC2",
      "name": "image + text 混合 segment 序列化",
      "given": "segments = [{type:'image', path:'/tmp/a.png', name:'a.png'}, {type:'text', text:'这张图怎么修'}]",
      "when": "segmentsToText(segments)",
      "expected": "'[图片: a.png] 这张图怎么修'（image 段后跟 text，中间补空格，复用现有 chip+text 补空格逻辑）"
    },
    {
      "id": "TC3",
      "name": "无 image 时行为不变（回归）",
      "given": "segments = [{type:'text', text:'hi'}, {type:'skill', name:'review'}]",
      "when": "segmentsToText(segments)",
      "expected": "与扩展前完全一致（'hi /skill:review'）—— image case 的加入不破坏既有 text/skill/file/mention 序列化"
    },
    {
      "id": "TC4",
      "name": "message.send images 字段类型校验",
      "given": "message.send payload",
      "when": "tsc 编译含 images?: Array<{data:string; mimeType:string}> 的 protocol.ts",
      "expected": "编译通过；不传 images 时行为不变（可选字段）；传 images 时类型为 {data, mimeType} 数组"
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "statement": "在 segments.ts 的 Segment 判别联合新增 {type:'image'; path:string; name:string}",
      "files": ["packages/shared/src/segments.ts"],
      "dependsOn": []
    },
    {
      "id": "T2",
      "statement": "在 segmentsToText 的 switch(seg.type) 加 image case，产出 '[图片: ' + seg.name + ']'",
      "files": ["packages/shared/src/segments.ts"],
      "dependsOn": ["T1"]
    },
    {
      "id": "T3",
      "statement": "在 protocol.ts 的 message.send payload 新增 images?: Array<{data:string; mimeType:string}>",
      "files": ["packages/shared/src/protocol.ts"],
      "dependsOn": []
    },
    {
      "id": "T4",
      "statement": "跑 vitest 验证 TC1-3 + tsc 验证 TC4",
      "files": [],
      "dependsOn": ["T2", "T3"]
    }
  ],
  "files": [
    {
      "path": "packages/shared/src/segments.ts",
      "change": "新增 image Segment 类型（联合加 case）+ segmentsToText 加 image 分支。不改正文注释里的类型列举（line 22 'file: 文件引用段'后补 'image: 图片段'）",
      "linesEstimated": 12
    },
    {
      "path": "packages/shared/src/protocol.ts",
      "change": "message.send 的 payload 类型加 images?: Array<{data:string; mimeType:string}>（line 196 现有 sessionId/content/subagent 后追加）",
      "linesEstimated": 1
    }
  ],
  "contracts": [
    {
      "id": "C1",
      "name": "image Segment 类型契约",
      "before": "Segment 联合 4 种：text/skill/file/mention",
      "after": "Segment 联合 5 种：text/skill/file/mention/image。image = {type:'image'; path:string; name:string}",
      "consumers": ["下游 renderer useChat（读 path 转 base64）", "ContextChipsBar（读 name 显示）", "ComposerInput（读 name 显示 badge）", "下游 runtime session-message-handler（透传 images 时无关，不读 image segment）"]
    },
    {
      "id": "C2",
      "name": "segmentsToText image 行为契约",
      "before": "image 段不存在",
      "after": "image 段产出 '[图片: {name}]' 文本占位，不产出 path（绝对路径不进展示文本），不产出 base64（走 images 通道）",
      "consumers": ["segmentsToPrompt（pi 边界，复用 segmentsToText）", "normalizeContent（消息归一化展示）"]
    },
    {
      "id": "C3",
      "name": "message.send images 字段契约",
      "before": "message.send = {sessionId; content; subagent?}",
      "after": "message.send = {sessionId; content; subagent?; images?}。images = Array<{data:string(base64无前缀); mimeType:string}>。可选，不传时行为不变",
      "consumers": ["renderer useChat.send（填）", "runtime session-message-handler（读，透传给 dispatcher）", "runtime rpc-client（补 type:'image' 成 ImageContent 传 pi）"]
    }
  ]
}
