# check-unsafe-stream-writes 护栏豁免清单（R1 行级豁免）
# ⚠ 本清单仅对 R1（裸流写）生效——R2（socket error 挂载）/ R4（readline 吞转发）
#   违规没有豁免通道，误登记其行号的条目会永久 ⚠ 未命中告警且违规照报，请直接修复代码。
#
# 格式：每行一条 `<相对仓库根路径>@<行号>`；`#` 开头为注释。
# pre-commit / CI 均无参调用本护栏，脚本自动读取本文件——豁免只能走这里登记
# （CLI 传参不生效，防「当次绿了、CI 永久红」的登记断裂）。
#
# 登记门槛：仅限确属 best-effort 写形态的误报；条目随本文件进 commit，review 可审计。
#
# 行号随代码漂移：条目失效时对应违规重现，重新评估后更新行号或删除条目。
# 例外（窗口）：若代码插入使新违规恰好落在同文件同一行号，条目会错误豁免它——
#   唯一线索是扫描输出的「ℹ 豁免命中」提示行，看到命中请核对命中处确属登记时的误报形态。
# 条目失效（代码已修复/删除）或笔误永不命中时，扫描结束打 ⚠ 未命中告警提示清理。
#
# 当前豁免条目：无
